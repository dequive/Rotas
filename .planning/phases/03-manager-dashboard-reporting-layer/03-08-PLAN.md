---
phase: 03-manager-dashboard-reporting-layer
plan: "08"
type: execute
wave: 2
depends_on: ["03-07"]
files_modified:
  - apps/manager/app/components/SidebarLayout.tsx
  - apps/manager/app/components/ControlTowerOverview.tsx
  - apps/manager/app/components/CostMarginBoard.tsx
autonomous: true
requirements:
  - CT-01
  - CT-02
  - CT-03

must_haves:
  truths:
    - "SidebarLayout renders identically using Tailwind classes — no .shell/.sidebar/.brand/.nav classes remain"
    - "SidebarLayout has the new /analytics nav entry with BarChart2 icon between Viagens and Contratos"
    - "ControlTowerOverview renders identically using Tailwind classes — no .tower-grid/.tower-metric/.worklist/.fleet-strip classes remain"
    - "CostMarginBoard renders identically using Tailwind classes — no .cost-kpis/.cost-panel/.margin-governance classes remain"
  artifacts:
    - path: "apps/manager/app/components/SidebarLayout.tsx"
      provides: "Sidebar migrated to Tailwind; /analytics nav entry added"
      contains: "bg-nav"
    - path: "apps/manager/app/components/ControlTowerOverview.tsx"
      provides: "CT overview migrated to Tailwind"
      contains: "text-muted"
    - path: "apps/manager/app/components/CostMarginBoard.tsx"
      provides: "CostMarginBoard migrated to Tailwind"
      contains: "border-line"
  key_links:
    - from: "apps/manager/app/components/SidebarLayout.tsx"
      to: "apps/manager/tailwind.config.ts"
      via: "bg-nav bg-soft text-muted border-line utility classes"
      pattern: "bg-nav|text-muted|border-line"
---

<objective>
Migrate the first batch of existing manager components from custom CSS classes to Tailwind utility classes, and add the /analytics nav entry to the sidebar (D-03, D-05).

Purpose: Component migration per D-03 locked decision. SidebarLayout is the shell for every page — it must be migrated first to validate the Tailwind setup. ControlTowerOverview is the primary daily dashboard view.

Output: 3 components fully migrated to Tailwind; /analytics nav entry added; builds clean.
</objective>

<execution_context>
@C:\Users\Quive\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Quive\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/03-manager-dashboard-reporting-layer/03-CONTEXT.md
@.planning/phases/03-manager-dashboard-reporting-layer/03-UI-SPEC.md

@apps/manager/app/globals.css
@apps/manager/tailwind.config.ts

<interfaces>
<!-- CSS class → Tailwind mapping from UI-SPEC Migration Contract -->

SidebarLayout classes to replace:
- .shell → `min-h-screen grid grid-cols-[248px_minmax(0,1fr)]`
- .sidebar → `bg-nav text-white p-5 flex flex-col`
- .brand → `text-[22px] font-extrabold mb-7`
- .nav → `grid gap-1.5`
- .nav a / .nav button → `text-[#d9e4f2] bg-transparent border-0 text-left px-3 py-2.5 rounded-md no-underline flex items-center gap-2 text-sm`
- .nav a.active / .nav a:hover → `bg-white/[0.12] text-white`
- .logout-btn → `mt-auto pt-5 w-full border-0 text-[#d9e4f2] bg-transparent flex items-center gap-2 px-3 py-2.5 rounded-md text-sm cursor-pointer`
- .main → `min-w-0 p-6`

ControlTowerOverview classes to replace:
- .tower-grid → `grid grid-cols-[repeat(auto-fill,minmax(160px,1fr))] gap-3 mb-6`
- .tower-metric → `min-h-[86px] p-3 bg-panel border border-line rounded-lg grid content-between gap-1.5 border-l-4 border-l-blue`
- .worklist-grid → `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`
- .worklist → `bg-panel border border-line rounded-lg overflow-hidden`
- .worklist header → `px-4 py-3 border-b border-line flex items-center justify-between`
- .worklist-item → `px-4 py-3 border-b border-line last:border-0 flex items-center justify-between gap-3`
- .fleet-strip → `grid grid-cols-[repeat(auto-fill,minmax(200px,1fr))] gap-3 mt-4`
- .fleet-fact → `bg-panel border border-line rounded-lg p-3 flex items-center gap-3`

CostMarginBoard classes to replace:
- .cost-kpis → `grid grid-cols-[repeat(auto-fill,minmax(160px,1fr))] gap-3 mb-4`
- .cost-panel / .cost-item → `bg-panel border border-line rounded-lg p-4`
- .cost-work-grid → `grid grid-cols-1 lg:grid-cols-2 gap-4`
- .margin-governance → `bg-panel border border-line rounded-lg p-4`

New SidebarLayout nav entry (per D-05):
```typescript
import { BarChart2 } from "lucide-react";
// Add to NAV_ITEMS between "viagens" and "contratos":
{ key: "analytics", label: "Análise", href: "/analytics", icon: BarChart2 }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate SidebarLayout.tsx to Tailwind + add /analytics nav entry</name>
  <files>apps/manager/app/components/SidebarLayout.tsx</files>
  <read_first>
    - apps/manager/app/components/SidebarLayout.tsx (read entire file — all JSX, className strings, imports)
    - apps/manager/app/globals.css (reference CSS classes being replaced — confirm .shell, .sidebar, .brand, .nav, .logout-btn, .main)
    - apps/manager/tailwind.config.ts (confirm color token names: bg-nav, bg-soft, text-muted, border-line, etc.)
  </read_first>
  <action>
Rewrite `apps/manager/app/components/SidebarLayout.tsx` replacing ALL custom CSS class strings with Tailwind utility classes. Keep all logic, imports, and component structure identical. Only change className prop values.

Migration rules:
- `className="shell"` → `className="min-h-screen grid grid-cols-[248px_minmax(0,1fr)]"`
- `className="sidebar"` → `className="bg-nav text-white p-5 flex flex-col"`
- `className="brand"` → `className="text-[22px] font-extrabold mb-7 tracking-normal"`
- `className="nav"` → `className="grid gap-1.5 flex-1"`
- Nav link base class → `"text-[#d9e4f2] bg-transparent no-underline px-3 py-2.5 rounded-md flex items-center gap-2 text-sm border-0"`
- Nav link active/hover → add `"hover:bg-white/[0.12] hover:text-white [&.active]:bg-white/[0.12] [&.active]:text-white"`
- Logout button → `"mt-auto pt-5 w-full border-0 text-[#d9e4f2] bg-transparent flex items-center gap-2 px-3 py-2.5 rounded-md text-sm cursor-pointer hover:bg-white/[0.12]"`
- `className="main"` → `className="min-w-0 p-6"`

Add /analytics nav entry to NAV_ITEMS array. Import `BarChart2` from lucide-react. Insert after the "viagens" entry and before the "contratos" entry:
```typescript
{ key: "analytics", label: "Análise", href: "/analytics", icon: BarChart2 }
```

After migration: verify no `className="shell"`, no `className="sidebar"`, no `className="brand"`, no `className="nav"`, no `className="main"`, no `className="logout-btn"` remain in the file.
  </action>
  <verify>
    <automated>cd /c/Users/Quive/OneDrive/Documents/Rotas/apps/manager && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "className=\"shell\"" apps/manager/app/components/SidebarLayout.tsx` returns NO match
    - `grep "className=\"sidebar\"" apps/manager/app/components/SidebarLayout.tsx` returns NO match
    - `grep "className=\"main\"" apps/manager/app/components/SidebarLayout.tsx` returns NO match
    - `grep "bg-nav" apps/manager/app/components/SidebarLayout.tsx` returns a match
    - `grep "analytics" apps/manager/app/components/SidebarLayout.tsx` returns a match (new nav entry)
    - `grep "BarChart2" apps/manager/app/components/SidebarLayout.tsx` returns a match
    - `npx tsc --noEmit` from apps/manager exits 0 (no type errors)
  </acceptance_criteria>
  <done>SidebarLayout uses only Tailwind classes; /analytics nav entry exists; TypeScript compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ControlTowerOverview.tsx and CostMarginBoard.tsx to Tailwind</name>
  <files>apps/manager/app/components/ControlTowerOverview.tsx, apps/manager/app/components/CostMarginBoard.tsx</files>
  <read_first>
    - apps/manager/app/components/ControlTowerOverview.tsx (read entire file — all className strings, props, sub-components)
    - apps/manager/app/components/CostMarginBoard.tsx (read entire file)
    - apps/manager/app/globals.css (reference the CSS classes being replaced: .tower-metric, .worklist, .fleet-fact, .cost-kpis, .margin-governance)
    - apps/manager/tailwind.config.ts (confirm color token names)
  </read_first>
  <action>
Migrate both components by replacing every custom CSS class string with equivalent Tailwind utility classes. Keep all JSX structure, props, TypeScript types, and logic unchanged. Only className values change.

**ControlTowerOverview.tsx — key mappings:**
- `.tower-metric` → `min-h-[86px] p-3 bg-panel border border-line rounded-lg grid content-between gap-1.5 border-l-[3px] border-l-blue`
- Metric label span → `text-[12px] font-bold text-muted`
- Metric value strong → `text-[26px] font-extrabold text-ink`
- `.worklist` → `bg-panel border border-line rounded-lg overflow-hidden`
- `.worklist` header → `px-4 py-3 border-b border-line flex items-center justify-between`
- `.worklist-item` → `px-4 py-3 border-b border-line last:border-b-0 flex items-center justify-between gap-3 text-sm`
- `.fleet-fact` → `bg-panel border border-line rounded-lg p-3 flex items-center gap-3`
- Fleet fact label → `text-[12px] text-muted`
- Fleet fact value → `font-bold text-ink`

**CostMarginBoard.tsx — key mappings:**
- `.cost-kpis` grid → `grid grid-cols-[repeat(auto-fill,minmax(160px,1fr))] gap-3 mb-4`
- `.cost-item` → `bg-panel border border-line rounded-lg p-4`
- `.margin-governance` section → `bg-panel border border-line rounded-lg p-4 mt-4`
- Item label → `text-[12px] font-bold text-muted mb-1`
- Item value → `text-[18px] font-extrabold text-ink`

After migration: verify no `.tower-metric`, `.worklist-grid`, `.worklist`, `.fleet-strip`, `.fleet-fact`, `.cost-kpis`, `.cost-item`, `.margin-governance` class strings remain in either file.
  </action>
  <verify>
    <automated>cd /c/Users/Quive/OneDrive/Documents/Rotas/apps/manager && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "className=\"tower-metric\"" apps/manager/app/components/ControlTowerOverview.tsx` returns NO match
    - `grep "className=\"worklist\"" apps/manager/app/components/ControlTowerOverview.tsx` returns NO match
    - `grep "className=\"cost-kpis\"" apps/manager/app/components/CostMarginBoard.tsx` returns NO match
    - `grep "bg-panel" apps/manager/app/components/ControlTowerOverview.tsx` returns a match
    - `grep "border-line" apps/manager/app/components/CostMarginBoard.tsx` returns a match
    - `grep "text-muted" apps/manager/app/components/ControlTowerOverview.tsx` returns a match
    - `npx tsc --noEmit` from apps/manager exits 0
  </acceptance_criteria>
  <done>ControlTowerOverview and CostMarginBoard use only Tailwind classes; TypeScript clean</done>
</task>

</tasks>

<verification>
```bash
cd /c/Users/Quive/OneDrive/Documents/Rotas/apps/manager && npm run build 2>&1 | tail -10
```
Build succeeds. No TypeScript errors.
</verification>

<success_criteria>
- SidebarLayout, ControlTowerOverview, CostMarginBoard: zero custom CSS class strings remaining
- SidebarLayout has /analytics nav entry with BarChart2 icon
- All three components use bg-nav, bg-panel, border-line, text-muted, text-ink Tailwind tokens
- `npm run build` in apps/manager succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-manager-dashboard-reporting-layer/03-08-SUMMARY.md`
</output>
