---
phase: 22-rbac-permission-based
plan: 03
type: execute
wave: 3
depends_on:
  - 22-01
  - 22-02
files_modified:
  - backend/alembic/versions/GENERATED_add_tenant_roles.py
  - backend/app/modules/users/models.py
  - backend/app/modules/users/schemas.py
  - backend/app/modules/users/service.py
  - backend/app/modules/users/router.py
  - backend/app/modules/auth/service.py
  - backend/tests/test_tenant_roles_api.py
autonomous: true
requirements:
  - SEC-RBAC-04

must_haves:
  truths:
    - "Tenant owner can POST /api/v1/tenant-roles with name/slug/permissions and receive 201"
    - "Permissions containing unknown strings are rejected 400 with code invalid_permissions"
    - "User assigned a custom role gets a JWT whose 'perms' claim reflects only the custom role permissions"
    - "Standard viewer with custom role billing.read only does NOT get fleet.read in JWT (overrides ROLE_PERMISSIONS)"
    - "Cross-tenant: owner of tenant B cannot list or update roles belonging to tenant A"
    - "Non-admin roles (manager, viewer) receive 403 on all /tenant-roles endpoints"
    - "tenant_roles table has RLS enabled and GRANT to rotas_app in the same migration file"
  artifacts:
    - path: "backend/alembic/versions/GENERATED_add_tenant_roles.py"
      provides: "tenant_roles table + users.custom_role_id + RLS ENABLE/FORCE/POLICY + GRANT in one file"
      contains: "ENABLE ROW LEVEL SECURITY"
    - path: "backend/app/modules/users/models.py"
      provides: "TenantRole ORM model + User.custom_role_id nullable FK"
    - path: "backend/app/modules/users/service.py"
      provides: "create_tenant_role(), list_tenant_roles(), assign_custom_role_to_user() service functions"
    - path: "backend/app/modules/users/router.py"
      provides: "GET /tenant-roles, POST /tenant-roles, PATCH /tenant-roles/{id}, POST /users/{id}/role"
    - path: "backend/app/modules/auth/service.py"
      provides: "_load_user_permissions() replaces inline ROLE_PERMISSIONS lookup; queries TenantRole when custom_role_id set"
    - path: "backend/tests/test_tenant_roles_api.py"
      provides: "6 integration tests covering full custom role lifecycle"
  key_links:
    - from: "backend/app/modules/auth/service.py"
      to: "backend/app/modules/users/models.py"
      via: "_load_user_permissions() queries TenantRole by user.custom_role_id at login time"
      pattern: "await db.get(TenantRole, user.custom_role_id)"
    - from: "backend/app/modules/users/router.py"
      to: "backend/app/core/rbac.py"
      via: "All 4 new endpoints gated on require_permission(ADMIN_USERS)"
      pattern: "require_permission(ADMIN_USERS)"
    - from: "backend/app/modules/users/service.py"
      to: "backend/app/core/rbac.py"
      via: "ALL_PERMISSIONS validates payload.permissions on create and update"
      pattern: "from app.core.rbac import ALL_PERMISSIONS"
---

<objective>
Add custom tenant roles: DB table, ORM model, service layer, 4 API endpoints, and auth-time
custom permission loading.

Purpose: Tenant owners can create roles like "Director Financeiro" with specific permission
subsets (e.g. billing.read + billing.write only) and assign them to users. The next login JWT
for that user will carry only those permissions, not the standard ROLE_PERMISSIONS set.

Output: tenant_roles table with RLS+GRANT inline; TenantRole model; 4 CRUD endpoints under
/api/v1/tenant-roles and /api/v1/users/{id}/role; _load_user_permissions() in auth service;
6 tests GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-rbac-permission-based/RESEARCH.md
@.planning/phases/22-rbac-permission-based/22-01-SUMMARY.md
@backend/app/modules/users/models.py
@backend/app/modules/users/router.py
@backend/app/modules/auth/service.py
@backend/app/core/rbac.py

<interfaces>
<!-- Existing User model — add custom_role_id after last column -->
class User(Base):
    __tablename__ = "users"
    id: Mapped[uuid.UUID]
    tenant_id: Mapped[uuid.UUID]
    email: Mapped[str]
    password_hash: Mapped[str]
    full_name: Mapped[str]
    phone: Mapped[str | None]
    role: Mapped[str]          # kept: "owner"|"admin"|"manager"|"viewer"|"mechanic"
    is_active: Mapped[bool]
    # ... mfa fields, timestamps
    # ADD: custom_role_id: Mapped[uuid.UUID | None]  — FK to tenant_roles.id ON DELETE SET NULL

<!-- ALL_PERMISSIONS from rbac.py — the validation whitelist -->
ALL_PERMISSIONS: frozenset[str]  # 23 strings, e.g. "billing.read", "fleet.write"

<!-- _create_user_tokens() current signature in auth/service.py -->
async def _create_user_tokens(db: AsyncSession, user: User, *, ip_address, user_agent) -> dict:
    permissions = ROLE_PERMISSIONS.get(user.role, frozenset())  # Phase 22-01 addition
    access_token, expires_in = create_access_token(..., permissions=permissions)

<!-- require_permission already wired on users router endpoints (from Plan 02) -->
from app.core.rbac import require_permission, ADMIN_USERS

<!-- Alembic v2.0 rule: RLS + GRANT must be in the SAME migration as CREATE TABLE -->
<!-- Any table with tenant_id gets: -->
<!--   op.execute("ALTER TABLE {t} ENABLE ROW LEVEL SECURITY;") -->
<!--   op.execute("ALTER TABLE {t} FORCE ROW LEVEL SECURITY;") -->
<!--   op.execute("CREATE POLICY rls_{t} ON {t} USING (tenant_id::text = current_setting('app.tenant_id', true));") -->
<!--   op.execute("GRANT SELECT, INSERT, UPDATE, DELETE ON {t} TO rotas_app;") -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic migration — tenant_roles table + users.custom_role_id + RLS + GRANT</name>
  <files>backend/alembic/versions/GENERATED_add_tenant_roles.py</files>
  <action>
    Run from backend/ directory to generate the revision file:
      cd backend && alembic revision --autogenerate -m "add_tenant_roles"

    This creates a file at backend/alembic/versions/{hex}_add_tenant_roles.py.
    The auto-generated upgrade() body will be incomplete — replace it entirely with
    the following explicit SQL. Keep the generated revision and down_revision values.

    UPGRADE BODY:
      op.create_table(
          "tenant_roles",
          sa.Column("id", postgresql.UUID(as_uuid=True), primary_key=True,
                    server_default=sa.text("gen_random_uuid()")),
          sa.Column("tenant_id", postgresql.UUID(as_uuid=True),
                    sa.ForeignKey("tenants.id", ondelete="CASCADE"), nullable=False),
          sa.Column("name", sa.String(80), nullable=False),
          sa.Column("slug", sa.String(80), nullable=False),
          sa.Column("permissions", postgresql.ARRAY(sa.Text()), nullable=False,
                    server_default=sa.text("ARRAY[]::text[]")),
          sa.Column("is_system", sa.Boolean(), nullable=False,
                    server_default=sa.text("false")),
          sa.Column("created_by", postgresql.UUID(as_uuid=True),
                    sa.ForeignKey("users.id", ondelete="SET NULL"), nullable=True),
          sa.Column("created_at", sa.DateTime(timezone=True), nullable=False,
                    server_default=sa.text("NOW()")),
          sa.Column("updated_at", sa.DateTime(timezone=True), nullable=False,
                    server_default=sa.text("NOW()")),
          sa.UniqueConstraint("tenant_id", "slug", name="uq_tenant_roles_tenant_slug"),
      )
      op.create_index("ix_tenant_roles_tenant_id", "tenant_roles", ["tenant_id"])

      # v2.0 rule: RLS + GRANT in same migration as CREATE TABLE
      op.execute("ALTER TABLE tenant_roles ENABLE ROW LEVEL SECURITY;")
      op.execute("ALTER TABLE tenant_roles FORCE ROW LEVEL SECURITY;")
      op.execute(
          "CREATE POLICY rls_tenant_roles ON tenant_roles "
          "USING (tenant_id::text = current_setting('app.tenant_id', true));"
      )
      op.execute("GRANT SELECT, INSERT, UPDATE, DELETE ON tenant_roles TO rotas_app;")

      # Add nullable FK column to users
      op.add_column(
          "users",
          sa.Column(
              "custom_role_id",
              postgresql.UUID(as_uuid=True),
              sa.ForeignKey("tenant_roles.id", ondelete="SET NULL"),
              nullable=True,
          ),
      )
      op.create_index("ix_users_custom_role_id", "users", ["custom_role_id"])

    DOWNGRADE BODY:
      op.drop_index("ix_users_custom_role_id", table_name="users")
      op.drop_column("users", "custom_role_id")
      op.execute("DROP POLICY IF EXISTS rls_tenant_roles ON tenant_roles;")
      op.drop_table("tenant_roles")

    Required imports at top of migration file:
      import sqlalchemy as sa
      from alembic import op
      from sqlalchemy.dialects import postgresql

    After writing the migration, apply it:
      cd backend && alembic upgrade head

    NOTE: The files_modified entry in this plan uses GENERATED_add_tenant_roles.py as a
    placeholder. The actual filename will be {autogenerated_hex}_add_tenant_roles.py.
    Update the SUMMARY.md with the real filename after completion.
  </action>
  <verify>
    <automated>cd backend && alembic current 2>&1 | grep "add_tenant_roles"</automated>
  </verify>
  <done>alembic upgrade head applies cleanly; alembic current shows this revision as HEAD; tenant_roles table exists with TEXT[] permissions column, RLS policy rls_tenant_roles, and GRANT to rotas_app; users.custom_role_id column exists as nullable UUID FK</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TenantRole ORM model, schemas, and service functions</name>
  <files>backend/app/modules/users/models.py, backend/app/modules/users/schemas.py, backend/app/modules/users/service.py</files>
  <behavior>
    - create_tenant_role() with unknown permission string raises ApiError 400, code "invalid_permissions"
    - create_tenant_role() with duplicate slug in same tenant raises ApiError 409, code "slug_conflict"
    - create_tenant_role() with slug containing uppercase raises ApiError 400, code "invalid_slug"
    - create_tenant_role() with valid inputs persists TenantRole row; permissions stored sorted
    - list_tenant_roles() returns only roles belonging to the requested tenant_id, ordered by name
    - assign_custom_role_to_user() with role from different tenant raises ApiError 404
    - assign_custom_role_to_user() with custom_role_id=None clears the FK (revert to standard role)
    - assign_custom_role_to_user() writes an audit log entry with action "user.custom_role_assigned"
  </behavior>
  <action>
    MODELS.PY — add TenantRole class after the existing User class. Do not modify the User
    class body here; the DB column was added by migration in Task 1. Add the ORM column:

      class TenantRole(Base):
          __tablename__ = "tenant_roles"
          __table_args__ = (
              UniqueConstraint("tenant_id", "slug", name="uq_tenant_roles_tenant_slug"),
          )
          id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True,
                                                 default=uuid.uuid4)
          tenant_id: Mapped[uuid.UUID] = mapped_column(
              ForeignKey("tenants.id", ondelete="CASCADE"), index=True
          )
          name: Mapped[str] = mapped_column(String(80))
          slug: Mapped[str] = mapped_column(String(80))
          permissions: Mapped[list[str]] = mapped_column(ARRAY(Text()), default=list)
          is_system: Mapped[bool] = mapped_column(Boolean, default=False)
          created_by: Mapped[uuid.UUID | None] = mapped_column(
              ForeignKey("users.id", ondelete="SET NULL"), nullable=True
          )
          created_at: Mapped[datetime] = mapped_column(
              DateTime(timezone=True), server_default=func.now()
          )
          updated_at: Mapped[datetime] = mapped_column(
              DateTime(timezone=True), server_default=func.now(), onupdate=func.now()
          )

    Also add to User class (after the last existing column, before __table_args__ or end):
      custom_role_id: Mapped[uuid.UUID | None] = mapped_column(
          ForeignKey("tenant_roles.id", ondelete="SET NULL"), nullable=True, index=True
      )

    Required new imports in models.py:
      from sqlalchemy.dialects.postgresql import ARRAY
      from sqlalchemy import Text  (if not already imported)

    SCHEMAS.PY — append to existing schemas file (do not replace existing schemas):

      import re as _re
      _SLUG_RE = _re.compile(r"^[a-z0-9_-]{1,80}$")

      class TenantRoleCreate(BaseModel):
          name: str
          slug: str
          permissions: list[str]

      class TenantRoleUpdate(BaseModel):
          name: str | None = None
          permissions: list[str] | None = None

      class TenantRoleRead(BaseModel):
          id: uuid.UUID
          tenant_id: uuid.UUID
          name: str
          slug: str
          permissions: list[str]
          is_system: bool
          created_at: datetime
          updated_at: datetime
          model_config = {"from_attributes": True}

      class AssignCustomRoleRequest(BaseModel):
          custom_role_id: uuid.UUID | None  # None = revert to standard role

    SERVICE.PY — add these functions and one private helper to the existing service file.
    Import at top of service.py additions block:
      import re as _re
      from sqlalchemy import select
      from app.core.rbac import ALL_PERMISSIONS
      from app.modules.users.models import TenantRole
      from app.modules.users.schemas import TenantRoleCreate, TenantRoleUpdate

    _SLUG_RE = _re.compile(r"^[a-z0-9_-]{1,80}$")

    def _serialize_tenant_role(role: TenantRole) -> dict:
        return {
            "id": role.id,
            "tenant_id": role.tenant_id,
            "name": role.name,
            "slug": role.slug,
            "permissions": role.permissions,
            "is_system": role.is_system,
            "created_by": role.created_by,
            "created_at": role.created_at,
            "updated_at": role.updated_at,
        }

    async def create_tenant_role(
        db: AsyncSession, tenant_id: UUID, payload: TenantRoleCreate, *, actor_id: UUID
    ) -> dict:
        if not _SLUG_RE.match(payload.slug):
            raise ApiError("invalid_slug",
                "Slug must be 1-80 chars: lowercase letters, digits, hyphens, underscores.",
                status_code=400)
        unknown = frozenset(payload.permissions) - ALL_PERMISSIONS
        if unknown:
            raise ApiError("invalid_permissions",
                f"Unknown permissions: {sorted(unknown)}",
                status_code=400,
                details={"unknown": sorted(unknown), "valid": sorted(ALL_PERMISSIONS)})
        existing = await db.scalar(
            select(TenantRole).where(
                TenantRole.tenant_id == tenant_id, TenantRole.slug == payload.slug
            )
        )
        if existing:
            raise ApiError("slug_conflict",
                "A role with this slug already exists.", status_code=409)
        role = TenantRole(
            tenant_id=tenant_id,
            name=payload.name,
            slug=payload.slug,
            permissions=sorted(payload.permissions),
            created_by=actor_id,
        )
        db.add(role)
        await db.flush()
        await db.commit()
        return _serialize_tenant_role(role)

    async def list_tenant_roles(db: AsyncSession, tenant_id: UUID) -> list[dict]:
        roles = await db.scalars(
            select(TenantRole)
            .where(TenantRole.tenant_id == tenant_id)
            .order_by(TenantRole.name)
        )
        return [_serialize_tenant_role(r) for r in roles.all()]

    async def assign_custom_role_to_user(
        db: AsyncSession, tenant_id: UUID, user_id: UUID,
        *, custom_role_id: UUID | None, actor_id: UUID,
    ) -> dict:
        user = await db.get(User, user_id)
        if not user or user.tenant_id != tenant_id:
            raise ApiError("user_not_found", "User not found.", status_code=404)
        if custom_role_id is not None:
            role = await db.get(TenantRole, custom_role_id)
            if not role or role.tenant_id != tenant_id:
                raise ApiError("role_not_found", "Custom role not found.", status_code=404)
        old_role_id = user.custom_role_id
        user.custom_role_id = custom_role_id
        await record_audit_log(
            db, tenant_id=tenant_id, user_id=actor_id,
            action="user.custom_role_assigned", entity_type="user", entity_id=user_id,
            old_values={"custom_role_id": str(old_role_id) if old_role_id else None},
            new_values={"custom_role_id": str(custom_role_id) if custom_role_id else None},
        )
        await db.commit()
        return {"user_id": user_id, "custom_role_id": custom_role_id}

    Line-length rule: keep all lines under 100 chars (ruff). Split long strings or args
    across lines using parentheses as needed.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.modules.users.service import create_tenant_role, list_tenant_roles, assign_custom_role_to_user; from app.modules.users.models import TenantRole; print('imports OK')"</automated>
  </verify>
  <done>TenantRole ORM model registered in users/models.py; User.custom_role_id FK column added to ORM; three service functions importable; schemas TenantRoleCreate/Update/Read/AssignCustomRoleRequest defined</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Router endpoints + auth service custom role loading + integration tests</name>
  <files>backend/app/modules/users/router.py, backend/app/modules/auth/service.py, backend/tests/test_tenant_roles_api.py</files>
  <behavior>
    - GET /api/v1/tenant-roles returns 200 with empty list for a new tenant (owner auth)
    - POST /api/v1/tenant-roles returns 201 with created role; slug and permissions in body
    - PATCH /api/v1/tenant-roles/{id} updates name and permissions; returns updated role
    - POST /api/v1/users/{id}/role assigns custom_role_id; next login JWT has custom perms only
    - Manager role receives 403 on POST /api/v1/tenant-roles
    - Tenant B owner cannot see or modify tenant A roles (cross-tenant isolation)
    - _load_user_permissions() returns frozenset(role.permissions) when user.custom_role_id is set
    - _load_user_permissions() falls back to ROLE_PERMISSIONS[user.role] when custom_role_id is None
    - _load_user_permissions() falls back to ROLE_PERMISSIONS[user.role] if custom_role_id points to deleted/cross-tenant role
  </behavior>
  <action>
    PART A — users/router.py: add 4 endpoints to the existing router. The users router
    already has prefix="/users". Add tenant-roles endpoints here using explicit full paths
    to avoid prefix collision, OR check if the users router uses APIRouter(prefix="/users")
    and add a second sub-router for tenant-roles. The cleanest approach given the existing
    router structure is to add these routes directly in the users router file with explicit
    path strings (not relying on prefix):

      from app.modules.users.models import TenantRole  # noqa: F401 ensure ORM registered
      from app.modules.users import schemas

      @router.get("/tenant-roles")
      async def list_tenant_roles_endpoint(
          principal: Annotated[Principal, Depends(require_permission(ADMIN_USERS))],
          db: Annotated[AsyncSession, Depends(get_session)],
      ) -> list[dict]:
          return await service.list_tenant_roles(db, principal.tenant_id)

      @router.post("/tenant-roles", status_code=201)
      async def create_tenant_role_endpoint(
          payload: schemas.TenantRoleCreate,
          principal: Annotated[Principal, Depends(require_permission(ADMIN_USERS))],
          db: Annotated[AsyncSession, Depends(get_session)],
      ) -> dict:
          return await service.create_tenant_role(
              db, principal.tenant_id, payload, actor_id=principal.user_id
          )

      @router.patch("/tenant-roles/{role_id}")
      async def update_tenant_role_endpoint(
          role_id: UUID,
          payload: schemas.TenantRoleUpdate,
          principal: Annotated[Principal, Depends(require_permission(ADMIN_USERS))],
          db: Annotated[AsyncSession, Depends(get_session)],
      ) -> dict:
          role = await db.get(TenantRole, role_id)
          if not role or role.tenant_id != principal.tenant_id:
              raise ApiError("role_not_found", "Custom role not found.", status_code=404)
          if role.is_system:
              raise ApiError("cannot_modify_system_role",
                  "System roles cannot be modified.", status_code=400)
          if payload.name is not None:
              role.name = payload.name
          if payload.permissions is not None:
              unknown = frozenset(payload.permissions) - ALL_PERMISSIONS
              if unknown:
                  raise ApiError("invalid_permissions",
                      f"Unknown: {sorted(unknown)}", status_code=400)
              role.permissions = sorted(payload.permissions)
          await db.commit()
          await db.refresh(role)
          return service._serialize_tenant_role(role)

      @router.post("/{user_id}/role")
      async def assign_custom_role_endpoint(
          user_id: UUID,
          payload: schemas.AssignCustomRoleRequest,
          principal: Annotated[Principal, Depends(require_permission(ADMIN_USERS))],
          db: Annotated[AsyncSession, Depends(get_session)],
      ) -> dict:
          return await service.assign_custom_role_to_user(
              db, principal.tenant_id, user_id,
              custom_role_id=payload.custom_role_id,
              actor_id=principal.user_id,
          )

    Add required imports to users/router.py: UUID, ALL_PERMISSIONS, ApiError, TenantRole.
    Note: if users router currently has prefix="/users", the endpoints above register as:
      GET  /api/v1/users/tenant-roles
      POST /api/v1/users/tenant-roles
      PATCH /api/v1/users/tenant-roles/{role_id}
      POST /api/v1/users/{user_id}/role
    Verify actual prefix by reading the router file before editing. Use whatever prefix
    gives clean non-conflicting URLs. Document final URLs in SUMMARY.md.

    PART B — auth/service.py: replace the inline ROLE_PERMISSIONS lookup added in Plan 01
    with a proper async helper. Add _load_user_permissions() before _create_user_tokens():

      async def _load_user_permissions(db: AsyncSession, user: User) -> frozenset[str]:
          """Load effective permissions once at login — embedded in JWT 'perms' claim.

          Priority: custom_role_id (DB lookup) > ROLE_PERMISSIONS[user.role] (static fallback).
          Falls back to static if custom role is missing or cross-tenant (data integrity guard).
          """
          from app.core.rbac import ROLE_PERMISSIONS
          from app.modules.users.models import TenantRole

          if user.custom_role_id is None:
              return ROLE_PERMISSIONS.get(user.role, frozenset())
          role = await db.get(TenantRole, user.custom_role_id)
          if role is None or role.tenant_id != user.tenant_id:
              return ROLE_PERMISSIONS.get(user.role, frozenset())
          return frozenset(role.permissions)

    In _create_user_tokens(), replace:
      permissions = ROLE_PERMISSIONS.get(user.role, frozenset())
    with:
      permissions = await _load_user_permissions(db, user)

    Remove the top-level "from app.core.rbac import ROLE_PERMISSIONS" import added in Plan 01
    if it is only used in _create_user_tokens() — the late import inside _load_user_permissions
    handles it. If it is used elsewhere in the file, keep it.

    PART C — test_tenant_roles_api.py (new file):

    Copy _make_tenant() and _dashboard_headers() helpers from test_rbac_deny.py verbatim.
    Add _make_owner_headers(tenant_id) convenience wrapper that calls _dashboard_headers(tid, "owner").

    Write these 6 tests:

    test_create_tenant_role_success:
      owner creates role {name:"Director", slug:"director", permissions:["billing.read","billing.write"]}
      → 201; response has "id", "slug"=="director", "permissions"==["billing.read","billing.write"]

    test_create_tenant_role_unknown_permission_rejected:
      owner creates role with permissions=["billing.read","totally.fake"] → 400
      response body error code == "invalid_permissions"

    test_list_tenant_roles_returns_only_own_tenant:
      Create 2 roles in tenant A. Create tenant B owner. Tenant B GET /tenant-roles → empty list.
      Tenant A GET /tenant-roles → 2 roles.

    test_assign_custom_role_jwt_perms_reflect_custom_role:
      Step 1: owner creates role with permissions=["billing.read"] only.
      Step 2: owner creates a viewer user (standard viewer would have fleet.read etc.).
      Step 3: owner POST /{user_id}/role with custom_role_id.
      Step 4: viewer user logs in via POST /api/v1/auth/login (create with real password hash).
              Use hash_password() from app.core.passwords to set password_hash on the User record.
              Log in via HTTP POST to /api/v1/auth/login.
      Step 5: decode access_token with PyJWT directly.
              assert claims["perms"] == ["billing.read"]  (sorted, only custom role perms)
              assert "fleet.read" not in claims["perms"]  (standard viewer perm absent)

    test_non_admin_cannot_create_role:
      manager headers → POST /tenant-roles → 403

    test_patch_tenant_role_cross_tenant_denied:
      Tenant A owner creates role-a. Tenant B owner PATCH /tenant-roles/{role_a_id} → 404.
      (Tenant B sees no such role in their context — correct isolation, not 403.)

    Run command: pytest backend/tests/test_tenant_roles_api.py -x --tb=short -v
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_tenant_roles_api.py tests/test_auth_api.py tests/test_token_refresh.py -x --tb=short -q</automated>
  </verify>
  <done>4 new endpoints registered and accessible; _load_user_permissions() queries TenantRole when custom_role_id is set; all 6 new tests GREEN; test_auth_api.py and test_token_refresh.py still GREEN</done>
</task>

</tasks>

<verification>
1. cd backend && alembic current — shows add_tenant_roles revision as HEAD
2. psql: \dp tenant_roles — shows rls_tenant_roles policy; rotas_app has SELECT/INSERT/UPDATE/DELETE
3. cd backend && pytest tests/test_tenant_roles_api.py -x --tb=short -v — 6 tests GREEN
4. cd backend && pytest tests/ -x --tb=short -q — full suite GREEN, no regressions
5. Manual: POST /api/v1/auth/login for user with custom_role_id set → decode JWT → "perms" contains only custom role permissions, not standard ROLE_PERMISSIONS set
6. Manual: POST /api/v1/tenant-roles with permissions=["unknown.perm"] → 400, code "invalid_permissions"
</verification>

<success_criteria>
- tenant_roles table exists with TEXT[] permissions, RLS enabled (FORCE mode), GRANT to rotas_app — all in one migration file
- users.custom_role_id nullable FK column exists and ORM model reflects it
- create_tenant_role() validates permissions against ALL_PERMISSIONS; unknown strings → 400
- Slug uniqueness per tenant enforced with 409 conflict response
- Assigning custom role to user causes next JWT "perms" to reflect only that role's permissions
- Standard role lookup (custom_role_id=NULL) continues to work via ROLE_PERMISSIONS fallback
- 6 integration tests GREEN including JWT perms roundtrip test
- Full test suite GREEN (no regressions from model changes or auth service update)
</success_criteria>

<output>
After completion, create `.planning/phases/22-rbac-permission-based/22-03-SUMMARY.md`
documenting: generated Alembic revision ID, actual endpoint URLs registered (with prefix),
whether test 4 (JWT custom perms roundtrip) required any workaround, and full test run output.
</output>
