---
phase: 25-platform-tenant-separation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/auth.py
  - backend/app/core/tokens.py
  - backend/app/core/rbac.py
  - backend/app/modules/auth/service.py
  - backend/alembic/versions/e1a2b3c4d5f6_add_platform_users_and_audit_logs.py
  - backend/app/modules/platform/__init__.py
  - backend/app/modules/platform/models.py
  - backend/app/modules/platform/schemas.py
  - backend/app/modules/platform/auth_service.py
  - backend/app/modules/platform/audit_service.py
  - backend/app/modules/platform/auth_router.py
  - backend/app/main.py
  - backend/tests/test_platform_scope_isolation.py
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
  - PLAT-03

must_haves:
  truths:
    - "A JWT with scope=platform is accepted by require_platform_role() and rejected by require_permission()"
    - "A JWT with scope=dashboard (tenant) is rejected by require_platform_role() with 403 even if role equals platform_admin"
    - "Platform login endpoint returns a JWT with scope=platform and no tenant_id claim"
    - "All existing 348 tenant tests pass unchanged — scope defaults preserve backward compat"
    - "platform_users and platform_audit_logs tables exist with GRANT to rotas_app"
  artifacts:
    - path: "backend/app/modules/platform/models.py"
      provides: "PlatformUser, PlatformAuditLog ORM models"
      exports: ["PlatformUser", "PlatformAuditLog"]
    - path: "backend/app/modules/platform/auth_router.py"
      provides: "POST /api/v1/platform/auth/login"
    - path: "backend/app/core/rbac.py"
      provides: "require_platform_role() dependency and PLATFORM_* role constants"
      contains: "PLATFORM_ADMIN"
    - path: "backend/alembic/versions/e1a2b3c4d5f6_add_platform_users_and_audit_logs.py"
      provides: "platform_users + platform_audit_logs DDL + GRANT"
    - path: "backend/tests/test_platform_scope_isolation.py"
      provides: "6 scope isolation tests"
  key_links:
    - from: "backend/app/modules/platform/auth_router.py"
      to: "backend/app/modules/platform/auth_service.py"
      via: "login() service call"
    - from: "backend/app/modules/platform/auth_service.py"
      to: "backend/app/core/tokens.py create_access_token()"
      via: "scope=platform, tenant_id=None"
    - from: "backend/app/core/rbac.py require_platform_role()"
      to: "backend/app/core/auth.py get_current_platform_principal()"
      via: "Depends() injection — separate from get_current_principal"
---

<objective>
Build the complete core infrastructure for platform/tenant scope separation: new scope="platform"
JWT family, require_platform_role() RBAC dependency, platform_users/platform_audit_logs database
tables, the platform authentication module, and 6 isolation tests that prove both positive and
negative cases.

Purpose: This is the structural foundation. Every subsequent platform endpoint in Plans 02 and 03
depends on these primitives. Nothing ships in Phase 25 without this plan completing successfully.

Output: Platform auth login endpoint, require_platform_role() FastAPI dependency, migration with
two new tables, 6 GREEN tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@backend/app/core/auth.py
@backend/app/core/tokens.py
@backend/app/core/rbac.py
@backend/app/modules/auth/service.py
@backend/app/main.py
@backend/app/modules/audit/service.py
</context>

<interfaces>
From backend/app/core/auth.py — current Principal (DO NOT remove or rename existing fields):

    @dataclass(frozen=True)
    class Principal:
        subject: str
        tenant_id: UUID          # change annotation to UUID | None
        scope: str
        role: str | None = None
        user_id: UUID | None = None
        driver_id: UUID | None = None
        device_id: str | None = None
        permissions: frozenset[str] | None = None

get_current_principal() currently rejects scope not in {"dashboard", "driver_app"} with 401.
Do NOT modify that function — add get_current_platform_principal() as a new function.

From backend/app/core/tokens.py — current signature:
    def create_access_token(*, tenant_id: UUID, scope: str, ...) -> tuple[str, int]
Change tenant_id to UUID | None. Omit "tenant_id" key from JWT claims when None.

From backend/app/core/rbac.py — existing require_permission() is the pattern to follow.
It imports Principal and get_current_principal inside the closure to break circular imports.
Do the same in require_platform_role() but import get_current_platform_principal instead.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend core auth/token primitives for platform scope</name>
  <files>
    backend/app/core/tokens.py
    backend/app/core/auth.py
    backend/app/core/rbac.py
  </files>
  <behavior>
    - create_access_token(tenant_id=None, scope="platform") produces JWT with no tenant_id claim
    - get_current_platform_principal() with a dashboard-scoped token raises ApiError 403
    - get_current_platform_principal() with a valid platform token returns Principal(scope="platform", tenant_id=None)
    - require_platform_role("platform_admin") with dashboard token raises 403 before reading role
    - require_platform_role("platform_admin") with platform token role="platform_support" raises 403
    - require_platform_role("platform_admin") with platform token role="platform_admin" returns principal
    - PLATFORM_ADMIN = "platform_admin", PLATFORM_SUPPORT = "platform_support", PLATFORM_BILLING = "platform_billing"
  </behavior>
  <action>
1. backend/app/core/tokens.py
   Change `tenant_id: UUID` to `tenant_id: UUID | None`.
   When tenant_id is None, omit the "tenant_id" key from claims entirely (do not write "None").
   Change sub construction: use platform_user_id claim key when both user_id and driver_id are None:
     sub = f"{scope}:{user_id or driver_id}" if (user_id or driver_id) else f"{scope}:platform"
   Also add optional platform_user_id param (UUID | None = None); embed as str in claims when present.
   Keep all other behaviour identical. Ruff line-length 100.

2. backend/app/core/rbac.py
   Add at the bottom of the file, after ALL_PERMISSIONS definition:

   # Platform role constants (Phase 25)
   PLATFORM_ADMIN = "platform_admin"
   PLATFORM_SUPPORT = "platform_support"
   PLATFORM_BILLING = "platform_billing"

   PLATFORM_ROLES: frozenset[str] = frozenset({PLATFORM_ADMIN, PLATFORM_SUPPORT, PLATFORM_BILLING})

   def require_platform_role(*roles: str) -> Callable:
       """FastAPI dependency for platform-scoped endpoints.

       Validation is dual and ordered:
         1. Delegates to get_current_platform_principal() which ONLY accepts scope="platform" tokens.
            A tenant JWT (scope="dashboard") is rejected at the decode step with 403 — role is never
            read. This prevents a tenant user with a custom role named "platform_admin" from gaining
            access.
         2. Checks that principal.role is in the allowed set.
       """
       from app.core.auth import Principal, get_current_platform_principal  # noqa: PLC0415

       allowed: frozenset[str] = frozenset(roles)

       async def dependency(
           principal: Annotated[Principal, Depends(get_current_platform_principal)],
       ) -> Principal:
           # Scope already validated by get_current_platform_principal.
           # Belt-and-suspenders: assert it here so this guard is self-contained.
           if principal.scope != "platform":
               raise ApiError(
                   "forbidden",
                   "Platform scope required.",
                   status_code=status.HTTP_403_FORBIDDEN,
               )
           if principal.role not in allowed:
               raise ApiError(
                   "forbidden",
                   "Insufficient platform role for this operation.",
                   status_code=status.HTTP_403_FORBIDDEN,
                   details={"required_roles": sorted(allowed)},
               )
           return principal

       return dependency

3. backend/app/core/auth.py
   a. Change Principal.tenant_id annotation from `UUID` to `UUID | None`.
   b. Add import: from app.modules.platform.models import PlatformUser (guard with TYPE_CHECKING
      or use late import inside function to avoid circular imports).
   c. Add new async function get_current_platform_principal():

   async def get_current_platform_principal(
       authorization: Annotated[str | None, Header(alias="Authorization")] = None,
   ) -> Principal:
       if not authorization or not authorization.lower().startswith("bearer "):
           raise ApiError("unauthorized", "Authentication token is required.",
                         status_code=status.HTTP_401_UNAUTHORIZED)
       token = authorization.split(" ", 1)[1]
       settings = get_settings()
       try:
           claims = jwt.decode(
               token,
               settings.jwt_secret_key.get_secret_value(),
               algorithms=[settings.jwt_algorithm],
           )
           if claims.get("typ") != "access":
               raise jwt.DecodeError("invalid token type")
       except (jwt.PyJWTError, KeyError, TypeError, ValueError) as exc:
           raise ApiError("invalid_token", "Authentication token is invalid or expired.",
                         status_code=status.HTTP_401_UNAUTHORIZED) from exc

       # Scope check MUST come before role — rejects tenant tokens with custom role names.
       if claims.get("scope") != "platform":
           raise ApiError(
               "forbidden",
               "Platform scope required.",
               status_code=status.HTTP_403_FORBIDDEN,
           )

       platform_user_id_str = claims.get("platform_user_id")
       if not platform_user_id_str:
           raise ApiError("invalid_token", "Token missing platform_user_id.",
                         status_code=status.HTTP_401_UNAUTHORIZED)
       platform_user_id = UUID(platform_user_id_str)

       async with AsyncSessionLocal() as db:
           from app.modules.platform.models import PlatformUser  # noqa: PLC0415
           user = await db.get(PlatformUser, platform_user_id)
           if not user or not user.is_active:
               raise ApiError("forbidden", "Platform user inactive or not found.",
                             status_code=status.HTTP_403_FORBIDDEN)

       return Principal(
           subject=claims["sub"],
           tenant_id=None,
           scope="platform",
           role=claims.get("role"),
           user_id=platform_user_id,
       )
  </action>
  <verify>
    <automated>cd c:/Users/Quive/OneDrive/Documents/Rotas/backend && python -c "
from app.core.rbac import PLATFORM_ADMIN, PLATFORM_SUPPORT, PLATFORM_BILLING, require_platform_role
from app.core.auth import get_current_platform_principal
from app.core.tokens import create_access_token
import jwt as pyjwt
t, _ = create_access_token(tenant_id=None, scope='platform', role='platform_admin', platform_user_id=None)
c = pyjwt.decode(t, options={'verify_signature': False})
assert c['scope'] == 'platform', c
assert 'tenant_id' not in c, c
print('Task 1 primitives OK')
"</automated>
  </verify>
  <done>
    PLATFORM_ADMIN/SUPPORT/BILLING constants importable from rbac.py.
    require_platform_role() importable.
    get_current_platform_principal() importable.
    create_access_token(tenant_id=None) produces JWT without tenant_id claim.
    Principal.tenant_id is UUID | None without breaking existing usages.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Alembic migration + platform module (models, schemas, auth_service, audit_service, auth_router)</name>
  <files>
    backend/alembic/versions/e1a2b3c4d5f6_add_platform_users_and_audit_logs.py
    backend/app/modules/platform/__init__.py
    backend/app/modules/platform/models.py
    backend/app/modules/platform/schemas.py
    backend/app/modules/platform/auth_service.py
    backend/app/modules/platform/audit_service.py
    backend/app/modules/platform/auth_router.py
    backend/app/main.py
  </files>
  <behavior>
    - POST /api/v1/platform/auth/login with valid email+password returns {"access_token": str, "token_type": "bearer", "expires_in": int, "user": {...}} where access_token decodes to scope="platform"
    - POST /api/v1/platform/auth/login with wrong password returns 401
    - POST /api/v1/platform/auth/login with inactive platform user returns 401
    - record_platform_audit() inserts a row into platform_audit_logs and commits
  </behavior>
  <action>
1. backend/alembic/versions/e1a2b3c4d5f6_add_platform_users_and_audit_logs.py
   Use revision id "e1a2b3c4d5f6", down_revision = current head (find with: alembic heads).
   Depends_on = None (standalone migration).

   def upgrade():
       op.create_table(
           "platform_users",
           sa.Column("id", sa.UUID(), primary_key=True, server_default=sa.text("gen_random_uuid()")),
           sa.Column("email", sa.String(255), nullable=False, unique=True),
           sa.Column("password_hash", sa.String(255), nullable=False),
           sa.Column("role", sa.String(50), nullable=False),
           sa.Column("is_active", sa.Boolean(), nullable=False, server_default="true"),
           sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False,
                     server_default=sa.text("now()")),
           sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False,
                     server_default=sa.text("now()")),
       )

       # INTENTIONALLY NO RLS on platform_users.
       # This table is platform-scoped, not tenant-scoped. RLS policies use app.tenant_id which
       # has no meaning here. Platform authentication uses direct PK lookups by platform_user_id
       # after JWT validation — no tenant context is ever set in the session for platform flows.
       # GRANT is still required so rotas_app can read/write via the ORM.
       op.execute("GRANT SELECT, INSERT, UPDATE, DELETE ON platform_users TO rotas_app")

       op.create_table(
           "platform_audit_logs",
           sa.Column("id", sa.UUID(), primary_key=True, server_default=sa.text("gen_random_uuid()")),
           sa.Column("actor_id", sa.UUID(), nullable=False),
           sa.Column("actor_role", sa.String(50), nullable=False),
           sa.Column("target_tenant_id", sa.UUID(), nullable=True),
           sa.Column("action", sa.String(100), nullable=False),
           sa.Column("resource_type", sa.String(100), nullable=True),
           sa.Column("resource_id", sa.UUID(), nullable=True),
           sa.Column("payload", postgresql.JSONB(), nullable=True),
           sa.Column("ip_address", sa.String(45), nullable=True),
           sa.Column("timestamp", sa.TIMESTAMP(timezone=True), nullable=False,
                     server_default=sa.text("now()")),
       )

       # INTENTIONALLY NO RLS on platform_audit_logs.
       # Platform audit logs record cross-tenant operator actions. They must be readable by
       # platform_admin without a tenant_id session variable set. RLS would block all reads.
       op.execute("GRANT SELECT, INSERT ON platform_audit_logs TO rotas_app")
       op.create_index("ix_platform_audit_logs_actor_id", "platform_audit_logs", ["actor_id"])
       op.create_index("ix_platform_audit_logs_target_tenant_id", "platform_audit_logs",
                       ["target_tenant_id"])

   def downgrade():
       op.drop_table("platform_audit_logs")
       op.drop_table("platform_users")

2. backend/app/modules/platform/__init__.py — empty file.

3. backend/app/modules/platform/models.py
   Define SQLAlchemy ORM models matching the migration above:

   class PlatformUser(Base):
       __tablename__ = "platform_users"
       id: Mapped[UUID] = mapped_column(primary_key=True, default=uuid4)
       email: Mapped[str] = mapped_column(String(255), unique=True)
       password_hash: Mapped[str] = mapped_column(String(255))
       role: Mapped[str] = mapped_column(String(50))
       is_active: Mapped[bool] = mapped_column(default=True)
       created_at: Mapped[datetime] = mapped_column(default=lambda: datetime.now(UTC))
       updated_at: Mapped[datetime] = mapped_column(default=lambda: datetime.now(UTC),
                                                     onupdate=lambda: datetime.now(UTC))

   class PlatformAuditLog(Base):
       __tablename__ = "platform_audit_logs"
       id: Mapped[UUID] = mapped_column(primary_key=True, default=uuid4)
       actor_id: Mapped[UUID]
       actor_role: Mapped[str] = mapped_column(String(50))
       target_tenant_id: Mapped[UUID | None]
       action: Mapped[str] = mapped_column(String(100))
       resource_type: Mapped[str | None] = mapped_column(String(100))
       resource_id: Mapped[UUID | None]
       payload: Mapped[dict | None] = mapped_column(JSONB)
       ip_address: Mapped[str | None] = mapped_column(String(45))
       timestamp: Mapped[datetime] = mapped_column(default=lambda: datetime.now(UTC))

4. backend/app/modules/platform/schemas.py
   Pydantic models (pydantic v2):

   class PlatformLoginRequest(BaseModel):
       email: str
       password: str

   class PlatformUserRead(BaseModel):
       id: UUID
       email: str
       role: str
       is_active: bool
       created_at: datetime
       model_config = ConfigDict(from_attributes=True)

   class PlatformTokenResponse(BaseModel):
       access_token: str
       token_type: str = "bearer"
       expires_in: int
       user: PlatformUserRead

5. backend/app/modules/platform/auth_service.py
   async def platform_login(db: AsyncSession, email: str, password: str) -> dict:
       user = await db.scalar(select(PlatformUser).where(PlatformUser.email == email))
       if not user:
           raise ApiError("invalid_credentials", "Invalid email or password.", status_code=401)
       if not verify_password(password, user.password_hash):
           raise ApiError("invalid_credentials", "Invalid email or password.", status_code=401)
       if not user.is_active:
           raise ApiError("platform_user_inactive", "Platform user is inactive.", status_code=401)
       access_token, expires_in = create_access_token(
           tenant_id=None,
           scope="platform",
           role=user.role,
           platform_user_id=user.id,
       )
       return {
           "access_token": access_token,
           "token_type": "bearer",
           "expires_in": expires_in,
           "user": {"id": user.id, "email": user.email, "role": user.role,
                    "is_active": user.is_active, "created_at": user.created_at},
       }

   Use verify_password from app.core.passwords (same as tenant auth).
   Import create_access_token from app.core.tokens.

6. backend/app/modules/platform/audit_service.py
   async def record_platform_audit(
       db: AsyncSession,
       *,
       actor_id: UUID,
       actor_role: str,
       action: str,
       target_tenant_id: UUID | None = None,
       resource_type: str | None = None,
       resource_id: UUID | None = None,
       payload: dict | None = None,
       ip_address: str | None = None,
   ) -> PlatformAuditLog:
       log = PlatformAuditLog(
           actor_id=actor_id,
           actor_role=actor_role,
           target_tenant_id=target_tenant_id,
           action=action,
           resource_type=resource_type,
           resource_id=resource_id,
           payload=payload,
           ip_address=ip_address,
       )
       db.add(log)
       # Caller commits — same pattern as record_audit_log() in audit/service.py
       return log

7. backend/app/modules/platform/auth_router.py
   router = APIRouter(prefix="/platform/auth", tags=["platform-auth"])

   @router.post("/login")
   async def platform_login(
       payload: PlatformLoginRequest,
       db: Annotated[AsyncSession, Depends(get_session)],
   ):
       return await auth_service.platform_login(db, payload.email, payload.password)

8. backend/app/main.py
   Add to imports:
     from app.modules.platform.auth_router import router as platform_auth_router
   Add to includes after existing routers:
     app.include_router(platform_auth_router, prefix=api)
   Also add platform module to import_all_models() by ensuring platform/models.py is imported.
   In database.py MODEL_MODULES list, add "app.modules.platform.models".
   Check database.py first with Read tool before editing.
  </action>
  <verify>
    <automated>cd c:/Users/Quive/OneDrive/Documents/Rotas/backend && python -c "
from app.modules.platform.models import PlatformUser, PlatformAuditLog
from app.modules.platform.schemas import PlatformLoginRequest, PlatformTokenResponse
from app.modules.platform.auth_service import platform_login
from app.modules.platform.audit_service import record_platform_audit
from app.modules.platform.auth_router import router
print('platform module imports OK')
print('routes:', [r.path for r in router.routes])
"</automated>
  </verify>
  <done>
    All platform module files importable.
    Platform auth router exposes POST /platform/auth/login.
    Migration file exists with correct table DDL and GRANT statements.
    platform_auth_router registered in main.py.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write 6 scope isolation tests</name>
  <files>
    backend/tests/test_platform_scope_isolation.py
  </files>
  <behavior>
    - test_tenant_token_cannot_access_platform_endpoint: dashboard JWT sent to POST /platform/tenants/{id}/suspend returns 403
    - test_platform_token_cannot_access_tenant_endpoint: platform JWT sent to GET /api/v1/trips returns 403
    - test_platform_admin_login_returns_scope_platform: login as platform_admin user returns token with scope="platform" and no tenant_id claim
    - test_require_platform_role_checks_scope_before_role: craft JWT with scope="dashboard" and role="platform_admin" (abuse scenario) sent to platform endpoint returns 403
    - test_platform_billing_role_token_has_correct_role: login as platform_billing user yields JWT where role="platform_billing"
    - test_tenant_jwt_scope_is_dashboard: existing tenant login returns JWT with scope="dashboard"
  </behavior>
  <action>
Create backend/tests/test_platform_scope_isolation.py. Use the httpx AsyncClient + app fixture
pattern from existing test files (see backend/tests/conftest.py for the client fixture).

Fixture setup:
- Create a PlatformUser with role="platform_admin" and role="platform_billing" directly in DB
  using AsyncSession (same pattern as other test files create Users).
- Use hash_password() from app.core.passwords to set password_hash.
- For the tenant token tests, use an existing tenant user login (reuse the tenant/user fixtures
  already defined in conftest.py — check what's available: look for `auth_headers` or `token`
  fixtures in conftest.py before writing your own).

Test structure (each is a standalone async def test_*):

test_tenant_token_cannot_access_platform_endpoint:
    1. Log in as a tenant user to get a dashboard JWT.
    2. POST /api/v1/platform/tenants/{some_uuid}/suspend with Authorization: Bearer {dashboard_jwt}
    3. Assert response.status_code == 403
    4. Assert "scope" not in response.json().get("error", "") OR assert error code is "forbidden"

test_platform_token_cannot_access_tenant_endpoint:
    1. Log in via POST /api/v1/platform/auth/login to get a platform JWT.
    2. GET /api/v1/trips with Authorization: Bearer {platform_jwt} (no X-Tenant-Id header)
    3. Assert response.status_code == 403
    Note: /api/v1/trips uses require_permission() which calls get_current_principal().
    get_current_principal() will reject scope="platform" with 401 (invalid_token_scope).
    Adjust assertion: assert response.status_code in {401, 403} — both are acceptable
    isolation proofs since the platform token cannot access tenant data either way.
    Prefer asserting != 200.

test_platform_admin_login_returns_scope_platform:
    1. POST /api/v1/platform/auth/login with platform_admin credentials
    2. Assert 200, token present
    3. Decode JWT (no signature verify, just base64) and assert claims["scope"] == "platform"
    4. Assert "tenant_id" not in claims

test_require_platform_role_checks_scope_before_role:
    1. Manually craft a JWT using create_access_token(tenant_id=some_uuid, scope="dashboard",
       role="platform_admin") — this simulates a malicious tenant token claiming a platform role.
    2. POST /api/v1/platform/tenants/{some_uuid}/suspend with this crafted JWT
    3. Assert response.status_code == 403
    Note: The suspend endpoint may not exist yet (Plan 02). Use a stub route or hit
    POST /api/v1/platform/auth/login with the crafted token and a second header check.
    Alternative: create a minimal test router with a require_platform_role-guarded endpoint
    registered only in the test app. Or simply call get_current_platform_principal directly
    with a mocked Request object. Document the chosen approach clearly.
    Simplest approach: decode the JWT in the dependency directly in a unit test —
    import get_current_platform_principal, mock the Authorization header, call it with
    pytest.raises(ApiError) and check error code == "forbidden".

test_platform_billing_role_token_has_correct_role:
    1. POST /api/v1/platform/auth/login with platform_billing user credentials
    2. Assert 200
    3. Decode token, assert claims["role"] == "platform_billing"
    4. Assert claims["scope"] == "platform"

test_tenant_jwt_scope_is_dashboard:
    1. POST /api/v1/auth/login with a tenant user
    2. Assert 200
    3. Decode token, assert claims["scope"] == "dashboard"
    4. Assert "tenant_id" in claims and claims["tenant_id"] is not None

For JWT decoding in tests without verify_signature:
    import base64, json
    def decode_jwt_payload(token: str) -> dict:
        payload_b64 = token.split(".")[1]
        payload_b64 += "=" * (4 - len(payload_b64) % 4)
        return json.loads(base64.urlsafe_b64decode(payload_b64))
  </action>
  <verify>
    <automated>cd c:/Users/Quive/OneDrive/Documents/Rotas/backend && python -m pytest tests/test_platform_scope_isolation.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    All 6 tests in test_platform_scope_isolation.py pass GREEN.
    Full suite: pytest tests/ -x --tb=short passes with >=348 tests (existing tests not broken).
  </done>
</task>

</tasks>

<verification>
Run full test suite after all tasks complete:
  cd backend && python -m pytest tests/ -x --tb=short -q

Expect: all existing tests pass + 6 new platform isolation tests GREEN.
Expect: python -c "from app.modules.platform.auth_router import router; print(len(router.routes))" prints 1.
Expect: alembic check shows no pending migrations after applying e1a2b3c4d5f6.
</verification>

<success_criteria>
1. POST /api/v1/platform/auth/login with valid platform_admin credentials returns 200 with scope="platform" JWT containing no tenant_id claim.
2. Decoding that JWT: claims["scope"] == "platform", "tenant_id" not in claims, claims["role"] == "platform_admin".
3. Sending that JWT to GET /api/v1/trips returns 401 or 403 — never 200.
4. Sending a valid dashboard tenant JWT to any /platform/* endpoint returns 403 — never 200.
5. A manually crafted JWT with scope="dashboard" and role="platform_admin" sent to a platform endpoint returns 403 — the scope check fires before the role is read.
6. python -m pytest tests/ -x -q exits 0 with >=354 tests passing (348 + 6 new).
</success_criteria>

<output>
After completion, create .planning/phases/25-platform-tenant-separation/25-01-SUMMARY.md
following the summary template at @$HOME/.claude/get-shit-done/templates/summary.md.
</output>
