---
phase: quick-260621-p2c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/app/modules/notifications/router.py
  - backend/app/modules/notifications/service.py
  - backend/app/main.py
  - backend/app/worker.py
  - backend/tests/test_notifications_api.py
autonomous: true
requirements: [NOTIF-01, NOTIF-02, NOTIF-03]

must_haves:
  truths:
    - "POST /api/v1/notifications/email enqueues a notification and returns 201 with idempotency on request_reference"
    - "GET /api/v1/notifications returns paginated list scoped to tenant"
    - "GET /api/v1/notifications/{id} returns single notification or 404"
    - "task_process_notification_outbox sends queued emails via aiosmtplib; skips gracefully when email_provider=none"
    - "task_notify_dispatch_rejected enqueues email to owner/admin users when dispatch is rejected"
    - "task_check_vehicle_document_expiry generates alerts for vehicle documents expiring within 30 days"
    - "All three tasks registered in WorkerSettings.functions; outbox flush + doc expiry registered in cron_jobs"
  artifacts:
    - path: "backend/app/modules/notifications/router.py"
      provides: "HTTP endpoints for notification enqueueing and listing"
      exports: ["router"]
    - path: "backend/app/worker.py"
      provides: "Three new ARQ task functions + WorkerSettings registration"
      contains: "task_process_notification_outbox, task_notify_dispatch_rejected, task_check_vehicle_document_expiry"
    - path: "backend/tests/test_notifications_api.py"
      provides: "API tests: enqueue, idempotency, list, get-by-id, 404"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/modules/notifications/router.py"
      via: "app.include_router(notifications_router, prefix=api)"
    - from: "WorkerSettings.functions"
      to: "task_notify_dispatch_rejected"
      via: "list registration so arq routes enqueue_job calls from trip_orders/router.py"
    - from: "task_process_notification_outbox"
      to: "aiosmtplib.send()"
      via: "email_provider != 'none' guard before SMTP call"
---

<objective>
Wire the notifications module end-to-end: HTTP router, main.py registration, aiosmtplib dependency, and three missing ARQ tasks.

Purpose: The notifications module has complete models/schemas/service but is completely inert. No router exists, main.py does not import it, the outbox is never flushed, and a live `enqueue_job("task_notify_dispatch_rejected")` call in trip_orders/router.py would crash the worker in production because the task is not registered.

Output:
- backend/app/modules/notifications/router.py — 3 endpoints
- backend/app/modules/notifications/service.py — list_notifications + get_notification appended
- backend/app/main.py — import + include_router added
- backend/pyproject.toml — aiosmtplib>=3.0 added
- backend/app/worker.py — 3 task functions + WorkerSettings registration
- backend/tests/test_notifications_api.py — 6 API tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@backend/app/modules/notifications/models.py
@backend/app/modules/notifications/schemas.py
@backend/app/modules/notifications/service.py
@backend/app/modules/alerts/router.py
@backend/app/worker.py
@backend/app/main.py
</context>

<interfaces>
Key types and contracts extracted from codebase:

NotificationOutbox (backend/app/modules/notifications/models.py):
  id, tenant_id, request_reference (max 160, unique per tenant), channel (default "email"),
  recipient, subject, body_text, body_html, template, payload (JSON), status (queued|sent|failed|cancelled),
  attempts (int), last_error, scheduled_at, sent_at, created_at, updated_at

EmailNotificationCreate (backend/app/modules/notifications/schemas.py):
  request_reference: str (1-160), recipient: str (3-255), subject: str (1-255),
  body_text: str, body_html: str|None, template: str|None, payload: dict|None

enqueue_email(db, tenant_id, payload: EmailNotificationCreate, *, actor_id) -> dict
  Idempotent on (tenant_id, request_reference). Returns serialize_notification dict.

Vehicle (backend/app/modules/vehicles/models.py):
  id, tenant_id, plate: str
  documents: Mapped[dict | None]  -- JSON blob shape: {"doc_type": {"expiry_date": "YYYY-MM-DD", ...}}
  Known doc types: "licence_disc", "roadworthy", "radio_licence", "customs_clearance"

TripOrder (backend/app/modules/trip_orders/models.py):
  id, tenant_id, origin, destination, customer_reference: str|None,
  rejection_reason: str|None, rejected_at: datetime|None

RBAC pattern (from alerts/router.py):
  from app.core.rbac import FLEET_READ, FLEET_WRITE, require_permission
  principal: Annotated[Principal, Depends(require_permission(FLEET_READ))]

Worker session pattern:
  async with ctx["db_factory"]() as db:  # BYPASSRLS admin session, cross-tenant safe
      ... await db.commit()

Config SMTP fields (backend/app/config.py):
  settings.email_provider (default "none"), settings.email_from_address,
  settings.smtp_host, settings.smtp_port, settings.smtp_username,
  settings.smtp_password.get_secret_value(), settings.smtp_use_tls
</interfaces>

<tasks>

Task 1: notifications/router.py + service additions + main.py + pyproject.toml
Files: backend/pyproject.toml, backend/app/modules/notifications/router.py,
       backend/app/modules/notifications/service.py, backend/app/main.py

ACTION:

1. pyproject.toml — add "aiosmtplib>=3.0" to the dependencies list after "arq>=0.28".

2. notifications/service.py — append two functions after enqueue_email:

    async def list_notifications(
        db: AsyncSession,
        tenant_id: UUID,
        *,
        status: str | None = None,
        channel: str | None = None,
        limit: int = 50,
        offset: int = 0,
    ) -> list[dict]:
        q = select(NotificationOutbox).where(NotificationOutbox.tenant_id == tenant_id)
        if status:
            q = q.where(NotificationOutbox.status == status)
        if channel:
            q = q.where(NotificationOutbox.channel == channel)
        q = q.order_by(NotificationOutbox.created_at.desc()).limit(limit).offset(offset)
        result = await db.execute(q)
        return [serialize_notification(n) for n in result.scalars().all()]

    async def get_notification(
        db: AsyncSession,
        tenant_id: UUID,
        notification_id: UUID,
    ) -> dict:
        item = await db.scalar(
            select(NotificationOutbox).where(
                NotificationOutbox.id == notification_id,
                NotificationOutbox.tenant_id == tenant_id,
            )
        )
        if not item:
            raise ApiError(
                "notification_not_found",
                "Notification not found.",
                status_code=status.HTTP_404_NOT_FOUND,  # 'status' already imported from fastapi
            )
        return serialize_notification(item)

3. notifications/router.py — create new file:

    from typing import Annotated
    from uuid import UUID
    from fastapi import APIRouter, Depends, Query
    from sqlalchemy.ext.asyncio import AsyncSession
    from app.core.auth import Principal
    from app.core.deps import get_session
    from app.core.rbac import FLEET_READ, FLEET_WRITE, require_permission
    from app.modules.notifications import service
    from app.modules.notifications.schemas import EmailNotificationCreate

    router = APIRouter(prefix="/notifications", tags=["notifications"])

    @router.post("/email", status_code=201)
    async def enqueue_email_endpoint(
        payload: EmailNotificationCreate,
        principal: Annotated[Principal, Depends(require_permission(FLEET_WRITE))],
        db: Annotated[AsyncSession, Depends(get_session)],
    ):
        result = await service.enqueue_email(db, principal.tenant_id, payload, actor_id=principal.user_id)
        await db.commit()
        return result

    @router.get("")
    async def list_notifications(
        principal: Annotated[Principal, Depends(require_permission(FLEET_READ))],
        db: Annotated[AsyncSession, Depends(get_session)],
        status: str | None = None,
        channel: str | None = None,
        limit: int = Query(50, ge=1, le=200),
        offset: int = Query(0, ge=0),
    ):
        return await service.list_notifications(
            db, principal.tenant_id, status=status, channel=channel, limit=limit, offset=offset
        )

    @router.get("/{notification_id}")
    async def get_notification(
        notification_id: UUID,
        principal: Annotated[Principal, Depends(require_permission(FLEET_READ))],
        db: Annotated[AsyncSession, Depends(get_session)],
    ):
        return await service.get_notification(db, principal.tenant_id, notification_id)

4. main.py — add after existing platform imports (before settings = get_settings()):
    from app.modules.notifications.router import router as notifications_router

   Add after app.include_router(analytics_router, prefix=api):
    app.include_router(notifications_router, prefix=api)

VERIFY: cd backend && python -c "from app.modules.notifications.router import router; from app.modules.notifications.service import list_notifications, get_notification; print('imports OK')"

DONE:
- router.py exists with 3 endpoints
- service.py has list_notifications and get_notification
- main.py imports and registers notifications_router
- pyproject.toml lists aiosmtplib>=3.0
- Python import check passes


Task 2: Three ARQ tasks in worker.py
Files: backend/app/worker.py

ACTION:

Add three task functions before the WorkerSettings class.

--- task_process_notification_outbox ---
Cron flush every 5 minutes. Processes up to 50 queued notifications per run.
Dev-safe: if email_provider == 'none', marks as sent without SMTP.
Retry logic: up to 3 attempts; on 3rd failure sets status='failed'.

    async def task_process_notification_outbox(ctx: dict) -> str:
        from datetime import datetime
        import structlog
        from sqlalchemy import and_, select
        from app.config import get_settings as _get_settings
        from app.modules.notifications.models import NotificationOutbox

        logger = structlog.get_logger("worker")
        _settings = _get_settings()
        sent_count = 0
        failed_count = 0
        now = datetime.now(UTC)

        async with ctx["db_factory"]() as db:
            result = await db.execute(
                select(NotificationOutbox)
                .where(and_(
                    NotificationOutbox.status == "queued",
                    NotificationOutbox.scheduled_at <= now,
                ))
                .order_by(NotificationOutbox.created_at)
                .limit(50)
            )
            items = result.scalars().all()

            for item in items:
                item.attempts += 1
                if _settings.email_provider.lower() == "none":
                    item.status = "sent"
                    item.sent_at = now
                    sent_count += 1
                    continue
                try:
                    import aiosmtplib
                    await aiosmtplib.send(
                        message=item.body_text,
                        hostname=_settings.smtp_host,
                        port=_settings.smtp_port,
                        username=_settings.smtp_username or None,
                        password=_settings.smtp_password.get_secret_value() or None,
                        use_tls=_settings.smtp_use_tls,
                        sender=_settings.email_from_address,
                        recipients=[item.recipient],
                        subject=item.subject,
                    )
                    item.status = "sent"
                    item.sent_at = now
                    sent_count += 1
                except Exception as exc:
                    item.last_error = str(exc)[:500]
                    if item.attempts >= 3:
                        item.status = "failed"
                        failed_count += 1
                    logger.warning("notification_send_failed",
                        notification_id=str(item.id), attempt=item.attempts, error=str(exc)[:200])
            await db.commit()

        logger.info("task_process_notification_outbox", sent=sent_count, failed=failed_count)
        return f"Notifications: {sent_count} sent, {failed_count} failed"

--- task_notify_dispatch_rejected ---
On-demand (enqueued by trip_orders/router.py). No cron entry.
Sends one email per active owner/admin user with an email address.
Uses enqueue_email (idempotent on request_reference) — safe to retry.

    async def task_notify_dispatch_rejected(ctx: dict, order_id: str, tenant_id: str) -> str:
        from uuid import UUID
        import structlog
        from sqlalchemy import and_, select
        from app.modules.notifications.schemas import EmailNotificationCreate
        from app.modules.notifications.service import enqueue_email
        from app.modules.trip_orders.models import TripOrder
        from app.modules.users.models import User

        logger = structlog.get_logger("worker")
        _tenant_id = UUID(tenant_id)
        _order_id = UUID(order_id)

        async with ctx["db_factory"]() as db:
            order = await db.scalar(
                select(TripOrder).where(
                    TripOrder.id == _order_id,
                    TripOrder.tenant_id == _tenant_id,
                )
            )
            if not order:
                logger.warning("task_notify_dispatch_rejected_order_not_found", order_id=order_id)
                return f"Order {order_id} not found"

            result = await db.execute(
                select(User).where(
                    and_(
                        User.tenant_id == _tenant_id,
                        User.role.in_(["owner", "admin"]),
                        User.email.isnot(None),
                        User.is_active.is_(True),
                    )
                )
            )
            users = result.scalars().all()

            order_ref = order.customer_reference or str(order.id)[:8].upper()
            reason = order.rejection_reason or "Sem motivo indicado."
            notified = 0

            for user in users:
                ref = f"dispatch_rejected:{order.id}:{user.id}"
                body = (
                    f"O despacho da ordem #{order_ref} "
                    f"({order.origin} -> {order.destination}) foi rejeitado.\n\n"
                    f"Motivo: {reason}\n\n"
                    "Aceda ao painel de gestao para rever e reencaminhar a ordem."
                )
                try:
                    await enqueue_email(
                        db, _tenant_id,
                        EmailNotificationCreate(
                            request_reference=ref,
                            recipient=user.email,
                            subject=f"Despacho rejeitado — Ordem #{order_ref}",
                            body_text=body,
                        ),
                        actor_id=None,
                    )
                    notified += 1
                except Exception as exc:
                    logger.warning("task_notify_dispatch_rejected_enqueue_error",
                        user_id=str(user.id), error=str(exc)[:200])

            await db.commit()

        logger.info("task_notify_dispatch_rejected", order_id=order_id, notified=notified)
        return f"Dispatch rejected notification: {notified} user(s) notified"

--- task_check_vehicle_document_expiry ---
Daily cron at 06:30 Africa/Maputo = 04:30 UTC.
Queries Vehicle.documents JSON for keys with an "expiry_date" field.
Generates alert per expiring document within 30 days.
Deduplication: request_reference = "vehicle_doc:{vehicle.id}:{doc_type}:{expiry_date}".

    async def task_check_vehicle_document_expiry(ctx: dict) -> str:
        from datetime import date, timedelta
        import structlog
        from sqlalchemy import select
        from app.modules.alerts.schemas import AlertCreate
        from app.modules.alerts.service import create_alert
        from app.modules.vehicles.models import Vehicle

        logger = structlog.get_logger("worker")
        today = date.today()
        cutoff = today + timedelta(days=30)
        alert_count = 0

        async with ctx["db_factory"]() as db:
            result = await db.execute(select(Vehicle).where(Vehicle.documents.isnot(None)))
            vehicles = result.scalars().all()

            for vehicle in vehicles:
                docs = vehicle.documents or {}
                for doc_type, doc_data in docs.items():
                    if not isinstance(doc_data, dict):
                        continue
                    expiry_str = doc_data.get("expiry_date")
                    if not expiry_str:
                        continue
                    try:
                        expiry_date = date.fromisoformat(expiry_str)
                    except ValueError:
                        continue
                    if expiry_date < today or expiry_date > cutoff:
                        continue

                    days_remaining = (expiry_date - today).days
                    priority = "critical" if days_remaining <= 7 else "high"
                    request_reference = f"vehicle_doc:{vehicle.id}:{doc_type}:{expiry_date.isoformat()}"

                    try:
                        await create_alert(
                            db, vehicle.tenant_id,
                            AlertCreate(
                                request_reference=request_reference,
                                alert_type="vehicle_document_expiring_soon",
                                priority=priority,
                                entity_type="vehicle",
                                entity_id=vehicle.id,
                                title=f"Documento de viatura a expirar: {doc_type}",
                                message=(
                                    f"Documento '{doc_type}' da viatura {vehicle.plate} "
                                    f"expira em {days_remaining} dia(s) ({expiry_date.isoformat()})."
                                ),
                                channel="dashboard",
                            ),
                        )
                        alert_count += 1
                    except Exception as exc:
                        logger.warning("task_check_vehicle_document_expiry_alert_error",
                            vehicle_id=str(vehicle.id), doc_type=doc_type, error=str(exc))

        logger.info("task_check_vehicle_document_expiry", alerts_generated=alert_count)
        return f"Generated {alert_count} vehicle document expiry alerts"

--- WorkerSettings update ---
In WorkerSettings.functions list, add after task_check_insurance_renewals:
    task_process_notification_outbox,
    task_notify_dispatch_rejected,
    task_check_vehicle_document_expiry,

In WorkerSettings.cron_jobs list, add:
    # NOTIF-01: Flush notification outbox every 5 minutes
    cron(task_process_notification_outbox, minute={0, 5, 10, 15, 20, 25, 30, 35, 40, 45, 50, 55}),
    # NOTIF-03: Vehicle document expiry alerts — 06:30 Africa/Maputo = 04:30 UTC
    cron(task_check_vehicle_document_expiry, hour=4, minute=30),

Note: task_notify_dispatch_rejected is on-demand only — no cron entry.

VERIFY: cd backend && python -c "from app.worker import task_process_notification_outbox, task_notify_dispatch_rejected, task_check_vehicle_document_expiry, WorkerSettings; fns = [f.__name__ for f in WorkerSettings.functions]; assert 'task_notify_dispatch_rejected' in fns; assert 'task_process_notification_outbox' in fns; assert 'task_check_vehicle_document_expiry' in fns; print('worker OK')"

DONE:
- Three task functions present in worker.py
- All three in WorkerSettings.functions
- task_process_notification_outbox and task_check_vehicle_document_expiry in cron_jobs
- Python import check passes


Task 3: Tests — test_notifications_api.py
Files: backend/tests/test_notifications_api.py

ACTION:

Create test file following the project pattern (httpx AsyncClient, pytest-asyncio auto mode, fixtures from existing tests).

Tests to implement:

1. test_enqueue_email_returns_201
   POST /api/v1/notifications/email with valid EmailNotificationCreate payload
   Expect: 201, body has id, status="queued", recipient matches

2. test_enqueue_email_idempotent_same_values
   POST same request_reference + same payload twice
   Expect: both return 200/201, same id returned on second call (not a duplicate)

3. test_enqueue_email_conflict_different_values
   POST same request_reference with different recipient
   Expect: 409 with error code "notification_request_reference_reused"

4. test_list_notifications
   Enqueue 2 notifications, GET /api/v1/notifications
   Expect: 200, list contains both (by id)

5. test_get_notification_by_id
   Enqueue 1, GET /api/v1/notifications/{id}
   Expect: 200, body.id matches

6. test_get_notification_wrong_tenant_returns_404
   Enqueue notification for tenant_a, GET with tenant_b token
   Expect: 404

Use the same authentication helper pattern as existing test files in backend/tests/.
Check backend/tests/conftest.py for available fixtures (db, client, auth_headers, tenant).

VERIFY: cd backend && python -m pytest tests/test_notifications_api.py -v --tb=short

DONE:
- 6 tests all pass
- No ruff errors: ruff check backend/app/modules/notifications/ backend/app/worker.py --select E,F,I,UP,B

</tasks>

<verification>
Run in sequence after all tasks complete:

1. Import checks:
   cd backend && python -c "from app.modules.notifications.router import router; from app.main import app; print('main OK')"

2. Worker registration check:
   cd backend && python -c "from app.worker import WorkerSettings; fns = [f.__name__ for f in WorkerSettings.functions]; print(fns); assert 'task_notify_dispatch_rejected' in fns"

3. Tests:
   cd backend && python -m pytest tests/test_notifications_api.py -v

4. Lint:
   cd backend && ruff check app/modules/notifications/ app/worker.py app/main.py --select E,F,I,UP,B
</verification>

<success_criteria>
- POST /api/v1/notifications/email returns 201 and is idempotent on request_reference
- GET /api/v1/notifications and GET /api/v1/notifications/{id} work with tenant isolation
- task_notify_dispatch_rejected is registered in WorkerSettings.functions (production crash fixed)
- task_process_notification_outbox flushes outbox via aiosmtplib; skips without error when email_provider=none
- task_check_vehicle_document_expiry generates alerts for vehicle.documents JSON expiry fields
- 6 tests passing in test_notifications_api.py
- ruff passes on all modified files
</success_criteria>

<output>
After completion, create .planning/quick/260621-p2c-notifications-module-router-py-registo-e/260621-p2c-SUMMARY.md
</output>
