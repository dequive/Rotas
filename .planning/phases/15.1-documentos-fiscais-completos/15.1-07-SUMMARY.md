---
phase: "15.1"
plan: "07"
subsystem: "cargo"
status: complete
tags: [operational, carta-porte, dav, inatter, pdf, opdoc]
dependency_graph:
  requires: ["03"]
  provides: [carta_porte_endpoint, dav_endpoint]
  affects: [15.1-09]
tech_stack:
  added: []
  patterns: [pdf-generation, bilingual-pdf, digital-record-only]
key_files:
  created: []
  modified:
    - backend/app/modules/cargo/service.py
    - backend/app/modules/cargo/router.py
    - backend/app/modules/cargo/schemas.py
    - backend/app/modules/cargo/exporters.py
decisions:
  - "CPI PDF is bilingual PT/EN — required for SADC cross-border customs"
  - "DAV is digital record only — no PDF (INATTER authorization tracked by code string)"
  - "border_post and country_destination stored in extra_fields for CPI"
metrics:
  duration: "25 minutes"
  completed_date: "2026-06-18"
  tasks_completed: 5
  files_changed: 4
---

# Phase 15.1 Plan 07: Wave 3B — CPI + DAV/INATTER (OPDOC-03, OPDOC-04) Summary

## One-liner

`POST carta-porte-internacional` (bilingual PT/EN PDF, stores SADC fields in extra_fields) and `POST dav` (digital record only, no PDF, stores authorization_code in extra_fields).

## What Was Built

- `create_carta_porte()` — stores `border_post`, `country_destination`, `sadc_cpi_number` in `extra_fields`, generates bilingual PDF
- `create_dav()` — stores `authorization_code` in `extra_fields`, no PDF generated
- `CartaPorteCreate` and `DAVCreate` schemas
- `POST /trips/{id}/carta-porte-internacional` and `POST /trips/{id}/dav` router endpoints (status 201)
- CPI PDF accessible via `GET /cargo/transport-documents/{id}/pdf`

## Verification

All OPDOC-03 and OPDOC-04 tests pass:
- `test_create_carta_porte_stores_extra_fields` ✓
- `test_carta_porte_pdf_generated` ✓
- `test_create_dav_stores_authorization_code` ✓
- `test_dav_has_no_pdf_generation` ✓

## Deviations from Plan

None.

## Commits

| Hash | Message |
|------|---------|
| da7a383 | feat(15.1): fiscal document types + operational cargo documents |

## Self-Check: PASSED

- [x] `POST carta-porte` stores `border_post` and `country_destination` in `extra_fields`
- [x] `POST dav` stores `authorization_code` in `extra_fields`, no `pdf_url` in response
- [x] CPI PDF exists and is accessible
- [x] DAV response has no `pdf_url` field
