---
plan: 17-01
phase: 17
title: "Redis Rate Limiting + Structured Logging"
wave: 1
depends_on: []
requirements: [INFRA2-01, INFRA2-02]
files_modified:
  - backend/app/core/limiter.py
  - backend/app/core/logging.py
  - backend/app/core/middleware.py
  - backend/app/main.py
  - backend/app/worker.py
  - backend/pyproject.toml
autonomous: true
---

# Plan 17-01: Redis Rate Limiting + Structured Logging

## Objective

Substituir o rate limiter in-memory por Redis-backed (funciona correctamente em Railway multi-worker) e adicionar logging estruturado JSON com `structlog` ao FastAPI e ARQ worker — cada request produz uma linha JSON com `request_id`, `tenant_id`, `method`, `path`, `status_code`, `duration_ms`.

---

## Tasks

### Task 1: Instalar dependências — structlog e slowapi Redis backend

<read_first>
- backend/pyproject.toml
- backend/app/core/limiter.py
</read_first>

<action>
Em `backend/pyproject.toml`, na secção `[tool.poetry.dependencies]` ou equivalente, adicionar/verificar:

```toml
structlog = ">=24.0"
```

`slowapi` já está instalado. `redis[asyncio]` já está instalado (usado pelo ARQ worker e CT cache). Não é necessário adicionar.

Verificar versão instalada de `slowapi`:
```bash
pip show slowapi
```

Se `slowapi >= 0.1.9` (suporta `RedisRateLimiter`): continuar.
Se versão inferior: actualizar em pyproject.toml para `slowapi = ">=0.1.9"`.

Instalar:
```bash
cd backend && pip install structlog>=24.0
```
</action>

<acceptance_criteria>
- `python -c "import structlog; print(structlog.__version__)"` retorna versão >= 24.0
- `python -c "from slowapi.wrappers import RateLimitItem"` sem ImportError
- `grep "structlog" backend/pyproject.toml` retorna 1 linha
</acceptance_criteria>

---

### Task 2: Redis Rate Limiter — substituir InMemoryLimiter por RedisLimiter

<read_first>
- backend/app/core/limiter.py
- backend/app/config.py (verificar campo redis_url)
- backend/app/main.py (como limiter é registado no app)
</read_first>

<action>
Substituir completamente `backend/app/core/limiter.py` com:

```python
"""
INFRA2-01: Redis-backed rate limiter for multi-worker Railway deployment.

Previously used in-memory Limiter (D-07 from Phase 4 — deferred to this phase).
Redis instance is the same as ARQ worker and Control Tower cache.

Rate limits:
  - /auth/login: 10/minute per IP
  - /driver-auth/pair: 10/minute per IP
  - /api/v1/sync/batch: 60/minute per IP
  - /api/v1/gps/webhook/*: 60/minute per IP (Phase 12)
  - Default: no global limit (per-endpoint limits enforced in routers)
"""
from slowapi import Limiter
from slowapi.util import get_remote_address

from app.config import get_settings

_settings = get_settings()

# INFRA2-01: Use Redis storage for rate limiting so counters are shared
# across all Gunicorn workers. Falls back to in-memory if REDIS_URL absent
# (local dev without Redis). In production, REDIS_URL must be set.
if _settings.redis_url:
    limiter = Limiter(
        key_func=get_remote_address,
        storage_uri=_settings.redis_url,
    )
else:
    # Local dev fallback — in-memory, single-worker only
    import warnings
    warnings.warn(
        "REDIS_URL not set — rate limiter using in-memory storage. "
        "Multi-worker deployments will have independent rate limit counters.",
        RuntimeWarning,
        stacklevel=1,
    )
    limiter = Limiter(key_func=get_remote_address)
```

Verificar que `settings.redis_url` existe em `backend/app/config.py`. Se o campo se chamar diferente (ex. `REDIS_URL`), usar o nome correcto.
</action>

<acceptance_criteria>
- `grep -n "storage_uri\|redis_url\|RedisLimiter\|REDIS_URL" backend/app/core/limiter.py` retorna 2+ linhas
- `python -c "from app.core.limiter import limiter"` sem ImportError
- `grep -n "InMemoryRateLimiter\|# D-07" backend/app/core/limiter.py` retorna 0 linhas (removido)
</acceptance_criteria>

---

### Task 3: Configurar structlog — logging.py e integração FastAPI

<read_first>
- backend/app/main.py (lifespan, middleware existente)
- backend/app/core/request_context.py (RequestContextMiddleware — source de request_id e tenant_id)
</read_first>

<action>
**Criar `backend/app/core/logging.py`**:

```python
"""
INFRA2-02: Structured JSON logging with structlog.

Every log line emitted by FastAPI or the ARQ worker is a JSON object with:
  - timestamp (ISO 8601)
  - level
  - event (message)
  - request_id (injected by RequestContextMiddleware)
  - tenant_id (injected by database.py after_begin)
  - Any additional context bound by the caller

Usage in application code:
    import structlog
    logger = structlog.get_logger(__name__)
    logger.info("trip_created", trip_id=str(trip.id), driver_id=str(driver.id))

Usage in ARQ worker tasks:
    logger = structlog.get_logger("worker")
    logger.info("task_started", task="task_mark_overdue_billing_documents")
"""
import logging
import sys

import structlog


def configure_structlog(json_logs: bool = True) -> None:
    """Configure structlog for the application.

    Call once at startup in FastAPI lifespan and ARQ worker startup.
    json_logs=True for production (Railway / Railway Logs).
    json_logs=False for local dev (pretty console output).
    """
    shared_processors = [
        structlog.contextvars.merge_contextvars,
        structlog.stdlib.add_log_level,
        structlog.stdlib.add_logger_name,
        structlog.processors.TimeStamper(fmt="iso"),
        structlog.processors.StackInfoRenderer(),
    ]

    if json_logs:
        renderer = structlog.processors.JSONRenderer()
    else:
        renderer = structlog.dev.ConsoleRenderer()

    structlog.configure(
        processors=shared_processors + [
            structlog.stdlib.ProcessorFormatter.wrap_for_formatter,
        ],
        logger_factory=structlog.stdlib.LoggerFactory(),
        wrapper_class=structlog.make_filtering_bound_logger(logging.INFO),
        cache_logger_on_first_use=True,
    )

    formatter = structlog.stdlib.ProcessorFormatter(
        foreign_pre_chain=shared_processors,
        processors=[
            structlog.stdlib.ProcessorFormatter.remove_processors_meta,
            renderer,
        ],
    )

    handler = logging.StreamHandler(sys.stdout)
    handler.setFormatter(formatter)

    root_logger = logging.getLogger()
    root_logger.handlers.clear()
    root_logger.addHandler(handler)
    root_logger.setLevel(logging.INFO)

    # Silence noisy libraries
    logging.getLogger("uvicorn.access").setLevel(logging.WARNING)
    logging.getLogger("sqlalchemy.engine").setLevel(logging.WARNING)
```

**Em `backend/app/main.py`**, no lifespan (`@asynccontextmanager async def lifespan`), adicionar ANTES de qualquer outro init:

```python
from app.core.logging import configure_structlog
from app.config import get_settings as _get_settings

_s = _get_settings()
configure_structlog(json_logs=_s.environment == "production")
```

Adicionar import no topo do main.py:
```python
import structlog
logger = structlog.get_logger(__name__)
```
</action>

<acceptance_criteria>
- `test -f backend/app/core/logging.py` → file exists
- `grep -n "configure_structlog\|JSONRenderer\|ConsoleRenderer" backend/app/core/logging.py` retorna 3+ linhas
- `grep -n "configure_structlog" backend/app/main.py` retorna 1 linha
- `python -c "from app.core.logging import configure_structlog; configure_structlog(json_logs=False)"` sem erros
</acceptance_criteria>

---

### Task 4: Middleware de Logging HTTP — request_id, tenant_id, duration_ms

<read_first>
- backend/app/core/request_context.py (RequestContextMiddleware existente — já injeta request_id)
- backend/app/core/middleware.py (se existir)
- backend/app/main.py (onde middlewares são adicionados)
</read_first>

<action>
**Criar ou editar `backend/app/core/middleware.py`**:

Adicionar `StructlogRequestMiddleware` (adicionar ao ficheiro se já existe, criar se não):

```python
"""
INFRA2-02: HTTP request logging middleware.

Logs each request as a single JSON line AFTER response is sent, with:
  - method, path, status_code, duration_ms
  - request_id (from RequestContextMiddleware via contextvars)
  - tenant_id (from db.execute after_begin via contextvars — may be None for public endpoints)

Placed AFTER RequestContextMiddleware in the middleware stack so request_id is available.
"""
import time

import structlog
from starlette.middleware.base import BaseHTTPMiddleware
from starlette.requests import Request
from starlette.responses import Response

logger = structlog.get_logger("http")


class StructlogRequestMiddleware(BaseHTTPMiddleware):
    async def dispatch(self, request: Request, call_next) -> Response:
        start = time.perf_counter()
        response = await call_next(request)
        duration_ms = round((time.perf_counter() - start) * 1000, 2)

        # Skip logging for health and metrics endpoints to reduce noise
        path = request.url.path
        if path in ("/health", "/health/deep", "/metrics"):
            return response

        logger.info(
            "http_request",
            method=request.method,
            path=path,
            status_code=response.status_code,
            duration_ms=duration_ms,
            client_ip=request.client.host if request.client else None,
        )
        return response
```

**Em `backend/app/main.py`**, adicionar middleware APÓS `RequestContextMiddleware`:
```python
from app.core.middleware import StructlogRequestMiddleware
app.add_middleware(StructlogRequestMiddleware)
```

Verificar que `RequestContextMiddleware` já está registado antes (está, confirmar na ordem de `app.add_middleware()` calls — Starlette aplica middlewares em ordem inversa ao registo, por isso `RequestContextMiddleware` deve ser adicionado por ÚLTIMO para ser executado primeiro).
</action>

<acceptance_criteria>
- `grep -n "StructlogRequestMiddleware" backend/app/core/middleware.py backend/app/main.py` retorna 2 linhas
- `grep -n "duration_ms\|status_code\|method.*path" backend/app/core/middleware.py` retorna 3+ linhas
- Servidor inicia sem ImportError: `python -m uvicorn app.main:app --host 0.0.0.0 --port 8001 &` e `curl http://localhost:8001/health` produz log JSON em stdout com campo `duration_ms`
</acceptance_criteria>

---

### Task 5: Structured Logging no ARQ Worker

<read_first>
- backend/app/worker.py (WorkerSettings e on_startup hook)
</read_first>

<action>
Em `backend/app/worker.py`, no `on_startup` do WorkerSettings (ou criar se não existir):

```python
async def startup(ctx: dict) -> None:
    """Worker startup — configure structlog and log worker boot."""
    from app.core.logging import configure_structlog
    from app.config import get_settings

    settings = get_settings()
    configure_structlog(json_logs=settings.environment == "production")

    import structlog
    logger = structlog.get_logger("worker")
    logger.info("arq_worker_started", queues=ctx.get("queues", []))
```

Registar `startup` no `WorkerSettings`:
```python
class WorkerSettings:
    on_startup = startup
    # ... existing settings
```

No topo do `worker.py`, substituir quaisquer `print()` ou `logging.info()` por:
```python
import structlog
logger = structlog.get_logger("worker")
```

E nas funções de task existentes, substituir `print(f"...")` por `logger.info("event_name", key=value)`.
</action>

<acceptance_criteria>
- `grep -n "configure_structlog\|on_startup" backend/app/worker.py` retorna 2+ linhas
- `grep -n "logger = structlog.get_logger" backend/app/worker.py` retorna 1 linha
- `python -c "from app.worker import WorkerSettings; print(WorkerSettings.on_startup)"` retorna função startup
</acceptance_criteria>

---

## Verification

### Must-Haves (derived from INFRA2-01 + INFRA2-02 success criteria)

- [ ] `python -c "from app.core.limiter import limiter; print(type(limiter._storage))"` mostra tipo Redis (não InMemory) quando `REDIS_URL` está definido
- [ ] `GET /api/v1/auth/login` (POST) com rate limit excedido retorna HTTP 429 — e o mesmo IP é bloqueado em todos os workers (verificar com Railway multi-worker ou 2 processos uvicorn locais)
- [ ] `GET /health` em produção produz linha JSON em Railway Logs com `{"event": "http_request", "method": "GET", "path": "/health", ...}` — NÃO produz linha (path ignorado)
- [ ] `POST /api/v1/trips` produz linha JSON com `duration_ms` presente e > 0
- [ ] Worker ARQ inicia e produz linha JSON: `{"event": "arq_worker_started", "level": "info", ...}`
- [ ] Em desenvolvimento (`ENVIRONMENT != production`): output é colorido/legível, não JSON

### Tests to Run

```bash
cd backend
# Verificar que limiter importa sem erro
python -c "from app.core.limiter import limiter; print('limiter ok')"
# Verificar que logging configura sem erro
python -c "from app.core.logging import configure_structlog; configure_structlog(False); import structlog; structlog.get_logger().info('test', key='value')"
pytest tests/ -k "rate_limit or limiter" -v
```
