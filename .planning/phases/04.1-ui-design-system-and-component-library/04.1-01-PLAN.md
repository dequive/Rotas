---
phase: 04.1-ui-design-system-and-component-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/manager/app/globals.css
  - apps/manager/tailwind.config.ts
autonomous: true
requirements:
  - DS-01
  - DS-02
  - DS-05

must_haves:
  truths:
    - "shadcn Button renders amber (#f59e0b) background, not dark slate-gray"
    - "font-mono utility resolves to IBM Plex Mono, not Courier New"
    - "npm run build --workspace=apps/manager succeeds with zero errors"
    - "No oklch values remain anywhere in globals.css"
    - "Semantic color tokens (success, warning, error, info, amber, surface-*) are available as Tailwind utilities"
  artifacts:
    - path: "apps/manager/app/globals.css"
      provides: "Single consolidated @layer base :root block with all DESIGN.md tokens"
      contains: "--amber: #f59e0b"
    - path: "apps/manager/tailwind.config.ts"
      provides: "Extended color palette + font families mapping to CSS variables"
      contains: "IBM Plex Mono"
  key_links:
    - from: "tailwind.config.ts fontFamily.mono"
      to: "IBM Plex Mono (Google Fonts in layout.tsx)"
      via: "font-mono Tailwind utility class"
      pattern: "IBM Plex Mono"
    - from: "globals.css --primary"
      to: "shadcn button.tsx bg-primary"
      via: "CSS variable resolution"
      pattern: "--primary: 38 92% 50%"
---

<objective>
Fix the critical CSS variable conflict that makes shadcn render wrong colors, add missing Tailwind design tokens, and configure font-mono to resolve to IBM Plex Mono.

Purpose: Everything downstream depends on this. The oklch override bug means shadcn Button renders dark zinc instead of amber. IBM Plex Mono won't work for KPI values until tailwind.config.ts maps fontFamily.mono. These are zero-visual-change-to-pages fixes that unlock every wave that follows.
Output: Clean globals.css with one consolidated @layer base :root block. tailwind.config.ts extended with all DESIGN.md tokens. Build passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-ui-design-system-and-component-library/04.1-RESEARCH.md
@DESIGN.md
@apps/manager/app/globals.css
@apps/manager/tailwind.config.ts
@apps/manager/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix globals.css — remove oklch override, consolidate into single @layer base :root</name>
  <files>apps/manager/app/globals.css</files>
  <read_first>
    - apps/manager/app/globals.css (full file — read lines 1-100 AND lines 2140-2227 to see both conflict zones)
  </read_first>
  <action>
Rewrite apps/manager/app/globals.css with this exact structure. Do NOT keep the old content — replace it entirely with the structure below, preserving all legacy CSS class definitions (lines ~100-2148) between the base reset block and the end.

The file structure must be:

1. Tailwind directives (first 3 lines):
```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

2. Single `@layer base` block containing ONE `:root` with ALL tokens:
```css
@layer base {
  :root {
    color-scheme: light;

    /* ── Sidebar ─────────────────────────────────────── */
    --sidebar-bg:      #0f1623;
    --sidebar-hover:   #1e2d3d;
    --sidebar-active:  #1e3a52;
    --sidebar-text:    #94a3b8;
    --sidebar-text-active: #f1f5f9;
    --sidebar-section: #475569;

    /* ── Content surfaces ────────────────────────────── */
    --bg:          #f1f5f9;
    --surface:     #ffffff;
    --surface-2:   #f8fafc;
    --border-color: #e2e8f0;
    --border-strong: #cbd5e1;

    /* ── Text ────────────────────────────────────────── */
    --ink:         #0f172a;
    --ink-2:       #334155;
    --muted-color: #64748b;
    --placeholder: #94a3b8;

    /* ── Accent — amber (primary) ────────────────────── */
    --amber:       #f59e0b;
    --amber-light: #fef3c7;
    --amber-dark:  #d97706;

    /* ── Accent — blue (secondary) ───────────────────── */
    --blue:        #2563eb;
    --blue-light:  #eff6ff;
    --blue-dark:   #1d4ed8;

    /* ── Semantic ────────────────────────────────────── */
    --success:     #16a34a;
    --success-bg:  #f0fdf4;
    --warning:     #d97706;
    --warning-bg:  #fffbeb;
    --error:       #dc2626;
    --error-bg:    #fef2f2;
    --info:        #0891b2;
    --info-bg:     #ecfeff;

    /* ── Transitions ─────────────────────────────────── */
    --transition-fast:   80ms ease-out;
    --transition-base:  100ms ease-out;
    --transition-modal: 150ms ease-out;

    /* ── Legacy aliases (for unmigrated components) ───── */
    --ink: #0f172a;
    --muted: #64748b;
    --line: #e2e8f0;
    --panel: #ffffff;
    --soft: #f1f5f9;
    --nav: #0f1623;
    --green: #16a34a;
    --orange: #d97706;
    --red: #dc2626;
    --cyan: #0891b2;

    /* ── shadcn/ui HSL tokens (NO oklch anywhere) ─────── */
    --background: 213 27% 96%;
    --foreground: 222 47% 11%;
    --card: 0 0% 100%;
    --card-foreground: 222 47% 11%;
    --popover: 0 0% 100%;
    --popover-foreground: 222 47% 11%;
    --primary: 38 92% 50%;
    --primary-foreground: 222 47% 9%;
    --secondary: 213 27% 96%;
    --secondary-foreground: 222 47% 11%;
    --muted: 213 27% 96%;
    --muted-foreground: 215 16% 47%;
    --accent: 48 96% 89%;
    --accent-foreground: 26 90% 37%;
    --destructive: 0 84% 60%;
    --destructive-foreground: 0 0% 100%;
    --border: 214 32% 91%;
    --input: 213 27% 84%;
    --ring: 38 92% 50%;
    --radius: 0.375rem;

    /* ── Chart ───────────────────────────────────────── */
    --chart-1: 38 92% 50%;
    --chart-2: 221 83% 53%;
    --chart-3: 142 71% 45%;
    --chart-4: 0 84% 60%;
    --chart-5: 199 89% 48%;

    /* ── Sidebar shadcn tokens ───────────────────────── */
    --sidebar: 222 47% 9%;
    --sidebar-foreground: 215 20% 65%;
    --sidebar-primary: 38 92% 50%;
    --sidebar-primary-foreground: 222 47% 9%;
    --sidebar-accent: 215 28% 17%;
    --sidebar-accent-foreground: 213 27% 96%;
    --sidebar-border: 215 28% 17%;
    --sidebar-ring: 38 92% 50%;
  }

  * {
    @apply border-border;
    box-sizing: border-box;
  }

  body {
    @apply bg-background text-foreground;
    font-family: 'Manrope', system-ui, sans-serif;
    -webkit-font-smoothing: antialiased;
    margin: 0;
  }

  button, input, select {
    font: inherit;
  }

  button {
    cursor: pointer;
  }
}
```

3. After the @layer base block, keep ALL legacy CSS class definitions from the original file (everything from `.shell {` through the end of the `@media` block at the original line 2148). These class definitions stay outside the layer — they are regular CSS rules.

4. REMOVE completely: the entire `@layer base { :root { --background: oklch...` block (original lines 2149–2217) and the second `@layer base { * { @apply border-border } body { @apply bg-background... } }` block (original lines 2219–2226). These are the conflict sources.

Critical rules:
- The word "oklch" must not appear anywhere in the final file
- There must be exactly ONE `:root` block in the entire file, inside `@layer base`
- The --primary HSL value must be `38 92% 50%` (amber)
- Legacy CSS classes (.shell, .sidebar, .queue-icon, etc.) are preserved after the @layer base block
  </action>
  <verify>
    <automated>grep -c "oklch" apps/manager/app/globals.css && echo "FAIL: oklch found" || echo "PASS: no oklch"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "oklch" apps/manager/app/globals.css` returns 0
    - `grep -c "@layer base" apps/manager/app/globals.css` returns 1 (exactly one @layer base block)
    - `grep "primary: 38 92% 50%" apps/manager/app/globals.css` finds a match
    - `grep -- "--amber: #f59e0b" apps/manager/app/globals.css` finds a match
  </acceptance_criteria>
  <done>globals.css has one @layer base :root block, no oklch values, --primary is 38 92% 50% amber, all DESIGN.md tokens present</done>
</task>

<task type="auto">
  <name>Task 2: Extend tailwind.config.ts with DESIGN.md tokens + IBM Plex Mono font-mono</name>
  <files>apps/manager/tailwind.config.ts</files>
  <read_first>
    - apps/manager/tailwind.config.ts (full file)
  </read_first>
  <action>
Rewrite apps/manager/tailwind.config.ts to add all DESIGN.md tokens as Tailwind utility mappings and configure IBM Plex Mono as font-mono. Keep the existing shadcn color mappings (background, foreground, card, primary, etc.) — add the new tokens alongside them.

Replace the entire file with:

```typescript
import type { Config } from "tailwindcss"

const config: Config = {
  darkMode: ["class"],
  content: [
    "./app/**/*.{ts,tsx}",
    "./components/**/*.{ts,tsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        sans: ['Manrope', 'system-ui', 'sans-serif'],
        mono: ['IBM Plex Mono', 'monospace'],
      },
      colors: {
        // ── Legacy aliases (keep for unmigrated components) ──
        nav: 'var(--nav)',
        soft: 'var(--soft)',
        panel: 'var(--panel)',
        ink: 'var(--ink)',
        muted: 'var(--muted-color)',
        line: 'var(--line)',
        green: 'var(--green)',
        orange: 'var(--orange)',
        red: 'var(--red)',
        cyan: 'var(--cyan)',

        // ── DESIGN.md semantic tokens ─────────────────────────
        amber: {
          DEFAULT: 'var(--amber)',
          light: 'var(--amber-light)',
          dark: 'var(--amber-dark)',
        },
        blue: {
          DEFAULT: 'var(--blue)',
          light: 'var(--blue-light)',
          dark: 'var(--blue-dark)',
        },
        success: {
          DEFAULT: 'var(--success)',
          bg: 'var(--success-bg)',
        },
        warning: {
          DEFAULT: 'var(--warning)',
          bg: 'var(--warning-bg)',
        },
        error: {
          DEFAULT: 'var(--error)',
          bg: 'var(--error-bg)',
        },
        info: {
          DEFAULT: 'var(--info)',
          bg: 'var(--info-bg)',
        },
        // ── Surface tokens ────────────────────────────────────
        bg: 'var(--bg)',
        surface: 'var(--surface)',
        'surface-2': 'var(--surface-2)',
        'border-strong': 'var(--border-strong)',
        // ── Text tokens ───────────────────────────────────────
        'ink-2': 'var(--ink-2)',
        placeholder: 'var(--placeholder)',
        // ── Sidebar tokens ────────────────────────────────────
        'sidebar-bg': 'var(--sidebar-bg)',
        'sidebar-hover': 'var(--sidebar-hover)',
        'sidebar-active': 'var(--sidebar-active)',
        'sidebar-text': 'var(--sidebar-text)',
        'sidebar-text-active': 'var(--sidebar-text-active)',
        'sidebar-section': 'var(--sidebar-section)',

        // ── shadcn/ui HSL tokens (required by shadcn components) ─
        background: 'hsl(var(--background))',
        foreground: 'hsl(var(--foreground))',
        card: {
          DEFAULT: 'hsl(var(--card))',
          foreground: 'hsl(var(--card-foreground))',
        },
        popover: {
          DEFAULT: 'hsl(var(--popover))',
          foreground: 'hsl(var(--popover-foreground))',
        },
        primary: {
          DEFAULT: 'hsl(var(--primary))',
          foreground: 'hsl(var(--primary-foreground))',
        },
        secondary: {
          DEFAULT: 'hsl(var(--secondary))',
          foreground: 'hsl(var(--secondary-foreground))',
        },
        accent: {
          DEFAULT: 'hsl(var(--accent))',
          foreground: 'hsl(var(--accent-foreground))',
        },
        destructive: 'hsl(var(--destructive))',
        border: 'hsl(var(--border))',
        input: 'hsl(var(--input))',
        ring: 'hsl(var(--ring))',
        chart: {
          '1': 'hsl(var(--chart-1))',
          '2': 'hsl(var(--chart-2))',
          '3': 'hsl(var(--chart-3))',
          '4': 'hsl(var(--chart-4))',
          '5': 'hsl(var(--chart-5))',
        },
        sidebar: {
          DEFAULT: 'hsl(var(--sidebar))',
          foreground: 'hsl(var(--sidebar-foreground))',
          primary: 'hsl(var(--sidebar-primary))',
          'primary-foreground': 'hsl(var(--sidebar-primary-foreground))',
          accent: 'hsl(var(--sidebar-accent))',
          'accent-foreground': 'hsl(var(--sidebar-accent-foreground))',
          border: 'hsl(var(--sidebar-border))',
          ring: 'hsl(var(--sidebar-ring))',
        },
      },
      borderRadius: {
        lg: 'var(--radius)',
        md: 'calc(var(--radius) - 2px)',
        sm: 'calc(var(--radius) - 4px)',
      },
    },
  },
  plugins: [require("tailwindcss-animate")],
}

export default config
```

Note: `muted` is mapped to `var(--muted-color)` (not `var(--muted)`) because `--muted` in globals.css is the shadcn HSL token (213 27% 96%) for muted backgrounds. The text color token is `--muted-color: #64748b`. Tailwind utility `text-muted` will produce the correct gray text.
  </action>
  <verify>
    <automated>cd apps/manager && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "IBM Plex Mono" apps/manager/tailwind.config.ts` finds a match in fontFamily.mono
    - `grep "var(--amber)" apps/manager/tailwind.config.ts` finds a match
    - `grep "var(--success)" apps/manager/tailwind.config.ts` finds a match
    - `grep "var(--success-bg)" apps/manager/tailwind.config.ts` finds a match
    - `grep "var(--sidebar-bg)" apps/manager/tailwind.config.ts` finds a match
    - TypeScript compilation passes (no errors on this file)
  </acceptance_criteria>
  <done>tailwind.config.ts has fontFamily.mono = IBM Plex Mono, all DESIGN.md semantic tokens mapped, shadcn tokens preserved. font-mono Tailwind utility resolves to IBM Plex Mono.</done>
</task>

<task type="auto">
  <name>Task 3: Install missing shadcn components (tooltip, dropdown-menu, tabs, popover, sheet, alert)</name>
  <files>
    apps/manager/components/ui/tooltip.tsx
    apps/manager/components/ui/dropdown-menu.tsx
    apps/manager/components/ui/tabs.tsx
    apps/manager/components/ui/popover.tsx
    apps/manager/components/ui/sheet.tsx
    apps/manager/components/ui/alert.tsx
  </files>
  <read_first>
    - apps/manager/components.json (confirm style=new-york, aliases)
  </read_first>
  <action>
Run the shadcn CLI to add the missing component primitives. Execute from inside apps/manager/:

```bash
cd apps/manager && npx shadcn@latest add tooltip dropdown-menu tabs popover sheet alert --yes
```

The `--yes` flag skips confirmation prompts. This command:
1. Installs required @radix-ui/* peer packages via npm
2. Generates component files in components/ui/ following the new-york style (per components.json)

If the command fails for any component, add them individually:
```bash
cd apps/manager && npx shadcn@latest add tooltip --yes
cd apps/manager && npx shadcn@latest add dropdown-menu --yes
cd apps/manager && npx shadcn@latest add tabs --yes
cd apps/manager && npx shadcn@latest add popover --yes
cd apps/manager && npx shadcn@latest add sheet --yes
cd apps/manager && npx shadcn@latest add alert --yes
```

Do NOT manually edit the generated files — they are shadcn territory. If the generated files include oklch values in the CSS-in-JS parts (unlikely but possible), that is acceptable for these generated files only — the global token fix in Task 1 ensures the CSS variables they reference will resolve to amber, not zinc.

After install, run the build to confirm no TypeScript errors:
```bash
cd apps/manager && npm run build 2>&1 | tail -20
```
  </action>
  <verify>
    <automated>ls apps/manager/components/ui/tooltip.tsx apps/manager/components/ui/dropdown-menu.tsx apps/manager/components/ui/tabs.tsx apps/manager/components/ui/alert.tsx 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `apps/manager/components/ui/tooltip.tsx` exists
    - `apps/manager/components/ui/dropdown-menu.tsx` exists
    - `apps/manager/components/ui/tabs.tsx` exists
    - `apps/manager/components/ui/popover.tsx` exists
    - `apps/manager/components/ui/sheet.tsx` exists
    - `apps/manager/components/ui/alert.tsx` exists
    - `npm run build --workspace=apps/manager` exits 0
  </acceptance_criteria>
  <done>All 6 shadcn components installed. Build passes. Radix peer dependencies added to apps/manager/package.json.</done>
</task>

</tasks>

<verification>
Run the full build after all three tasks complete:

```bash
cd apps/manager && npm run build 2>&1 | tail -30
```

Expected: exits 0, no TypeScript errors, no missing module errors.

Also verify the CSS fix visually: open the analytics page (which already uses shadcn Button) and confirm the primary button renders amber, not dark slate-gray.

Additional checks:
- `grep -c "oklch" apps/manager/app/globals.css` → 0
- `grep -c "@layer base" apps/manager/app/globals.css` → 1
- `grep "IBM Plex Mono" apps/manager/tailwind.config.ts` → match found
- All 6 new component files exist in `apps/manager/components/ui/`
</verification>

<success_criteria>
- globals.css has exactly one @layer base block, zero oklch values, --primary = amber
- tailwind.config.ts maps font-mono → IBM Plex Mono and all DESIGN.md semantic tokens
- 6 new shadcn components installed and available
- `npm run build --workspace=apps/manager` passes with zero errors
- No visual regressions on existing pages (legacy CSS classes still intact)
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-ui-design-system-and-component-library/04.1-01-SUMMARY.md`
</output>
