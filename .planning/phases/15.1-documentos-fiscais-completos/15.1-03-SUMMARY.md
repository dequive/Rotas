---
phase: "15.1"
plan: "03"
subsystem: "cargo"
status: complete
tags: [ddl, migration, transport-documents, opdoc]
dependency_graph:
  requires: ["01"]
  provides: [transport_document_extra_fields_ddl]
  affects: [15.1-06, 15.1-07, 15.1-08]
tech_stack:
  added: [JSONB]
  patterns: [alembic-migration, sqlalchemy-mapped-columns, jsonb-extra-fields]
key_files:
  created:
    - backend/alembic/versions/NEW_add_transport_doc_extra_fields.py
  modified:
    - backend/app/modules/cargo/models.py
    - backend/app/modules/cargo/service.py
decisions:
  - "extra_fields uses JSONB (nullable) — flexible key-value store for doc-type-specific metadata"
  - "recipient_name and recipient_nuit added as first-class columns (not buried in JSONB)"
  - "serialize_transport_document() created as canonical ORM-to-dict function"
metrics:
  duration: "15 minutes"
  completed_date: "2026-06-18"
  tasks_completed: 3
  files_changed: 3
---

# Phase 15.1 Plan 03: Wave 1B — DDL transport_documents extension (OPDOC-01) Summary

## One-liner

Extended `transport_documents` with `extra_fields` (JSONB), `recipient_name`, `recipient_nuit` columns + `serialize_transport_document()` serializer.

## What Was Built

- Alembic migration adding 3 columns to `transport_documents`
- `TransportDocument` ORM model updated with `extra_fields`, `recipient_name`, `recipient_nuit`
- `serialize_transport_document()` function added to cargo service

## Verification

All OPDOC-01 tests pass:
- `test_transport_document_has_extra_fields_column` ✓
- `test_transport_document_extra_fields_roundtrip` ✓

## Deviations from Plan

None.

## Commits

| Hash | Message |
|------|---------|
| eb8f640 | feat(15.1-03): extend transport_documents with extra_fields JSONB, recipient_name/nuit + serializer (OPDOC-01) |

## Self-Check: PASSED

- [x] `transport_documents` has `extra_fields`, `recipient_name`, `recipient_nuit`
- [x] TransportDocument ORM model exposes those three fields
- [x] Existing rows default to `extra_fields=NULL`
- [x] `serialize_transport_document()` returns all fields
