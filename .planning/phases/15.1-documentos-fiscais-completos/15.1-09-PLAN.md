---
plan: "09"
phase: "15.1-documentos-fiscais-completos"
wave: 4
status: pending
title: "Wave 4 — Implement tests + full suite validation"
requirements: [FDOC-01, FDOC-02, FDOC-03, FDOC-04, FDOC-05, OPDOC-01, OPDOC-02, OPDOC-03, OPDOC-04, OPDOC-05]
depends_on: ["04", "05", "06", "07", "08"]
files_modified:
  - backend/tests/test_fiscal_documents.py
  - backend/tests/test_operational_documents.py
autonomous: true
must_haves:
  truths:
    - "All FDOC-01..05 test stubs are replaced with real assertions that pass"
    - "All OPDOC-01..05 test stubs are replaced with real assertions that pass"
    - "Cross-tenant isolation tests pass for all new endpoints"
    - "pytest exits 0 with no unexpected failures on the full test suite"
  artifacts:
    - path: "backend/tests/test_fiscal_documents.py"
      provides: "Real integration tests for all fiscal document requirements"
      exports: []
    - path: "backend/tests/test_operational_documents.py"
      provides: "Real integration tests for all operational document requirements"
      exports: []
  key_links:
    - from: "backend/tests/test_fiscal_documents.py"
      to: "POST /api/v1/billing/documents/{id}/debit-note"
      via: "async_client.post()"
      pattern: "debit.note"
    - from: "backend/tests/test_operational_documents.py"
      to: "GET /api/v1/cargo/trips/{id}/document-checklist"
      via: "async_client.get()"
      pattern: "document.checklist"
---

<objective>
Replace all `@pytest.mark.skip` stubs in both test files with real integration test assertions. Run the full backend test suite and confirm it exits 0.

Purpose: validates that all 10 FDOC and OPDOC requirements are met end-to-end against a live database.
Output: working test files with real assertions; clean `pytest` run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.1-documentos-fiscais-completos/CONTEXT.md
@backend/tests/conftest.py
@backend/tests/test_fiscal_compliance.py
@.planning/phases/15.1-documentos-fiscais-completos/15.1-04-SUMMARY.md
@.planning/phases/15.1-documentos-fiscais-completos/15.1-05-SUMMARY.md
@.planning/phases/15.1-documentos-fiscais-completos/15.1-06-SUMMARY.md
@.planning/phases/15.1-documentos-fiscais-completos/15.1-07-SUMMARY.md
@.planning/phases/15.1-documentos-fiscais-completos/15.1-08-SUMMARY.md

<interfaces>
<!-- conftest.py fixtures available -->
db: AsyncSession          -- direct DB session
tenant_id: UUID           -- pre-created test tenant
auth_headers: dict        -- {"Authorization": "Bearer test-token", "X-Tenant-Id": str(tenant_id)}
async_client: AsyncClient -- httpx ASGI test client

<!-- Test helper pattern from test_fiscal_compliance.py -->
async def _make_contract(db, tenant_id, client_nuit="400123456") -> Contract
async def _make_vehicle(db, tenant_id) -> Vehicle
async def _make_driver(db, tenant_id) -> Driver
async def _make_trip(db, tenant_id, vehicle_id, driver_id, contract_id=None, **kw) -> Trip

<!-- New endpoints to test -->
POST /api/v1/billing/documents/{id}/debit-note       body: {amount, reason, iva_rate}
POST /api/v1/billing/documents/{id}/credit-note      body: {amount, reason}
POST /api/v1/billing/documents/{id}/invoice-receipt  no body
POST /api/v1/billing/documents/{id}/receipt          body: {amount_paid}
GET  /api/v1/billing/ar                              ?aging_bucket=&contract_id=

POST /api/v1/cargo/trips/{id}/guia-remessa           body: {recipient_name, document_number, ...}
POST /api/v1/cargo/trips/{id}/carta-porte            body: {document_number, border_post, country_destination, ...}
POST /api/v1/cargo/trips/{id}/dav                    body: {document_number, authorization_code, ...}
GET  /api/v1/cargo/trips/{id}/document-checklist

<!-- To create an issued billing_document for debit/credit note tests:
     1. create BillingDocument directly in DB with status="issued" and invoice_number set
     2. OR call the full create_document + issue_document service flow -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement test_fiscal_documents.py — real assertions</name>
  <files>backend/tests/test_fiscal_documents.py</files>
  <action>
Rewrite `backend/tests/test_fiscal_documents.py` completely — replace all stubs with real tests.

Follow the pattern from `backend/tests/test_fiscal_compliance.py`:
- Copy `_make_contract`, `_make_vehicle`, `_make_driver`, `_make_trip` helpers or import them.
- Add a `_make_issued_billing_document` helper that creates a BillingDocument with status="issued", invoice_number set, document_type="invoice", billing_period_start/end filled (use past month), and a contract_id.

**FDOC-01 tests:** Instantiate `BillingDocument()` and assert attribute access returns correct defaults.

**FDOC-02 tests:**
- Create issued billing_document via helper
- `async_client.post(f"/api/v1/billing/documents/{doc_id}/debit-note", json={...}, headers=auth_headers)`
- Assert 201, response has `document_type=="debit_note"`, `invoice_number` is not None, `parent_invoice_number` matches parent
- Draft parent test: create a draft document, POST debit-note → assert 409

**FDOC-03 tests:**
- Same pattern; assert `document_type=="credit_note"`, parent status unchanged after credit note creation

**FDOC-04 tests:**
- invoice-receipt: POST → assert 201, `document_type=="invoice_receipt"`, then GET parent → assert `status=="paid"`
- receipt: POST with `amount_paid`, assert 201, `document_type=="receipt"`, then GET parent → assert status unchanged

**FDOC-05 tests:**
- Create issued billing_document with `due_date` set to 45 days ago (so `days_overdue=45`, `aging_bucket="31_60"`)
- GET `/api/v1/billing/ar?aging_bucket=31_60` → assert response contains that document
- GET `/api/v1/billing/ar?aging_bucket=current` → assert that document NOT in response

Key implementation note for due_date setup: directly insert a `BillingDocument` with `due_date=date.today() - timedelta(days=45)` and `status="issued"`. The `due_date` column added in plan 02 — set it via `BillingDocument(due_date=..., ...)`.

All tests must be `async def` and use `await`.
  </action>
  <verify>
    <automated>cd C:/Users/Quive/OneDrive/Documents/Rotas/backend && python -m pytest tests/test_fiscal_documents.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All 10 tests in `test_fiscal_documents.py` pass (no skips, no failures)</done>
</task>

<task type="auto">
  <name>Task 2: Implement test_operational_documents.py — real assertions</name>
  <files>backend/tests/test_operational_documents.py</files>
  <action>
Rewrite `backend/tests/test_operational_documents.py` completely — replace all stubs with real tests.

Reuse or re-declare the same `_make_contract`, `_make_vehicle`, `_make_driver`, `_make_trip` helpers (copy from test_fiscal_compliance.py or import from a shared location if one exists).

Add a `_make_trip_with_extras(db, tenant_id, is_international=False, is_hazmat=False)` helper that creates a trip with the relevant flags if those fields exist on the Trip model.

**OPDOC-01 tests:**
- Instantiate `TransportDocument()` and assert `extra_fields` is None by default
- Assign a dict to `extra_fields`, flush/commit, re-query, assert dict preserved

**OPDOC-02 tests (guia_remessa):**
- Create a trip via helper
- `async_client.post(f"/api/v1/cargo/trips/{trip_id}/guia-remessa", json={...}, headers=auth_headers)`
- Required body: `{"recipient_name": "ABC Lda", "document_number": "GR-2026/001"}`
- Assert 201, `document_type=="guia_remessa"`, `pdf_url` present in response
- Missing recipient_name test: POST without `recipient_name` → assert 422
- Cross-tenant test: use a second tenant_id fixture, assert 404

**OPDOC-03 tests (carta_porte):**
- POST with `{document_number, border_post, country_destination}` → assert 201
- Assert `extra_fields["border_post"]` and `extra_fields["country_destination"]` in response
- Assert `pdf_url` in response
- GET the pdf_url → assert 200 with Content-Type `application/pdf`

**OPDOC-04 tests (dav):**
- POST with `{document_number, authorization_code}` → assert 201
- Assert `extra_fields["authorization_code"]` in response
- Assert NO `pdf_url` key in response (DAV is digital record only)

**OPDOC-05 tests (checklist):**
- Domestic trip checklist: create trip without international flag, GET checklist, assert 4 required types, assert `is_international==False`
- International trip checklist: create trip with international flag OR create a carta_porte_internacional transport_document for the trip, GET checklist, assert `carta_porte_internacional` in required types
- Hazmat checklist: create a CargoManifest with `is_hazmat=True` for the trip, GET checklist, assert `declaracao_carga_perigosa` in required types

All tests must be `async def`.
  </action>
  <verify>
    <automated>cd C:/Users/Quive/OneDrive/Documents/Rotas/backend && python -m pytest tests/test_operational_documents.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All 11 tests in `test_operational_documents.py` pass (no skips, no failures)</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and fix regressions</name>
  <files>backend/tests/test_fiscal_documents.py, backend/tests/test_operational_documents.py</files>
  <action>
Run the complete backend test suite:

```bash
cd C:/Users/Quive/OneDrive/Documents/Rotas/backend
python -m pytest tests/ -x --tb=short -q 2>&1
```

For any failing pre-existing tests:
1. Read the failure output carefully
2. Check if our new migrations or model changes caused the regression
3. Fix the minimal thing that makes the test pass — do NOT change the test assertion; change the implementation if needed
4. Common regression causes:
   - `serialize_billing_document` now has new required fields — check if any existing test asserts the exact key set
   - `BillingDocument` model now has new columns with defaults — should be safe
   - New Alembic migration heads — if tests create DB from scratch, ensure migrations apply cleanly

Run the suite again after fixes until `pytest tests/ -q` exits 0 (or all remaining failures are pre-existing and unrelated to Phase 15.1 work).

If pre-existing failures exist that are unrelated to Phase 15.1, document them in the SUMMARY.md but do NOT fix them here (out of scope).
  </action>
  <verify>
    <automated>cd C:/Users/Quive/OneDrive/Documents/Rotas/backend && python -m pytest tests/test_fiscal_documents.py tests/test_operational_documents.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>
- `tests/test_fiscal_documents.py`: 10 passed, 0 failed, 0 skipped
- `tests/test_operational_documents.py`: 11 passed, 0 failed, 0 skipped
- No new failures introduced in pre-existing test files
  </done>
</task>

</tasks>

<verification>
```bash
cd C:/Users/Quive/OneDrive/Documents/Rotas/backend
python -m pytest tests/test_fiscal_documents.py tests/test_operational_documents.py -v 2>&1 | tail -30
python -m pytest tests/ -q --tb=line 2>&1 | tail -10
```

Phase 15.1 tests: all green. Pre-existing suite: no new regressions.
</verification>

<success_criteria>
- `pytest tests/test_fiscal_documents.py` — 10 passed, 0 skipped, 0 failed
- `pytest tests/test_operational_documents.py` — 11 passed, 0 skipped, 0 failed
- Full suite has no new failures compared to pre-Phase-15.1 baseline
- All 10 FDOC/OPDOC requirements have at least one passing test covering the acceptance criteria in CONTEXT.md
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-documentos-fiscais-completos/15.1-09-SUMMARY.md`
</output>
