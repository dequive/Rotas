---
phase: "15.1"
plan: "01"
subsystem: "billing, cargo"
status: complete
tags: [tdd, stubs, fiscal, operational, documents]
dependency_graph:
  requires: []
  provides: [test_fiscal_documents, test_operational_documents]
  affects: [15.1-02, 15.1-03, 15.1-04, 15.1-05]
tech_stack:
  added: []
  patterns: [pytest-skip-stubs, asyncio_mode_auto]
key_files:
  created:
    - backend/tests/test_fiscal_documents.py
    - backend/tests/test_operational_documents.py
  modified: []
decisions:
  - "23 stubs collected (11 fiscal + 12 operational) — PLAN function list is authoritative over prompt summary counts"
metrics:
  duration: "5 minutes"
  completed_date: "2026-06-18"
  tasks_completed: 2
  files_changed: 2
---

# Phase 15.1 Plan 01: Wave 0 — Test Stubs (Fiscal + Operational) Summary

## One-liner

23 pytest skip stubs for FDOC-01..05 and OPDOC-01..05 covering billing document types, debit/credit notes, receipts, AR fields, transport document extensions, guia remessa, carta porte, DAV, and document checklist.

## What Was Built

Two new test files establishing the TDD contract for Phase 15.1 implementation waves:

- `backend/tests/test_fiscal_documents.py` — 11 async skip stubs covering all FDOC requirements:
  - FDOC-01 (2 stubs): BillingDocument DDL extension fields
  - FDOC-02 (3 stubs): Nota de Debito creation and isolation
  - FDOC-03 (2 stubs): Nota de Credito sequential numbering
  - FDOC-04 (2 stubs): Fatura-Recibo and Recibo transitions
  - FDOC-05 (2 stubs): AR endpoint and serialization fields

- `backend/tests/test_operational_documents.py` — 12 async skip stubs covering all OPDOC requirements:
  - OPDOC-01 (2 stubs): TransportDocument DDL extension fields
  - OPDOC-02 (3 stubs): Guia de Remessa creation and validation
  - OPDOC-03 (2 stubs): Carta de Porte Internacional with extra_fields
  - OPDOC-04 (2 stubs): DAV / Guia INATTER (no PDF)
  - OPDOC-05 (3 stubs): Document checklist for domestic/international/hazmat trips

All stubs are `async def` (compatible with pytest-asyncio `asyncio_mode = "auto"`), collect without import errors, and run 0 assertions.

## Verification

```
23 tests collected in 0.12s
```

No import errors, no unexpected failures.

## Decisions Made

- Used exact function names from PLAN.md tasks (11 fiscal + 12 operational = 23 total). The prompt summary mentioned "10 + 11 = 21" but the PLAN's explicit function list is authoritative — both counts are consistent with the PLAN source.
- Stubs use `...` body (Ellipsis) rather than `pass` to match Python convention for intentionally empty async stubs.

## Deviations from Plan

None — plan executed exactly as written. Stub count (23 vs 21 mentioned in prompt summary) reflects the PLAN.md function list, which is the authoritative source.

## Commits

| Hash | Message |
|------|---------|
| 19e80f8 | test(15.1-01): add 23 skip stubs for FDOC + OPDOC requirements |

## Self-Check: PASSED

- [x] `backend/tests/test_fiscal_documents.py` exists
- [x] `backend/tests/test_operational_documents.py` exists
- [x] `pytest --collect-only` reports 23 items, 0 errors
- [x] Commit `19e80f8` exists in git log
