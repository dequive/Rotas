---
phase: "15.1"
plan: "06"
subsystem: "cargo"
status: complete
tags: [operational, guia-remessa, pdf, opdoc]
dependency_graph:
  requires: ["03"]
  provides: [guia_remessa_endpoint]
  affects: [15.1-09]
tech_stack:
  added: [fpdf2]
  patterns: [pdf-generation, a4-landscape, cargo-document]
key_files:
  created:
    - backend/app/modules/cargo/exporters.py
  modified:
    - backend/app/modules/cargo/service.py
    - backend/app/modules/cargo/router.py
    - backend/app/modules/cargo/schemas.py
decisions:
  - "A4 landscape layout — fits shipper/recipient columns and cargo table side-by-side"
  - "PDF stored as local file via storage abstraction (same as billing PDFs)"
  - "recipient_name required (422 if missing) — it's the essential delivery address field"
metrics:
  duration: "25 minutes"
  completed_date: "2026-06-18"
  tasks_completed: 4
  files_changed: 4
---

# Phase 15.1 Plan 06: Wave 3A — Guia de Remessa (OPDOC-02) Summary

## One-liner

`POST /trips/{id}/guia-remessa` creates a transport document with A4 landscape PDF (shipper/recipient columns, cargo table, signature block) stored via local file provider.

## What Was Built

- `create_guia_remessa()` service function — validates recipient_name (422), creates TransportDocument with `document_type='guia_remessa'`, generates and stores PDF
- `GuiaRemessaCreate` schema with required `client_name`, `recipient_name`, `origin`, `destination`
- `POST /trips/{trip_id}/guia-remessa` router endpoint (status 201, WRITE_ROLES)
- PDF generation: A4 landscape, company header, shipper/recipient table, cargo details, signature block with UTF-8/DejaVuSans font support

## Verification

All OPDOC-02 tests pass:
- `test_create_guia_remessa_stores_document` ✓
- `test_guia_remessa_pdf_generated` ✓
- `test_guia_remessa_requires_recipient_name` ✓

## Deviations from Plan

None.

## Commits

| Hash | Message |
|------|---------|
| da7a383 | feat(15.1): fiscal document types + operational cargo documents |

## Self-Check: PASSED

- [x] `POST /trips/{id}/guia-remessa` creates `document_type='guia_remessa'`
- [x] Response includes `document_id` and `pdf_url`
- [x] PDF generated with A4 landscape, shipper/recipient columns, cargo table, signature block
- [x] Missing `recipient_name` returns 422
