---
phase: 04.1-ui-design-system-and-component-library
plan: "08"
subsystem: manager-frontend
tags:
  - design-system
  - css-cleanup
  - build-verification
dependency_graph:
  requires:
    - 04.1-05  # ControlTowerOverview migrated (tower-grid, worklist-grid eliminated)
    - 04.1-06  # FuelControlBoard, FleetHistoryBoard, TransportCargoBoard, CostMarginBoard migrated
    - 04.1-07  # page.tsx billing section migrated (billing-kpi-grid, queue-grid eliminated)
  provides:
    - clean-globals-css
    - phase-4.1-automated-tasks-complete
  affects:
    - apps/manager/app/globals.css
tech_stack:
  added: []
  patterns:
    - CSS audit via grep before removal (zero false positives)
    - Selective removal of component-layer classes, preservation of layout/structural/modal CSS
key_files:
  created: []
  modified:
    - apps/manager/app/globals.css
decisions:
  - "Kept .worklist-item (1 usage in TransportCargoBoard) but removed .worklist container, .worklist-grid, .worklist-items — partial removal where inner element is still referenced"
  - "Kept .operational-heading only in media query responsive variants — wait, it was removed from both base and media queries since zero tsx references"
  - "transport-kpis and transport-kpi kept — still used in DriverScorecardPanel (scorecard panel not yet migrated)"
  - "data-source kept — still used in DriverDespachoTableAdmin.tsx with dynamic class variant (.data-source.api/.fallback)"
  - "eyebrow kept as CSS class — DriverDespachoTableAdmin uses className='eyebrow' directly (not just as prop name)"
metrics:
  duration_minutes: 20
  completed_date: "2026-06-06"
  tasks_completed: 1
  tasks_total: 2
  files_modified: 1
---

# Phase 04.1 Plan 08: CSS Audit, Cleanup, and Build Verification Summary

Audited all legacy CSS component classes in globals.css against grep results across all .tsx/.ts files in apps/manager. Removed 14 dead class definitions (220 lines deleted), confirmed build passes at exit code 0 with zero TypeScript errors. Human visual checkpoint not yet completed — required before Phase 4.1 can be declared done.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Audit and remove unused legacy CSS classes | d91e7fe | apps/manager/app/globals.css |

## CSS Audit Results

### Classes REMOVED (confirmed zero tsx/ts references)

| Class | Reason Removed |
|-------|---------------|
| `.billing-kpi-grid` | Replaced by `grid grid-cols-2 md:grid-cols-4 gap-3` Tailwind in page.tsx (Plan 07) |
| `.queue-grid` | Replaced by WorkQueue component (Plans 05, 07) |
| `.fuel-metric` + `.fuel-metric.alert` | Replaced by KpiCard in FuelControlBoard (Plan 06) |
| `.red-line`, `.orange-line`, `.blue-line`, `.cyan-line`, `.green-line` | Replaced by KpiCard `semantic` prop (Plans 05–07) |
| `.tower-grid`, `.tower-metric` | Replaced by KpiCard grid in ControlTowerOverview (Plan 05) |
| `.worklist-grid`, `.worklist` (container), `.worklist-items` | Replaced by WorkQueue component (Plans 05, 07) |
| `.compliance-grid` | No tsx references found |
| `.cost-kpis` | Replaced by KpiCard in CostMarginBoard (Plan 06) |
| `.operational-heading` (+ responsive variants) | No tsx references found (PageHeader component used instead) |

**Total removed:** 14 class definitions, 220 lines deleted. globals.css reduced from ~2185 to ~1967 lines.

### Classes KEPT (still referenced in tsx files)

| Class | File(s) Still Using It | Why Kept |
|-------|----------------------|----------|
| `.queue-icon` (+ `.blue`/`.orange`/`.green`/`.red`/`.cyan`) | FleetHistoryBoard.tsx, TransportCargoBoard.tsx, FuelControlBoard.tsx | Icon coloring in WorkQueue items not yet migrated to primitive |
| `.eyebrow` | DriverDespachoTableAdmin.tsx | Used as `className="eyebrow"` directly |
| `.domain-heading`, `.domain-section` | DriverDespachoTableAdmin.tsx, DriverScorecardPanel.tsx | Admin section headings not migrated |
| `.transport-kpis`, `.transport-kpi` | DriverScorecardPanel.tsx | Driver scorecard KPI grid not migrated |
| `.section-divider` | DriverScorecardPanel.tsx | `<hr className="section-divider">` |
| `.module-state` | DriverDespachoTableAdmin.tsx | Status badge for dispatch state |
| `.data-source` (+ `.api`, `.fallback`) | DriverDespachoTableAdmin.tsx | Dynamic class pattern `className={\`data-source ${result.source}\`}` |
| `.icon-btn`, `.tool-btn` | 8+ files (modals, forms, CostMarginBoard) | Modal close buttons, form action buttons |
| `.empty-state` | MaintenanceImminentPanel.tsx | Empty state placeholder |
| `.worklist-item` | TransportCargoBoard.tsx | Individual work queue item inside migrated component |

## Build Verification

```
npm run build (apps/manager) → exit code 0
TypeScript errors: 0
CSS parsing errors: 0
```

Full build output confirmed routes compile cleanly:
- `/` (page.tsx), `/contratos`, `/motoristas`, `/rotas-config`, `/viagens`, `/viaturas`
- All API routes (auth/login, auth/logout, contracts, drivers, known-routes, trips, vehicles)

**Acceptance criteria verified:**
- `grep -c "queue-grid|billing-kpi-grid|fuel-metric|red-line|tower-grid"` → 0 in globals.css: PASSED
- `grep -c "oklch" globals.css` → 0: PASSED
- `grep "IBM Plex Mono" tailwind.config.ts` → match found: PASSED
- `--amber: #f59e0b` present in :root: PASSED

## Human Visual Checkpoint (PENDING)

**Task 2 requires human verification before Phase 4.1 is complete.**

Start dev server: `cd apps/manager && npm run dev` then open http://localhost:3030

### What to verify

**1. Sidebar — visual identity**
- [ ] ROTAS logo visible at top
- [ ] 4 section groups: OPERACOES, FROTA, FINANCEIRO, CONFIG (small uppercase section headers)
- [ ] Active page has amber left border (2px amber stripe) on Torre de Controlo when on /
- [ ] Non-active items show hover effect on mouseover
- [ ] Logout button at bottom with separator

**2. CSS token fix (amber primary)**
- [ ] Any primary Button is AMBER (not dark slate/blue)
- [ ] DevTools: inspect `--primary` CSS variable → must show `38 92% 50%`
- [ ] No oklch in computed styles for any element

**3. KPI cards (IBM Plex Mono)**
- [ ] Billing KPI values ("Sem contrato: 3", etc.) render in IBM Plex Mono (monospaced)
- [ ] Digits are visually distinct from letters (0 vs O, 1 vs l)
- [ ] NOT in Manrope sans-serif

**4. Status badges (colored dot)**
- [ ] Trip status badges show small colored circle to the LEFT of status text
- [ ] "Em Rota" = green dot, "Alerta" = red dot, "Pendente" = orange dot
- [ ] No plain text badges without dots

**5. Control tower**
- [ ] Torre de Controlo loads 6 metric KPI cards in top grid
- [ ] WorkQueue cards render with colored left stripe
- [ ] Work items (if any) show reference in monospace font

**6. Monetary values**
- [ ] Billing table "Valor" column shows MZN amounts in IBM Plex Mono
- [ ] Billing KPI card values are in IBM Plex Mono

Report any visual regressions. Type "approved" if all items pass.

## Deviations from Plan

**Auto-removed fewer classes than plan listed as candidates:**
- `.eyebrow` — plan listed as candidate for removal, but it IS still used as a direct CSS class in `DriverDespachoTableAdmin.tsx:132` (`className="eyebrow"`). Kept.
- `.worklist-item` — plan listed `.worklist` for removal, but `.worklist-item` inner element is still used in `TransportCargoBoard.tsx:324`. The container `.worklist` was removed but the item element kept.
- `.transport-kpis` — plan listed as removal candidate, but used in `DriverScorecardPanel.tsx` (scorecard not migrated in Phase 4.1). Kept with comment.

These are correct per the plan's rule: "If any class still has TSX references: do NOT remove it."

## Known Stubs

None — no new components created, no data sources modified. CSS-only cleanup.

## Self-Check: PASSED

- `d91e7fe` commit exists: FOUND (git log confirms)
- `apps/manager/app/globals.css` modified: CONFIRMED (220 lines removed)
- Grep for removed classes in globals.css → 0 matches: PASSED
- `npm run build` exit code 0: PASSED
- `--amber` token preserved: PASSED
- IBM Plex Mono in tailwind.config.ts: PASSED
