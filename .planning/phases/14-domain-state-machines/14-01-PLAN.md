---
plan: 14-01
phase: 14
title: "BillingDocument + Contract State Machines"
wave: 1
depends_on: []
requirements: [SM-01, SM-02]
files_modified:
  - backend/app/modules/billing/models.py
  - backend/app/modules/billing/service.py
  - backend/app/modules/billing/router.py
  - backend/app/modules/billing/schemas.py
  - backend/app/modules/contracts/models.py
  - backend/app/modules/contracts/service.py
  - backend/app/modules/contracts/router.py
  - backend/app/modules/contracts/schemas.py
  - backend/app/worker.py
  - alembic/versions/
autonomous: true
---

# Plan 14-01: BillingDocument + Contract State Machines

## Objective

Fechar as state machines de `BillingDocument` e `Contract`. Actualmente ambos têm campo `status: String(30)` sem transições guardadas nem lógica de negócio. Este plano adiciona os estados em falta, guards no service layer, ARQ crons, e endpoint de PATCH com audit log — sem alterar a lógica de negócio existente.

---

## Tasks

### Task 1: Migração — Adicionar estados e campos em falta a BillingDocument e Contract

<read_first>
- backend/app/modules/billing/models.py
- backend/app/modules/contracts/models.py
- alembic/env.py
- alembic/versions/ (ver última migração para naming convention)
</read_first>

<action>
Criar nova migração Alembic `alembic/versions/{timestamp}_sm_billing_contract_states.py`:

```python
"""SM-01 SM-02: billing document and contract state fields

Revision ID: sm01_sm02_states
Revises: <latest_revision>
Create Date: 2026-06-18

"""
from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects import postgresql

revision = 'sm01_sm02_states'
down_revision = '<latest_revision>'
branch_labels = None
depends_on = None


def upgrade() -> None:
    # SM-01: BillingDocument — add paid_at and overdue_since_at
    op.add_column('billing_documents',
        sa.Column('paid_at', sa.DateTime(timezone=True), nullable=True))
    op.add_column('billing_documents',
        sa.Column('overdue_since_at', sa.DateTime(timezone=True), nullable=True))
    op.add_column('billing_documents',
        sa.Column('cancellation_reason', sa.Text(), nullable=True))

    # SM-02: Contract — add missing state fields
    # status already exists (default='active') — no column needed
    op.add_column('contracts',
        sa.Column('paused_at', sa.DateTime(timezone=True), nullable=True))
    op.add_column('contracts',
        sa.Column('terminated_at', sa.DateTime(timezone=True), nullable=True))
    op.add_column('contracts',
        sa.Column('termination_reason', sa.Text(), nullable=True))
    op.add_column('contracts',
        sa.Column('renewed_at', sa.DateTime(timezone=True), nullable=True))

    # RLS policies for new columns are inherited from table-level policy — no action needed


def downgrade() -> None:
    op.drop_column('billing_documents', 'paid_at')
    op.drop_column('billing_documents', 'overdue_since_at')
    op.drop_column('billing_documents', 'cancellation_reason')
    op.drop_column('contracts', 'paused_at')
    op.drop_column('contracts', 'terminated_at')
    op.drop_column('contracts', 'termination_reason')
    op.drop_column('contracts', 'renewed_at')
```

Gerar com: `alembic revision --autogenerate -m "SM-01 SM-02: billing document and contract state fields"`
Verificar e ajustar gerado para corresponder ao acima.
</action>

<acceptance_criteria>
- `alembic upgrade head` corre sem erros
- `SELECT column_name FROM information_schema.columns WHERE table_name='billing_documents' AND column_name IN ('paid_at','overdue_since_at','cancellation_reason')` retorna 3 linhas
- `SELECT column_name FROM information_schema.columns WHERE table_name='contracts' AND column_name IN ('paused_at','terminated_at','termination_reason','renewed_at')` retorna 4 linhas
</acceptance_criteria>

---

### Task 2: Actualizar Models — BillingDocument e Contract

<read_first>
- backend/app/modules/billing/models.py
- backend/app/modules/contracts/models.py
</read_first>

<action>
Em `backend/app/modules/billing/models.py`, na classe `BillingDocument`, adicionar após o campo `status`:

```python
# SM-01: State machine fields
paid_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
overdue_since_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
cancellation_reason: Mapped[str | None] = mapped_column(Text(), nullable=True)

# SM-01: Valid status values: draft | issued | paid | overdue | cancelled
# Transitions:
#   draft → issued (issue action)
#   issued → paid (mark_paid action, sets paid_at)
#   issued → overdue (ARQ cron, sets overdue_since_at)
#   overdue → paid (mark_paid action)
#   draft | overdue → cancelled (cancel action, requires cancellation_reason)
```

Em `backend/app/modules/contracts/models.py`, na classe `Contract`, adicionar após o campo `status`:

```python
# SM-02: State machine fields
paused_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
terminated_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
termination_reason: Mapped[str | None] = mapped_column(Text(), nullable=True)
renewed_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)

# SM-02: Valid status values: draft | active | paused | expired | terminated
# Transitions:
#   draft → active (activate action or on first trip assignment)
#   active → paused (pause action, sets paused_at)
#   paused → active (resume action, clears paused_at)
#   active | paused → expired (ARQ cron, when ends_at < NOW())
#   active | paused | expired → terminated (terminate action, requires termination_reason)
#   expired → active (renew action, extends ends_at, sets renewed_at)
```

Adicionar import `from datetime import datetime` se não existir.
</action>

<acceptance_criteria>
- `grep -n "paid_at\|overdue_since_at\|cancellation_reason" backend/app/modules/billing/models.py` retorna 3 linhas
- `grep -n "paused_at\|terminated_at\|termination_reason\|renewed_at" backend/app/modules/contracts/models.py` retorna 4 linhas
- `python -c "from app.modules.billing.models import BillingDocument; from app.modules.contracts.models import Contract"` corre sem ImportError
</acceptance_criteria>

---

### Task 3: Service Layer — Guards e Transitions BillingDocument (SM-01)

<read_first>
- backend/app/modules/billing/service.py
- backend/app/modules/audit/service.py
- backend/app/core/errors.py (padrão ApiError)
</read_first>

<action>
Em `backend/app/modules/billing/service.py`, adicionar as seguintes funções após as funções existentes de criação/listagem:

```python
# SM-01: BillingDocument state machine transitions
_BILLING_VALID_TRANSITIONS: dict[str, set[str]] = {
    "draft":    {"issued", "cancelled"},
    "issued":   {"paid", "overdue", "cancelled"},
    "overdue":  {"paid", "cancelled"},
    "paid":     set(),      # terminal
    "cancelled": set(),     # terminal
}

async def transition_billing_document(
    db: AsyncSession,
    *,
    document: BillingDocument,
    new_status: str,
    user_id: UUID,
    tenant_id: UUID,
    paid_at: datetime | None = None,
    cancellation_reason: str | None = None,
) -> BillingDocument:
    """Guard-enforced state transition for BillingDocument."""
    allowed = _BILLING_VALID_TRANSITIONS.get(document.status, set())
    if new_status not in allowed:
        raise ApiError(
            "invalid_state_transition",
            f"BillingDocument cannot transition from '{document.status}' to '{new_status}'",
            status_code=409,
        )
    if new_status == "cancelled" and not cancellation_reason:
        raise ApiError(
            "cancellation_reason_required",
            "cancellation_reason is required when cancelling a billing document",
            status_code=422,
        )

    old_status = document.status
    document.status = new_status

    if new_status == "paid":
        document.paid_at = paid_at or datetime.utcnow()
    elif new_status == "overdue":
        document.overdue_since_at = datetime.utcnow()
    elif new_status == "cancelled":
        document.cancellation_reason = cancellation_reason

    await record_audit_log(
        db,
        tenant_id=tenant_id,
        action=f"billing.document.{new_status}",
        entity_type="billing_document",
        entity_id=document.id,
        user_id=user_id,
        old_values={"status": old_status},
        new_values={"status": new_status, "paid_at": str(document.paid_at) if document.paid_at else None},
    )
    db.add(document)
    return document


async def mark_billing_document_paid(
    db: AsyncSession,
    *,
    document_id: UUID,
    tenant_id: UUID,
    user_id: UUID,
    paid_at: datetime | None = None,
) -> BillingDocument:
    doc = await db.get(BillingDocument, document_id)
    if not doc or doc.tenant_id != tenant_id:
        raise ApiError("not_found", "BillingDocument not found", status_code=404)
    return await transition_billing_document(
        db, document=doc, new_status="paid",
        user_id=user_id, tenant_id=tenant_id, paid_at=paid_at,
    )


async def cancel_billing_document(
    db: AsyncSession,
    *,
    document_id: UUID,
    tenant_id: UUID,
    user_id: UUID,
    reason: str,
) -> BillingDocument:
    doc = await db.get(BillingDocument, document_id)
    if not doc or doc.tenant_id != tenant_id:
        raise ApiError("not_found", "BillingDocument not found", status_code=404)
    return await transition_billing_document(
        db, document=doc, new_status="cancelled",
        user_id=user_id, tenant_id=tenant_id, cancellation_reason=reason,
    )
```

Adicionar imports necessários em topo do ficheiro:
```python
from app.core.errors import ApiError
from app.modules.audit.service import record_audit_log
from datetime import datetime
```
</action>

<acceptance_criteria>
- `grep -n "transition_billing_document\|mark_billing_document_paid\|cancel_billing_document" backend/app/modules/billing/service.py` retorna 3 funções
- `grep -n "_BILLING_VALID_TRANSITIONS" backend/app/modules/billing/service.py` retorna 1 linha
- `python -c "from app.modules.billing.service import transition_billing_document"` corre sem ImportError
</acceptance_criteria>

---

### Task 4: Service Layer — Guards e Transitions Contract (SM-02)

<read_first>
- backend/app/modules/contracts/service.py
- backend/app/modules/audit/service.py
- backend/app/core/errors.py
</read_first>

<action>
Em `backend/app/modules/contracts/service.py`, adicionar após funções existentes:

```python
# SM-02: Contract state machine transitions
_CONTRACT_VALID_TRANSITIONS: dict[str, set[str]] = {
    "draft":      {"active"},
    "active":     {"paused", "expired", "terminated"},
    "paused":     {"active", "expired", "terminated"},
    "expired":    {"active", "terminated"},   # active via renew
    "terminated": set(),  # terminal
}

async def transition_contract(
    db: AsyncSession,
    *,
    contract: Contract,
    new_status: str,
    user_id: UUID,
    tenant_id: UUID,
    termination_reason: str | None = None,
    new_ends_at: datetime | None = None,
) -> Contract:
    """Guard-enforced state transition for Contract."""
    allowed = _CONTRACT_VALID_TRANSITIONS.get(contract.status, set())
    if new_status not in allowed:
        raise ApiError(
            "invalid_state_transition",
            f"Contract cannot transition from '{contract.status}' to '{new_status}'",
            status_code=409,
        )
    if new_status == "terminated" and not termination_reason:
        raise ApiError(
            "termination_reason_required",
            "termination_reason is required when terminating a contract",
            status_code=422,
        )

    old_status = contract.status
    contract.status = new_status

    now = datetime.utcnow()
    if new_status == "paused":
        contract.paused_at = now
    elif new_status == "active" and old_status == "paused":
        contract.paused_at = None
    elif new_status == "active" and old_status == "expired":
        # Renew: extend ends_at
        if not new_ends_at:
            raise ApiError("ends_at_required", "new ends_at is required for contract renewal", status_code=422)
        contract.ends_at = new_ends_at
        contract.renewed_at = now
    elif new_status == "terminated":
        contract.terminated_at = now
        contract.termination_reason = termination_reason

    await record_audit_log(
        db,
        tenant_id=tenant_id,
        action=f"contract.{new_status}",
        entity_type="contract",
        entity_id=contract.id,
        user_id=user_id,
        old_values={"status": old_status},
        new_values={"status": new_status},
    )
    db.add(contract)
    return contract


async def renew_contract(
    db: AsyncSession,
    *,
    contract_id: UUID,
    tenant_id: UUID,
    user_id: UUID,
    new_ends_at: datetime,
) -> Contract:
    contract = await db.get(Contract, contract_id)
    if not contract or contract.tenant_id != tenant_id:
        raise ApiError("not_found", "Contract not found", status_code=404)
    return await transition_contract(
        db, contract=contract, new_status="active",
        user_id=user_id, tenant_id=tenant_id, new_ends_at=new_ends_at,
    )
```
</action>

<acceptance_criteria>
- `grep -n "_CONTRACT_VALID_TRANSITIONS\|transition_contract\|renew_contract" backend/app/modules/contracts/service.py` retorna 3+ linhas
- `python -c "from app.modules.contracts.service import transition_contract, renew_contract"` sem ImportError
</acceptance_criteria>

---

### Task 5: Router Endpoints — PATCH para transições (SM-01 + SM-02)

<read_first>
- backend/app/modules/billing/router.py
- backend/app/modules/contracts/router.py
- backend/app/modules/billing/schemas.py
- backend/app/modules/contracts/schemas.py
</read_first>

<action>
**Em `backend/app/modules/billing/schemas.py`**, adicionar schemas:
```python
class BillingDocumentMarkPaidRequest(BaseModel):
    paid_at: datetime | None = None

class BillingDocumentCancelRequest(BaseModel):
    reason: str = Field(..., min_length=5, max_length=500)
```

**Em `backend/app/modules/billing/router.py`**, adicionar endpoints (após endpoints existentes):
```python
@router.patch(
    "/documents/{document_id}/mark-paid",
    response_model=BillingDocumentRead,
    summary="Mark billing document as paid (SM-01)",
)
async def mark_billing_document_paid_endpoint(
    document_id: UUID,
    body: BillingDocumentMarkPaidRequest,
    db: AsyncSession = Depends(get_session),
    principal: UserPrincipal = Depends(require_roles(["owner", "admin"])),
):
    doc = await mark_billing_document_paid(
        db,
        document_id=document_id,
        tenant_id=principal.tenant_id,
        user_id=principal.user_id,
        paid_at=body.paid_at,
    )
    await db.commit()
    await db.refresh(doc)
    return doc


@router.patch(
    "/documents/{document_id}/cancel",
    response_model=BillingDocumentRead,
    summary="Cancel billing document (SM-01)",
)
async def cancel_billing_document_endpoint(
    document_id: UUID,
    body: BillingDocumentCancelRequest,
    db: AsyncSession = Depends(get_session),
    principal: UserPrincipal = Depends(require_roles(["owner", "admin"])),
):
    doc = await cancel_billing_document(
        db,
        document_id=document_id,
        tenant_id=principal.tenant_id,
        user_id=principal.user_id,
        reason=body.reason,
    )
    await db.commit()
    await db.refresh(doc)
    return doc
```

**Em `backend/app/modules/contracts/schemas.py`**, adicionar:
```python
class ContractTransitionRequest(BaseModel):
    action: Literal["activate", "pause", "resume", "expire", "terminate", "renew"]
    termination_reason: str | None = Field(None, min_length=5, max_length=500)
    new_ends_at: datetime | None = None
```

**Em `backend/app/modules/contracts/router.py`**, adicionar endpoint:
```python
@router.patch(
    "/{contract_id}/status",
    response_model=ContractRead,
    summary="Transition contract status (SM-02)",
)
async def transition_contract_endpoint(
    contract_id: UUID,
    body: ContractTransitionRequest,
    db: AsyncSession = Depends(get_session),
    principal: UserPrincipal = Depends(require_roles(["owner", "admin"])),
):
    action_to_status = {
        "activate": "active", "pause": "paused", "resume": "active",
        "expire": "expired", "terminate": "terminated", "renew": "active",
    }
    new_status = action_to_status[body.action]
    contract = await db.get(Contract, contract_id)
    if not contract or contract.tenant_id != principal.tenant_id:
        raise ApiError("not_found", "Contract not found", status_code=404)
    updated = await transition_contract(
        db, contract=contract, new_status=new_status,
        user_id=principal.user_id, tenant_id=principal.tenant_id,
        termination_reason=body.termination_reason,
        new_ends_at=body.new_ends_at,
    )
    await db.commit()
    await db.refresh(updated)
    return updated
```
</action>

<acceptance_criteria>
- `grep -n "mark-paid\|cancel\|/status" backend/app/modules/billing/router.py backend/app/modules/contracts/router.py` retorna 3 endpoints
- `curl -s http://localhost:8000/openapi.json | python -c "import json,sys; d=json.load(sys.stdin); print([p for p in d['paths'] if 'mark-paid' in p])"` retorna 1 path
- `curl -s http://localhost:8000/openapi.json | python -c "import json,sys; d=json.load(sys.stdin); print([p for p in d['paths'] if 'contracts' in p and 'status' in p])"` retorna 1 path
</acceptance_criteria>

---

### Task 6: ARQ Crons — mark_overdue_billing_documents + expire_contracts (SM-01 + SM-02)

<read_first>
- backend/app/worker.py (estrutura de crons existentes)
- backend/app/modules/billing/models.py
- backend/app/modules/contracts/models.py
- backend/app/database.py (padrão get_admin_session para ARQ)
</read_first>

<action>
Em `backend/app/worker.py`, adicionar as duas funções de cron após os crons existentes:

```python
async def task_mark_overdue_billing_documents(ctx: dict) -> str:
    """SM-01: Daily cron — mark issued billing documents past due_date as overdue.
    Runs at 01:00 Africa/Maputo.
    """
    from sqlalchemy import update, and_
    from datetime import datetime, timezone
    from app.modules.billing.models import BillingDocument

    async with get_admin_session() as db:
        now = datetime.now(timezone.utc)
        result = await db.execute(
            update(BillingDocument)
            .where(
                and_(
                    BillingDocument.status == "issued",
                    BillingDocument.due_date < now,
                )
            )
            .values(status="overdue", overdue_since_at=now)
            .returning(BillingDocument.id)
        )
        overdue_ids = result.scalars().all()
        await db.commit()

    count = len(overdue_ids)
    logger.info("task_mark_overdue_billing_documents", count=count)
    return f"Marked {count} billing documents as overdue"


async def task_expire_contracts(ctx: dict) -> str:
    """SM-02: Daily cron — mark active/paused contracts past ends_at as expired.
    Runs at 00:30 Africa/Maputo.
    """
    from sqlalchemy import update, and_
    from datetime import datetime, timezone
    from app.modules.contracts.models import Contract

    async with get_admin_session() as db:
        now = datetime.now(timezone.utc)
        result = await db.execute(
            update(Contract)
            .where(
                and_(
                    Contract.status.in_(["active", "paused"]),
                    Contract.ends_at != None,
                    Contract.ends_at < now,
                )
            )
            .values(status="expired")
            .returning(Contract.id)
        )
        expired_ids = result.scalars().all()
        await db.commit()

    count = len(expired_ids)
    logger.info("task_expire_contracts", count=count)
    return f"Expired {count} contracts"
```

**Registar os crons em `WorkerSettings.cron_jobs`** (lista existente de cron_jobs no worker):
```python
# SM-01: Daily at 01:00 Africa/Maputo (UTC+2 = 23:00 UTC)
cron(task_mark_overdue_billing_documents, hour=23, minute=0, run_at_startup=False),
# SM-02: Daily at 00:30 Africa/Maputo (UTC+2 = 22:30 UTC)
cron(task_expire_contracts, hour=22, minute=30, run_at_startup=False),
```

**Registar as funções em `functions` list** do `WorkerSettings`:
```python
task_mark_overdue_billing_documents,
task_expire_contracts,
```
</action>

<acceptance_criteria>
- `grep -n "task_mark_overdue_billing_documents\|task_expire_contracts" backend/app/worker.py` retorna 4+ linhas (def + cron + functions list)
- `python -c "from app.worker import task_mark_overdue_billing_documents, task_expire_contracts"` sem ImportError
- Worker inicia sem erro: `python -m arq app.worker.WorkerSettings --check` retorna exit 0
</acceptance_criteria>

---

## Verification

### Must-Haves (derived from SM-01 + SM-02 success criteria)

- [ ] `BillingDocument` com `due_date` no passado e `status='issued'` é marcado `overdue` após cron
- [ ] `PATCH /api/v1/billing/documents/{id}/mark-paid` transiciona `issued` → `paid` e regista audit log com `action='billing.document.paid'`
- [ ] `PATCH /api/v1/billing/documents/{id}/mark-paid` com documento em `draft` retorna HTTP 409 `invalid_state_transition`
- [ ] `Contract` com `ends_at` no passado e `status='active'` é marcado `expired` após cron
- [ ] `PATCH /api/v1/contracts/{id}/status` com `action='renew'` sem `new_ends_at` retorna HTTP 422 `ends_at_required`
- [ ] `PATCH /api/v1/contracts/{id}/status` com `action='terminate'` sem `termination_reason` retorna HTTP 422 `termination_reason_required`
- [ ] Todas as transições registam linha em `audit_logs` com `entity_type` e `action` corretos
- [ ] Transições inválidas (e.g. `paid → issued`) retornam HTTP 409 `invalid_state_transition`

### Tests to Run

```bash
cd backend
pytest tests/ -k "billing" -v
pytest tests/ -k "contract" -v
# Verificar coverage das state machines:
pytest tests/ -k "state_machine or sm01 or sm02" -v
```
