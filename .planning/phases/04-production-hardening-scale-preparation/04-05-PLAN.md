---
phase: 04-production-hardening-scale-preparation
plan: 05
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - backend/app/modules/billing/models.py
  - backend/app/modules/trips/models.py
  - backend/app/modules/fuel/models.py
  - backend/app/modules/workshop/models.py
  - backend/app/modules/contracts/models.py
  - backend/app/modules/trip_orders/models.py
autonomous: true
requirements:
  - MAINT-01

must_haves:
  truths:
    - "All Mapped[float] annotations on Numeric(x,y) columns are updated to Mapped[Decimal]"
    - "No new Alembic migration is needed — this is annotation-only (D-15 confirmed)"
    - "mypy or ruff checks no longer flag Mapped[float] on Numeric columns"
    - "No runtime behavior changes — SQLAlchemy already returns Decimal values from Numeric columns"
  artifacts:
    - path: "backend/app/modules/billing/models.py"
      provides: "Decimal annotations on subtotal, tax_amount, total_amount, quantity, unit_price, amount"
      contains: "Mapped[Decimal]"
    - path: "backend/app/modules/trips/models.py"
      provides: "Decimal annotations on cost columns"
      contains: "Mapped[Decimal]"
    - path: "backend/app/modules/fuel/models.py"
      provides: "Decimal annotations on liters, price_per_liter, total_cost"
      contains: "Mapped[Decimal]"
  key_links:
    - from: "backend/app/modules/billing/models.py"
      to: "PostgreSQL Numeric(x,y) columns"
      via: "SQLAlchemy type mapping — Numeric column → Decimal Python type"
      pattern: "Mapped\\[Decimal\\]"
---

<objective>
Decimal type annotation cleanup — update Mapped[float] to Mapped[Decimal] on all Numeric(x,y) columns.

Purpose: D-15 confirmed no schema migration needed. This is a code quality hardening task: Python type annotations should reflect the actual runtime type (Decimal) returned by SQLAlchemy for Numeric columns. Prevents subtle bugs where code assumes float arithmetic on monetary values.
Output: Six model files updated with correct Decimal annotations; no migration file (confirmed by research).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-production-hardening-scale-preparation/04-RESEARCH.md

<interfaces>
<!-- D-15 confirmed: All monetary/financial columns already use Numeric(x,y) at DB level.
     Mapped[float] annotations are WRONG — SQLAlchemy returns Decimal at runtime, not float.
     This is annotation-only fix. Zero schema changes. -->

<!-- Files requiring annotation updates (from RESEARCH.md D-15 section) -->
1. billing/models.py — subtotal, tax_amount, total_amount, quantity, unit_price, amount
2. trips/models.py — total_fuel_cost, total_expense_cost, total_transport_cost,
                     actual_revenue, actual_margin, cost (TripStop), amount (TripCost)
3. fuel/models.py — liters, price_per_liter, total_cost, capacity_liters,
                    current_stock_liters, and all other Numeric columns
4. workshop/models.py — estimated_cost, actual_cost, current_quantity,
                        average_unit_cost, unit_cost, total_cost, quantity, balance_after_quantity
5. contracts/models.py — default_unit_price
6. trip_orders/models.py — estimated cost columns

<!-- Required import change -->
BEFORE: from decimal import Decimal  # may not be imported yet
AFTER (add if missing): from decimal import Decimal

<!-- Pattern to apply -->
BEFORE: some_column: Mapped[float] = mapped_column(Numeric(x, y))
AFTER:  some_column: Mapped[Decimal] = mapped_column(Numeric(x, y))

BEFORE: some_column: Mapped[float | None] = mapped_column(Numeric(x, y))
AFTER:  some_column: Mapped[Decimal | None] = mapped_column(Numeric(x, y))
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Mapped[float] → Mapped[Decimal] across all financial model files</name>
  <read_first>
    - backend/app/modules/billing/models.py (full file — identify all Numeric columns with float annotation)
    - backend/app/modules/trips/models.py (full file)
    - backend/app/modules/fuel/models.py (full file)
    - backend/app/modules/workshop/models.py (full file — note: already partially read)
    - backend/app/modules/contracts/models.py (full file)
    - backend/app/modules/trip_orders/models.py (full file)
  </read_first>
  <files>
    backend/app/modules/billing/models.py
    backend/app/modules/trips/models.py
    backend/app/modules/fuel/models.py
    backend/app/modules/workshop/models.py
    backend/app/modules/contracts/models.py
    backend/app/modules/trip_orders/models.py
  </files>
  <action>
For each file listed:

1. Add `from decimal import Decimal` to the imports block (after existing stdlib imports, before sqlalchemy imports). If already imported, skip.

2. Replace every occurrence of `Mapped[float]` on a `Numeric(...)` column with `Mapped[Decimal]`.
   Replace every occurrence of `Mapped[float | None]` on a `Numeric(...)` column with `Mapped[Decimal | None]`.

**DO NOT change:**
- Columns that are `Integer` — those legitimately use `int` annotations
- Columns that are `Float` type (if any exist) — those are legitimate float columns (but confirm none exist per D-15)
- Non-ORM float values (service layer variables, computed values, plain Python variables)

**Rule:** Only change annotations where the SQLAlchemy column type is `Numeric(x, y)` or `Numeric(x,y)`. The column type is in the `mapped_column(...)` call on the same line.

**Example transformations:**
```python
# BEFORE
liters: Mapped[float] = mapped_column(Numeric(10, 2))
total_cost: Mapped[float | None] = mapped_column(Numeric(14, 2))

# AFTER
liters: Mapped[Decimal] = mapped_column(Numeric(10, 2))
total_cost: Mapped[Decimal | None] = mapped_column(Numeric(14, 2))
```

After editing all 6 files, run ruff on each:
```bash
cd backend && ruff check app/modules/billing/models.py app/modules/trips/models.py app/modules/fuel/models.py app/modules/workshop/models.py app/modules/contracts/models.py app/modules/trip_orders/models.py
```

Fix any `UP006` (use `Decimal` instead of `typing.Decimal`) or import order issues (`I001`) that ruff reports.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.modules.billing.models import BillingDocument; from app.modules.trips.models import Trip; from app.modules.fuel.models import FuelLog; print('all models import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "Mapped\[float\]" backend/app/modules/billing/models.py` returns 0 matches
    - `grep -r "Mapped\[float\]" backend/app/modules/trips/models.py` returns 0 matches for Numeric columns (verify visually — integer columns with Mapped[int] are unrelated and correct)
    - `grep -r "Mapped\[float\]" backend/app/modules/fuel/models.py` returns 0 matches for Numeric columns
    - `grep -r "Mapped\[float\]" backend/app/modules/workshop/models.py` returns 0 matches for Numeric columns
    - All 6 model files contain `from decimal import Decimal`
    - `python -c "from app.modules.billing.models import BillingDocument"` exits 0
    - `python -c "from app.modules.trips.models import Trip"` exits 0
    - `ruff check backend/app/modules/billing/models.py backend/app/modules/trips/models.py backend/app/modules/fuel/models.py backend/app/modules/workshop/models.py backend/app/modules/contracts/models.py backend/app/modules/trip_orders/models.py` exits 0
    - `alembic upgrade head` exits 0 (no new migration needed — confirms annotation-only change)
  </acceptance_criteria>
  <done>All Numeric(x,y) columns annotated Mapped[Decimal]; no schema changes; ruff clean; models import without error</done>
</task>

<task type="auto">
  <name>Task 2: Verify no regressions — full backend test suite</name>
  <read_first>
    - backend/pyproject.toml (pytest config — asyncio_mode, testpaths)
  </read_first>
  <files></files>
  <action>
Run the full backend test suite to confirm that changing Mapped[float] to Mapped[Decimal] caused no runtime regressions. SQLAlchemy should already return Decimal at runtime from Numeric columns, so this should be transparent.

```bash
cd backend && python -m pytest tests/ -x -q --timeout=30
```

If any test fails due to type comparison (e.g., `assert value == 40.0` where value is now `Decimal("40.0")`), fix the assertion to use `float(value) == 40.0` or `Decimal("40.0")` as appropriate. The fix belongs in the test, not in the model.

If `alembic upgrade head` shows a pending migration after the annotation change, that means a `Mapped[float]` was on a `Float` column (not Numeric) that was accidentally changed. Revert that specific change and use `Mapped[float]` on actual Float columns.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/ -x -q --timeout=60</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/ -x -q` exits 0 — all tests pass
    - `alembic check` (or `alembic upgrade head`) exits 0 with no pending migrations
    - No test files were modified except to fix type comparison assertions (if any)
  </acceptance_criteria>
  <done>Full test suite green after annotation changes; no schema drift; Decimal annotations applied consistently</done>
</task>

</tasks>

<verification>
```bash
cd backend && python -m pytest tests/ -x -q --timeout=60 && alembic upgrade head
```
All tests pass. No pending migrations. Decimal annotations in place.
</verification>

<success_criteria>
- All Numeric(x,y) columns in 6 model files annotated with Mapped[Decimal] or Mapped[Decimal | None]
- `from decimal import Decimal` imported in all 6 files
- No Alembic migration generated (confirms annotation-only change per D-15)
- Full test suite green
- ruff check exits 0 on all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/04-production-hardening-scale-preparation/04-05-SUMMARY.md`
</output>
