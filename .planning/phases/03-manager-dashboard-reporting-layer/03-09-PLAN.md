---
phase: 03-manager-dashboard-reporting-layer
plan: "09"
type: execute
wave: 2
depends_on: ["03-07"]
files_modified:
  - apps/manager/app/components/FleetComplianceBoard.tsx
  - apps/manager/app/components/BillingTripActions.tsx
  - apps/manager/app/components/FuelControlBoard.tsx
  - apps/manager/app/components/FleetHistoryBoard.tsx
  - apps/manager/app/components/TransportCargoBoard.tsx
  - apps/manager/app/components/TransportCargoActions.tsx
  - apps/manager/app/components/DriverDespachoTableAdmin.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "FleetComplianceBoard, BillingTripActions, FuelControlBoard, FleetHistoryBoard render identically using Tailwind classes"
    - "TransportCargoBoard, TransportCargoActions, DriverDespachoTableAdmin render identically using Tailwind classes"
    - "No custom CSS class strings from globals.css remain in any of the 7 migrated components"
    - "apps/manager builds without TypeScript errors after migration"
  artifacts:
    - path: "apps/manager/app/components/FleetComplianceBoard.tsx"
      provides: "FleetComplianceBoard migrated to Tailwind"
      contains: "border-line"
    - path: "apps/manager/app/components/BillingTripActions.tsx"
      provides: "BillingTripActions migrated to Tailwind (ready for waiver UI in Plan 10)"
      contains: "bg-panel"
    - path: "apps/manager/app/components/FuelControlBoard.tsx"
      provides: "FuelControlBoard migrated to Tailwind"
      contains: "text-muted"
  key_links:
    - from: "apps/manager/app/components/BillingTripActions.tsx"
      to: "apps/manager/app/lib/billing-api.ts"
      via: "waiver_status field expected in Plan 10 — preserve all API type references"
      pattern: "BillingTrip|billing-api"
---

<objective>
Migrate the remaining 7 existing manager components from custom CSS classes to Tailwind utility classes (D-03 second batch).

Purpose: Completes the full CSS-to-Tailwind migration mandated by D-03. These 7 components must be clean before the waiver UI (Plan 10) and /analytics page (Plan 11) can be built on top.

Output: 7 components fully migrated to Tailwind; builds clean; no globals.css custom classes remaining.
</objective>

<execution_context>
@C:\Users\Quive\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Quive\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/03-manager-dashboard-reporting-layer/03-CONTEXT.md
@.planning/phases/03-manager-dashboard-reporting-layer/03-UI-SPEC.md

@apps/manager/app/globals.css
@apps/manager/tailwind.config.ts

<interfaces>
<!-- Key CSS mappings from globals.css for these components -->

FleetComplianceBoard classes:
- .fleet-compliance-grid → `grid grid-cols-[repeat(auto-fill,minmax(280px,1fr))] gap-4`
- .fleet-compliance-panel → `bg-panel border border-line rounded-lg p-4`
- .fleet-compliance-item → `flex items-center justify-between py-2 border-b border-line last:border-0 text-sm`

BillingTripActions classes:
- .row-actions → `flex items-center gap-2 flex-wrap`
- .badge → `inline-flex items-center px-2 py-0.5 rounded-full text-xs font-bold`
- Action buttons → replace with shadcn Button component (variant="outline" size="sm") or Tailwind button classes

FuelControlBoard classes:
- .fuel-layout → `grid grid-cols-1 lg:grid-cols-[1fr_320px] gap-6`
- .fuel-metric → `bg-panel border border-line rounded-lg p-4`
- .tank-status → `bg-panel border border-line rounded-lg p-4`
- .tank-progress → `h-2 bg-line rounded-full overflow-hidden` (progress bar track)
- .tank-progress-fill → `h-full bg-green rounded-full` (progress bar fill)

FleetHistoryBoard classes:
- .history-split → `grid grid-cols-1 lg:grid-cols-2 gap-6`
- .history-panel → `bg-panel border border-line rounded-lg overflow-hidden`
- .history-event → `px-4 py-3 flex items-start gap-3 border-b border-line last:border-0`
- .history-marker → `w-2 h-2 rounded-full bg-blue mt-1.5 flex-shrink-0`

TransportCargoBoard / TransportCargoActions / DriverDespachoTableAdmin:
- Table wrappers → `bg-panel border border-line rounded-lg overflow-hidden`
- .table → Replace with shadcn Table component (import Table, TableHeader, TableBody, TableRow, TableHead, TableCell from @/components/ui/table)
- .table-wrap → `overflow-x-auto`
- .badge variants → use shadcn Badge component with variant prop or Tailwind classes
- .empty-row → `text-center py-8 text-muted text-sm`
- .tool-btn → `inline-flex items-center gap-1 px-3 py-1.5 text-sm border border-line rounded-md bg-panel hover:bg-soft cursor-pointer`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate FleetComplianceBoard, FuelControlBoard, FleetHistoryBoard</name>
  <files>
    apps/manager/app/components/FleetComplianceBoard.tsx,
    apps/manager/app/components/FuelControlBoard.tsx,
    apps/manager/app/components/FleetHistoryBoard.tsx
  </files>
  <read_first>
    - apps/manager/app/components/FleetComplianceBoard.tsx (read entire file)
    - apps/manager/app/components/FuelControlBoard.tsx (read entire file)
    - apps/manager/app/components/FleetHistoryBoard.tsx (read entire file)
    - apps/manager/app/globals.css (reference original CSS classes: .fleet-compliance-*, .fuel-*, .history-*)
    - apps/manager/tailwind.config.ts (confirm color token names)
  </read_first>
  <action>
For each of the three components, replace all custom className strings with Tailwind utility classes using the mappings in the interfaces section above.

**FleetComplianceBoard.tsx:** Replace .fleet-compliance-grid, .fleet-compliance-panel, .fleet-compliance-item. Keep compliance warning logic, props, and TypeScript interfaces unchanged.

**FuelControlBoard.tsx:** Replace .fuel-layout, .fuel-metric, .tank-status. For tank progress bars: replace .tank-progress and .tank-progress-fill with `h-2 bg-line rounded-full overflow-hidden` and `h-full bg-green rounded-full` respectively. The progress percentage (width) is set via inline style `style={{ width: \`${pct}%\` }}` — preserve this inline style.

**FleetHistoryBoard.tsx:** Replace .history-split, .history-panel, .history-event, .history-marker. Keep all data props and rendering logic unchanged.

After migration: verify no .fleet-compliance-*, .fuel-*, .history-* class strings remain in any of the three files.
  </action>
  <verify>
    <automated>cd /c/Users/Quive/OneDrive/Documents/Rotas/apps/manager && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "fleet-compliance-" apps/manager/app/components/FleetComplianceBoard.tsx` returns NO match
    - `grep "fuel-layout\|fuel-metric\|tank-status\|tank-progress" apps/manager/app/components/FuelControlBoard.tsx` returns NO match
    - `grep "history-split\|history-panel\|history-event" apps/manager/app/components/FleetHistoryBoard.tsx` returns NO match
    - `grep "border-line" apps/manager/app/components/FleetComplianceBoard.tsx` returns a match
    - `grep "bg-panel" apps/manager/app/components/FuelControlBoard.tsx` returns a match
    - `npx tsc --noEmit` from apps/manager exits 0
  </acceptance_criteria>
  <done>FleetComplianceBoard, FuelControlBoard, FleetHistoryBoard use only Tailwind classes; TypeScript clean</done>
</task>

<task type="auto">
  <name>Task 2: Migrate BillingTripActions, TransportCargoBoard, TransportCargoActions, DriverDespachoTableAdmin</name>
  <files>
    apps/manager/app/components/BillingTripActions.tsx,
    apps/manager/app/components/TransportCargoBoard.tsx,
    apps/manager/app/components/TransportCargoActions.tsx,
    apps/manager/app/components/DriverDespachoTableAdmin.tsx
  </files>
  <read_first>
    - apps/manager/app/components/BillingTripActions.tsx (read entire file — critical: this is where waiver UI will be added in Plan 10)
    - apps/manager/app/components/TransportCargoBoard.tsx (read entire file)
    - apps/manager/app/components/TransportCargoActions.tsx (read entire file)
    - apps/manager/app/components/DriverDespachoTableAdmin.tsx (read entire file)
    - apps/manager/app/lib/billing-api.ts (read — understand BillingTrip type fields; preserve all type references)
    - apps/manager/app/globals.css (reference .badge, .row-actions, .table, .table-wrap, .tool-btn, .empty-row)
  </read_first>
  <action>
Migrate all four components. Keep all logic, TypeScript types, API calls, and data rendering unchanged.

**BillingTripActions.tsx — key migration steps:**
- Replace `.row-actions` with `flex items-center gap-2 flex-wrap`
- Replace `.badge` strings with shadcn Badge import: `import { Badge } from "@/components/ui/badge"` and render `<Badge variant="outline">text</Badge>` or with explicit Tailwind classes for colored badges
- Replace action buttons with shadcn Button: `import { Button } from "@/components/ui/button"` and render `<Button variant="outline" size="sm">Cobrar</Button>`
- PRESERVE all onClick handlers, disabled logic, and prop access to BillingTrip fields
- Do NOT add waiver UI yet — that is Plan 10. Only migrate existing UI to Tailwind.

**TransportCargoBoard.tsx + TransportCargoActions.tsx:**
- Replace .table-wrap with `overflow-x-auto`
- Use shadcn Table components: `import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table"` — replace `<table>`, `<thead>`, `<tbody>`, `<tr>`, `<th>`, `<td>` with shadcn equivalents
- Replace .badge with shadcn Badge
- Replace .tool-btn buttons with shadcn Button (variant="ghost" size="sm")
- Replace .empty-row with `<TableRow><TableCell colSpan={N} className="text-center py-8 text-muted text-sm">...</TableCell></TableRow>`

**DriverDespachoTableAdmin.tsx:** Same table migration pattern as above.

After all four: no .badge, .row-actions, .table, .table-wrap, .tool-btn, .empty-row class strings remain.
  </action>
  <verify>
    <automated>cd /c/Users/Quive/OneDrive/Documents/Rotas/apps/manager && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "className=\"row-actions\"\|className=\"badge\"\|className=\"table-wrap\"\|className=\"tool-btn\"\|className=\"empty-row\"" apps/manager/app/components/BillingTripActions.tsx` returns NO match
    - `grep "className=\"table\"" apps/manager/app/components/TransportCargoBoard.tsx` returns NO match (native table replaced with shadcn Table)
    - `grep "from \"@/components/ui/table\"" apps/manager/app/components/TransportCargoBoard.tsx` returns a match
    - `grep "from \"@/components/ui/badge\"" apps/manager/app/components/BillingTripActions.tsx` returns a match
    - `grep "from \"@/components/ui/button\"" apps/manager/app/components/BillingTripActions.tsx` returns a match
    - `npx tsc --noEmit` from apps/manager exits 0
  </acceptance_criteria>
  <done>BillingTripActions, TransportCargoBoard, TransportCargoActions, DriverDespachoTableAdmin use Tailwind + shadcn components; TypeScript clean</done>
</task>

</tasks>

<verification>
```bash
cd /c/Users/Quive/OneDrive/Documents/Rotas/apps/manager && npm run build 2>&1 | tail -10
```
Build succeeds. No TypeScript errors.
</verification>

<success_criteria>
- All 7 components: zero custom CSS class strings from globals.css remaining
- shadcn Table used in table-rendering components
- shadcn Badge and Button used in action/status-rendering components
- `npm run build` in apps/manager succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-manager-dashboard-reporting-layer/03-09-SUMMARY.md`
</output>
