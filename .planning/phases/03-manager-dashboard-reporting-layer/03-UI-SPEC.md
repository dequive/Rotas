---
phase: 3
slug: manager-dashboard-reporting-layer
status: approved
reviewed_at: 2026-06-05T00:00:00Z
shadcn_initialized: false
preset: none
created: 2026-06-05
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for the Manager Dashboard + Reporting Layer.
> Generated by gsd-ui-researcher. To be verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (to be initialized in this phase — D-01, D-02) |
| Preset | none — shadcn default with custom CSS-var tokens (see Color section) |
| Component library | Radix UI (via shadcn) |
| Icon library | lucide-react ^0.468.0 (already installed) |
| Font | system-ui, Arial, Helvetica sans-serif (no web font — preserve existing body declaration) |

**shadcn migration gate (D-01 to D-04):** Tailwind CSS and shadcn/ui are NOT yet installed in `apps/manager/`. The first task of this phase installs `tailwindcss`, `@tailwindcss/postcss`, and `shadcn/ui`. All existing custom CSS class names (`tower-metric`, `worklist`, `fleet-fact`, `badge`, `.tool-btn`, etc.) are migrated to Tailwind utilities in the same phase. The `:root` CSS variables in `globals.css` are preserved — they become Tailwind arbitrary-value tokens, e.g. `bg-[var(--nav)]`, `text-[var(--muted)]`.

**Third-party registries:** None. All components sourced from shadcn official registry only.

---

## Spacing Scale

Declared values (multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gap, inline badge padding, list divider offset |
| sm | 8px | Compact list item padding, gap between action buttons, badge pill padding |
| md | 16px | Default element spacing, card inner padding, form field gap |
| lg | 24px | Page content padding (`main` area inset), section dividers, modal body gap |
| xl | 32px | Gap between major dashboard sections |
| 2xl | 48px | Reserved — not used in Phase 3 dashboard layouts |
| 3xl | 64px | Reserved — not used in Phase 3 dashboard layouts |

Exceptions:
- Sidebar width: 248px (existing, preserve as-is — not on 8-point but matches existing shell grid)
- KPI metric card minimum height: 86px (tower-metric pattern from existing globals.css — preserve)
- Modal widths: 540px (default), 680px (wide — waiver approval modal) — these are layout dimensions, not spacing tokens
- Nav item touch target: 44px minimum height on mobile (WCAG 2.5.5 — override compact 34px in media query)

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | implicit default (400) | 1.5 |
| Label / caption | 12px | 700 (bold) | 1.4 |
| Heading (section) | 21px | 800 (extrabold) | 1.2 |
| Display (page title / KPI metric value) | 26px | 800 (extrabold) | 1.2 |

**Declared weights:** 700 (bold — labels, captions, table headers) and 800 (extrabold — section headings, page titles, KPI metric values). Body text uses the browser implicit default weight (400) and is not a declared weight in this contract.

**KPI metric values:** Fleet count and stat values in `.tower-metric strong` and `.metric strong` render at 26px extrabold (`text-[26px] font-extrabold`) — same scale as page display titles. This distinguishes fleet counts at a glance without introducing a fifth size.

**Source:** Extracted from existing `globals.css` — `.title h1` is 26px, `.domain-heading h2` is 21px, table cells and body copy are 14px, captions and metadata are 12px. Weights confirmed in `.brand`, `.tool-btn`, `.worklist-item span`, `.table th`.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#f5f7fa` (`--soft`) | Page background, input backgrounds, empty state fills |
| Secondary (30%) | `#ffffff` (`--panel`) + `#102033` (`--nav`) | Cards, panels (`--panel`); sidebar, nav column (`--nav`) |
| Accent (10%) | `#2563eb` (`--blue`) | Primary CTAs, active nav state, metric card left-border accent, link text in worklist items, period selector underline |
| Destructive | `#b42318` (`--red`) | Negative margin badge, reject waiver button, error messages, disabled action feedback |

Accent (`--blue`) reserved for:
- Primary action buttons: "Exportar PDF", "Exportar XLSX", "Submeter pedido", "Associar contrato", "Cobrar"
- Active/hover state on sidebar nav links
- Left border accent on KPI tower-metric cards
- Period range selector active state on `/analytics` filter bar
- Pairing code display text (existing pattern)

Additional semantic colors (source: existing `:root`):
- `#16794c` (`--green`): Positive margin badge, success states, delivery validated badge, tank fill indicator
- `#b45309` (`--orange`): Document expiry warning at 30 days, negative-margin cost items, fuel low-stock alert
- `#0e7490` (`--cyan`): Secondary informational badges (e.g. "Em trânsito" trip status)
- `#667085` (`--muted`): All secondary text, labels, captions, metadata lines
- `#d8dee8` (`--line`): All dividers, borders, input borders, card borders

**Document expiry severity color mapping (D-23):**
- 30 days remaining: `--orange` (#b45309) badge
- 15 days remaining: `--red` (#b42318) light tint badge (background `#fee4e2`, text `#b42318`)
- 7 days remaining: `--red` (#b42318) solid badge (background `#b42318`, text `#ffffff`)

---

## Component Inventory

Shadcn components to install (`npx shadcn add <component>`):

| Component | Usage in Phase 3 |
|-----------|-----------------|
| `button` | All CTAs, action buttons, toolbar buttons |
| `dialog` | Waiver request modal (D-12), waiver approval modal (D-13), export job progress modal |
| `table` | Billing queue table, trips table, analytics driver summary table |
| `badge` | Billing status, document expiry severity, trip margin indicator, waiver status |
| `card` | KPI metric cards (`/analytics`), Control Tower metric tiles |
| `select` | Period filter dropdown, contract selector, vehicle/driver filter |
| `input` | Waiver justification (single-line), form fields in modals |
| `textarea` | Waiver justification (multi-line in modal — D-12) |
| `separator` | Section dividers replacing `.section-divider` class |
| `skeleton` | Loading state for KPI cards and table rows during data fetch |

No `DatePicker` from shadcn — the analytics period filter uses a `<select>` with preset options (este mês, últimos 3 meses, custom range). Custom date range uses two `<input type="date">` elements styled with shadcn `Input`.

---

## Interaction Contracts

### Export Job Flow (D-08, D-09)

1. Manager clicks "Exportar PDF" or "Exportar XLSX" button.
2. Button enters loading state: spinner icon + "A gerar..." label. Button is disabled. No modal opens yet.
3. Frontend calls export endpoint → receives `job_id`.
4. Polling begins at 2-second intervals, max 60 attempts (2 minutes total).
5. While polling: inline spinner below the button, text "A processar exportação…".
6. On `done`: button label changes to "Descarregar" with a download icon. No page reload.
7. On `failed`: inline error text in `--red`: "Exportação falhou. Tente novamente."
8. On timeout (60 attempts exhausted): inline error text: "A exportação está a demorar. Tente novamente mais tarde."
9. Download triggers via `<a href="/api/v1/jobs/{job_id}/download" download>` — no new tab, authenticated via cookie.

### Waiver Flow (D-11 to D-14)

**Request path (any manager/admin/owner):**
1. Billing queue row with `margin < 0` shows badge "Margem negativa" (orange) and button "Solicitar waiver".
2. Click opens a `Dialog` (540px wide) with: trip summary (route, margin value in red), mandatory textarea "Justificativa (obrigatório)", "Abandonar pedido" (ghost) and "Submeter pedido" (primary) buttons.
3. "Submeter pedido" is disabled until textarea has ≥ 10 characters.
4. On submit: button shows spinner + "A submeter…". On success: dialog closes, row badge updates to "Waiver pendente" (blue).
5. Error: inline `form-error` pattern inside dialog.

**Approval path (owner/admin only — modal only visible to these roles):**
1. Rows with `waiver_status = pending_approval` show button "Rever waiver" (cyan outline).
2. Click opens a `Dialog` (680px wide — modal-wide) with: financial detail (margin, revenue, costs), justificativa do gestor, "Rejeitar waiver" (destructive outline) and "Aprovar waiver" (primary) buttons.
3. On approve: dialog closes, row badge changes to "Aprovado" (green), trip enters billing queue.
4. On reject: dialog closes, row badge changes to "Rejeitado" (red), trip stays blocked.
5. Destructive confirmation: "Rejeitar waiver" button has NO additional confirmation dialog — the rejection is reversible (a new waiver can be requested). Single click is sufficient.

### KPI Analytics Page (`/analytics`) (D-05 to D-07)

- Filter bar at top: period `<Select>` (este mês / últimos 3 meses / personalizado) + optional vehicle `<Select>` + optional driver `<Select>`. On change, refetch immediately (no submit button).
- Custom period: reveals two `<Input type="date">` fields (De / Até). Apply button triggers refetch.
- KPI cards use `<Card>` with `<Skeleton>` during load. No full-page spinner — cards show individually as data resolves.
- Document expiry panel is a dedicated section at bottom of `/analytics` page, also surfaced in Control Tower header (D-22).

### Pagination (D-20)

All table/queue views use cursor-style page navigation: "Anterior" / "Próximo" buttons (shadcn `Button` variant ghost). Default page size: 50. No infinite scroll.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA — export PDF | "Exportar PDF" |
| Primary CTA — export XLSX | "Exportar XLSX" |
| Primary CTA — waiver request | "Solicitar waiver" |
| Primary CTA — waiver approve | "Aprovar waiver" |
| Primary CTA — waiver reject | "Rejeitar waiver" |
| Primary CTA — billing | "Cobrar" |
| Export in progress | "A gerar…" (button) / "A processar exportação…" (inline) |
| Export ready | "Descarregar" |
| Export failed | "Exportação falhou. Tente novamente." |
| Export timeout | "A exportação está a demorar. Tente novamente mais tarde." |
| Waiver submit button | "Submeter pedido" |
| Waiver dialog dismiss (request modal) | "Abandonar pedido" |
| Empty state — billing queue | Heading: "Nenhuma viagem para cobrar" / Body: "As viagens entram aqui após a descarga ser validada. Valide as descargas pendentes na secção Operação." |
| Empty state — analytics KPIs | Heading: "Sem dados para o período" / Body: "Não existem viagens concluídas no período selecionado. Experimente alargar o intervalo de datas." |
| Empty state — document expiry | Heading: "Sem documentos a vencer" / Body: "Todos os documentos de viaturas e motoristas estão válidos por mais de 30 dias." |
| Error state — API failure | "Não foi possível carregar os dados. Atualize a página ou contacte o suporte se o problema persistir." |
| Error state — waiver form | "Ocorreu um erro ao submeter o pedido. Verifique a ligação e tente novamente." |
| Waiver justification placeholder | "Descreva o motivo da margem negativa e a justificação para prosseguir com a cobrança…" |
| Negative margin badge | "Margem negativa" |
| Waiver pending badge | "Waiver pendente" |
| Waiver approved badge | "Aprovado" |
| Waiver rejected badge | "Rejeitado" |
| Document expiry badge (30d) | "Vence em {N} dias" |
| Document expiry badge (7d) | "Vence em {N} dias" (red solid) |
| Destructive action — reject waiver | No confirmation dialog required (rejection is reversible — no copy needed beyond button label "Rejeitar waiver") |

**Language:** All copy is Portuguese (pt-MZ). No English strings in the UI. Error messages from the API (snake_case codes) must be mapped to Portuguese human-readable messages before display.

---

## Layout Contract

### Shell (preserved from existing)
- Grid: `248px sidebar | 1fr main content`
- Sidebar background: `--nav` (#102033)
- Main content background: `--soft` (#f5f7fa)
- Main padding: 24px (md token) on desktop, 16px on mobile

### Focal Points

**`/analytics` page:** Primary visual anchor is the 4-card KPI row immediately below the filter bar. Metric values render at 26px extrabold — the largest text on the page — drawing the eye before section headings or table content. The left-border accent in `--blue` on each card reinforces this as the primary data zone.

**Control Tower page:** Primary visual anchor is the tower-metric grid at the top of the page (active trips, vehicles in field, pending alerts). These metric cards retain the same 26px extrabold treatment after the Tailwind migration, ensuring visual hierarchy is preserved from the existing custom CSS pattern.

### Sidebar Navigation (D-05 — add /analytics entry)

New nav entry to add to `SidebarLayout.tsx`:
```
{ key: "analytics", label: "Análise", href: "/analytics", icon: BarChart2 }
```
Position: between "Viagens" and "Contratos".
Icon: `BarChart2` from lucide-react.

### `/analytics` Page Layout

```
[Page header: "Análise de Frota" + subtitle]
[Filter bar: Período | Viatura | Motorista]
─────────────────────────────────────────
[KPI row: 4 cards — custo/km | utilização% | consumo L/100km | viagens concluídas]
─────────────────────────────────────────
[Driver summary table — viagens, km, custo total por motorista]
─────────────────────────────────────────
[Document expiry alert panel — vehicles + drivers expiring in 30/15/7 days]
```

### Control Tower Addition (D-22)
Document expiry alerts surface as a collapsible section at the bottom of the existing Control Tower page. The section header shows total count of expiring documents with color-coded severity dot.

---

## Migration Contract (D-03)

Components to migrate from custom CSS classes to Tailwind utilities, in order:

1. `SidebarLayout.tsx` — `.shell`, `.sidebar`, `.brand`, `.nav`, `.nav a`, `.logout-btn`, `.main`
2. `ControlTowerOverview.tsx` — `.tower-grid`, `.tower-metric`, `.worklist-grid`, `.worklist`, `.worklist-item`, `.fleet-strip`, `.fleet-fact`
3. `CostMarginBoard.tsx` — `.cost-kpis`, `.cost-panel`, `.cost-item`, `.cost-work-grid`, `.margin-governance`
4. `FleetComplianceBoard.tsx` — `.fleet-compliance-grid`, `.fleet-compliance-panel`, `.fleet-compliance-item`
5. `BillingTripActions.tsx` — `.row-actions`, `.badge`, action buttons
6. `FuelControlBoard.tsx` — `.fuel-layout`, `.fuel-metric`, `.tank-status`, `.tank-progress`
7. `FleetHistoryBoard.tsx` — `.history-split`, `.history-panel`, `.history-event`, `.history-marker`
8. `TransportCargoBoard.tsx`, `TransportCargoActions.tsx`, `DriverDespachoTableAdmin.tsx` — remaining custom classes
9. All modal components — `.modal-backdrop`, `.modal`, `.modal-header`, `.modal-form`, `.modal-actions`
10. Page-level layout — `.page-header`, `.table`, `.table-wrap`, `.badge` variants, `.empty-row`

CSS variable preservation rule: All `:root` vars in `globals.css` are kept. Tailwind config extends `colors` with these vars so they can be used as `bg-nav`, `text-muted`, `border-line`, etc.

Tailwind config extension (to declare):
```ts
// tailwind.config.ts
colors: {
  nav:   'var(--nav)',
  soft:  'var(--soft)',
  panel: 'var(--panel)',
  ink:   'var(--ink)',
  muted: 'var(--muted)',
  line:  'var(--line)',
  blue:  'var(--blue)',
  green: 'var(--green)',
  orange:'var(--orange)',
  red:   'var(--red)',
  cyan:  'var(--cyan)',
}
```

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | button, dialog, table, badge, card, select, input, textarea, separator, skeleton | not required — official registry, no vetting gate needed |

No third-party registries declared. Registry safety gate: not applicable.

---

## Accessibility Constraints

- All interactive elements must have visible focus rings (shadcn default ring behavior — do not suppress `outline: none` globally).
- Waiver dialogs must trap focus (shadcn Dialog uses Radix FocusTrap by default — confirm not overridden).
- Document expiry badges must not convey severity by color alone — include text ("Vence em N dias") alongside color coding.
- Table columns with financial values must use `text-right` alignment for readability.
- Aria labels: table sort headers must have `aria-sort`. Filter selects must have visible `<label>` elements.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
