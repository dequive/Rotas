---
phase: 13-frontend-completeness
plan: 04
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-02
  - 13-03
files_modified: []
autonomous: false
requirements:
  - FE-01
  - FE-02
  - FE-03
  - FE-04

must_haves:
  truths:
    - "TypeScript compiler finds zero errors in the manager app after all Phase 13 changes"
    - "All four sidebar pages load without blank screens or runtime errors"
    - "Every sidebar entry in the Config section links to a functional page"
  artifacts:
    - path: "apps/manager/app/alertas/AlertsClient.tsx"
      provides: "Three-view alert triage with acknowledge + resolve"
    - path: "apps/manager/app/settings/SettingsClient.tsx"
      provides: "Full settings with user management and tenant config"
    - path: "apps/manager/app/cobranca/actions.ts"
      provides: "Issue billing document action"
    - path: "apps/manager/app/cobranca/IssueDocumentButton.tsx"
      provides: "Client Component for document issue"
  key_links:
    - from: "SidebarLayout.tsx (alertas entry)"
      to: "/alertas page"
      via: "href=/alertas"
      pattern: "href.*alertas"
    - from: "SidebarLayout.tsx (settings entry)"
      to: "/settings page"
      via: "href=/settings"
      pattern: "href.*settings"
---

<objective>
Verify that all Phase 13 implementation is complete and TypeScript-clean. This plan is
a checkpoint that runs tsc, reviews the sidebar entries, and confirms each page loads.
No code changes are expected — this plan catches any errors introduced in plans 01-03.

Purpose: Phase 13 success criterion is "a manager who clicks any sidebar entry reaches a
functional page". This verification plan confirms that criterion is met.

Output: A clean tsc pass and human confirmation that the four pages are functional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-frontend-completeness/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript full check and fix any errors</name>
  <files></files>
  <action>
Run TypeScript check across the entire manager app:

```bash
cd apps/manager && npx tsc --noEmit 2>&1
```

Review ALL errors (not just Phase 13 files). Fix any errors introduced by Phase 13 changes
in the alertas/, settings/, and cobranca/ directories.

Common issues to watch for:
- Missing type imports (UserResponse, TenantData, DriverBasic defined locally in page.tsx
  but not imported in SettingsClient.tsx — they need to be co-located or use inline types)
- Prop type mismatches between page.tsx and SettingsClient props interface
- Missing `"use client"` on IssueDocumentButton.tsx
- StatusBadge prop type — check that `status` prop accepts the driver status strings
  ("active", "inactive", "suspended"); if not, pass `label` prop with the raw string and
  use a neutral status key

If StatusBadge doesn't accept arbitrary status strings, use this pattern instead of
`<StatusBadge status={d.status} />`:
```tsx
<span className={`inline-flex items-center gap-1.5 text-[12px] font-semibold ${
  d.status === "active" ? "text-success" : "text-muted"
}`}>
  <span className={`h-1.5 w-1.5 rounded-full ${
    d.status === "active" ? "bg-success" : "bg-muted"
  }`} />
  {d.status}
</span>
```

Fix all errors before proceeding to the checkpoint.
  </action>
  <verify>
    `cd apps/manager && npx tsc --noEmit 2>&1 | grep -c "error TS"` outputs `0`
  </verify>
  <done>Zero TypeScript errors in apps/manager</done>
</task>

<task type="auto">
  <name>Task 2: Sidebar completeness audit</name>
  <files></files>
  <action>
Read SidebarLayout.tsx and verify every `href` in NAV_SECTIONS resolves to an existing
page directory:

Check each route:
- `/` → apps/manager/app/page.tsx — OK (control tower)
- `/viagens` → apps/manager/app/viagens/ — check exists
- `/motoristas` → apps/manager/app/motoristas/ — check exists
- `/viaturas` → apps/manager/app/viaturas/ — check exists
- `/manutencao` → apps/manager/app/manutencao/page.tsx — COMPLETE (Phase 13.5)
- `/terceiros` → apps/manager/app/terceiros/ — check exists (Phase 24)
- `/clientes` → apps/manager/app/clientes/ — check exists
- `/contratos` → apps/manager/app/contratos/ — check exists
- `/cobranca` → apps/manager/app/cobranca/page.tsx — COMPLETE (Phase 13-03)
- `/ar` → apps/manager/app/ar/ — check exists
- `/analytics` → apps/manager/app/analytics/ — check exists
- `/rotas-config` → apps/manager/app/rotas-config/ — check exists
- `/alertas` → apps/manager/app/alertas/page.tsx — COMPLETE (Phase 13-01)
- `/security` → apps/manager/app/security/ — check exists
- `/settings` → apps/manager/app/settings/page.tsx — COMPLETE (Phase 13-02)
- `/motoristas#despacho` → apps/manager/app/motoristas/ — check exists

For any route that does NOT have a page.tsx (will return 404 or Next.js error), document
it. Do NOT create stub pages for missing routes in this plan — record which ones are
missing in the SUMMARY.md.

Use: `ls apps/manager/app/{route}/page.tsx 2>/dev/null || echo "MISSING: {route}"`
for each route. Run this check programmatically.
  </action>
  <verify>
    Command: `for d in viagens motoristas viaturas terceiros clientes contratos ar analytics rotas-config security; do ls "apps/manager/app/$d/page.tsx" 2>/dev/null && echo "OK: $d" || echo "MISSING: $d"; done`
    Output shows all routes have page.tsx or documents which are missing.
  </verify>
  <done>
    Audit complete — all sidebar routes either verified as functional or documented as known gaps.
    The four Phase 13 targets (alertas, settings, cobranca, manutencao) are all confirmed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 13 Frontend Completeness — four pages fully wired:
    - /alertas: three-view alert triage (Ativos/Reconhecidos/Resolvidos) with acknowledge + resolve actions
    - /settings: user management (list/invite/role change), tenant config (timezone/currency/whatsapp), driver roster
    - /cobranca: Emitir button wired on draft billing documents
    - /manutencao: already complete from Phase 13.5 (no changes)
    - TypeScript: zero errors
  </what-built>
  <how-to-verify>
    With the manager dev server running (cd apps/manager && npm run dev — port 3030):

    1. Visit http://localhost:3030/alertas
       - Confirm "Documentos a Expirar" tab shows document expiry cards (or empty state)
       - Click "Alertas de Sistema" tab
       - Confirm filter buttons: Ativos, Reconhecidos, Resolvidos are visible
       - If any alerts exist: confirm "Reconhecer" and "Resolver" buttons appear on Ativos
       - Click "Reconhecer" on an alert — confirm it moves to Reconhecidos after refresh

    2. Visit http://localhost:3030/settings
       - Confirm Perfil tab works (existing functionality)
       - Click "Gestão de Acessos" — confirm user list appears (not "Em breve")
       - If you are owner/admin: confirm invite form is visible with email/name/role fields
       - Click "Preferências" — confirm tenant settings form and driver list appear (not "Em breve")

    3. Visit http://localhost:3030/cobranca
       - Scroll to "Documentos Fiscais" section at the bottom
       - Confirm draft documents (status "Rascunho") show an "Emitir" button
       - Confirm issued documents (status "Emitido") show "Registar Pagamento" button
       - Top "Gerar Documento" button should be disabled/grayed out

    4. Visit http://localhost:3030/manutencao
       - Confirm 4 tabs: Ordens de Trabalho, Peças e Stock, Ferramentas, Planos Preventivos
       - All should show real data or empty states (no errors)

    5. Click every sidebar entry — confirm none shows a blank screen or 404.
  </how-to-verify>
  <resume-signal>Type "approved" if all pages work as described, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `cd apps/manager && npx tsc --noEmit` exits with 0 errors
2. All four Phase 13 pages functional (human verified above)
3. No sidebar entry produces a blank screen
4. Phase 13 success criteria from ROADMAP met:
   - FE-01: /manutencao shows real work orders (already complete)
   - FE-02: /cobranca has Emitir action wired
   - FE-03: /alertas has acknowledge + resolve actions with Ativos/Reconhecidos/Resolvidos views
   - FE-04: /settings has user management and tenant config (no "Em breve" stubs)
</verification>

<success_criteria>
- Zero TypeScript errors in apps/manager
- Human confirmation that all four pages are functional
- Sidebar audit documented — any missing routes recorded in SUMMARY
- Phase 13 ROADMAP success criteria 1-4 all met
</success_criteria>

<output>
After completion, create `.planning/phases/13-frontend-completeness/13-04-SUMMARY.md`

Include in the summary:
- TypeScript error count before/after
- Sidebar audit results (which routes have pages, which are missing)
- Any issues found during human verification and how they were fixed
</output>
