---
phase: 02-pwa-offline-first-completion
plan: 08
type: execute
wave: 3
depends_on:
  - 02-05
  - 02-06
  - 02-07
files_modified: []
autonomous: false

requirements:
  - PWA-01
  - PWA-02
  - PWA-03
  - AUTH-01
  - AUTH-02

must_haves:
  truths:
    - "Chrome 'Add to Home Screen' prompt appears on Android device running the app"
    - "App launches in standalone mode with no browser chrome after installation"
    - "All views (dashboard, fuel, trip stop, delivery proof) load and function in airplane mode"
    - "Records created in airplane mode appear in the manager dashboard within 60 seconds of reconnecting"
    - "Manager session does not silently expire after 15 minutes — token refresh works"
    - "SW sw.js is served with Cache-Control: no-store header (visible in DevTools Network tab)"
    - "Lighthouse PWA installability audit reports no blocking issues"
  artifacts:
    - path: ".planning/phases/02-pwa-offline-first-completion/02-FIELD-TEST-CHECKLIST.md"
      provides: "Completed field test checklist documenting actual results"
  key_links: []
---

<objective>
Field testing checkpoint: verify the PWA on a real Android device before closing Phase 2. This plan pauses execution for human testing and documents results. Phase 2 cannot close until this checkpoint is signed off.

Purpose: Background Sync API compatibility must be confirmed on real Android hardware (Samsung Galaxy A-series or Tecno equivalent) — Chrome DevTools emulation cannot substitute for real device testing. This is a hard requirement from STATE.md.
Output: Completed field test checklist. Phase 2 sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-pwa-offline-first-completion/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run backend test suite and build both apps</name>
  <files></files>
  <read_first>
    - .planning/phases/02-pwa-offline-first-completion/02-VALIDATION.md (sampling requirements)
  </read_first>
  <action>
Run all automated verifications before the human checkpoint:

1. Full backend test suite:
```bash
cd backend && python -m pytest -x -q 2>&1
```
All tests must pass. If any fail, fix before proceeding to the checkpoint.

2. Backend specific Phase 2 tests:
```bash
cd backend && python -m pytest tests/test_sync_update.py tests/test_driver_revocation.py tests/test_token_refresh.py -v 2>&1
```
All 12 tests must be GREEN.

3. Driver PWA build:
```bash
cd apps/driver && npx vite build 2>&1
```
Must exit 0. Check dist/sw.js exists.

4. Manager TypeScript check:
```bash
cd apps/manager && npx tsc --noEmit 2>&1
```
Must exit 0.

5. Create field test checklist file `.planning/phases/02-pwa-offline-first-completion/02-FIELD-TEST-CHECKLIST.md` with the checklist template (pre-populated with test steps, results to be filled in by human tester):

```markdown
# Phase 2 — Field Test Checklist

**Device:** [to be filled]
**Android version:** [to be filled]
**Chrome version:** [to be filled]
**Date:** [to be filled]
**Tester:** [to be filled]

## Automated Pre-Flight (Claude fills before checkpoint)

- [ ] `cd backend && python -m pytest -x -q` — ALL PASS
- [ ] `cd backend && python -m pytest tests/test_sync_update.py tests/test_driver_revocation.py tests/test_token_refresh.py -v` — ALL PASS
- [ ] `cd apps/driver && npx vite build` — exits 0, dist/sw.js exists
- [ ] `cd apps/manager && npx tsc --noEmit` — exits 0

## Device Tests (Human fills during device testing)

### PWA-02: Installability

- [ ] Open app in Android Chrome on target device
- [ ] Three-dot menu shows "Add to Home Screen" or install banner appears
- [ ] App installs to home screen without error
- [ ] Launching from home screen: no browser address bar visible (standalone mode)
- [ ] App name on home screen shows "Motorista" (short_name)

### PWA-01 / PWA-03: Offline Operation

- [ ] Enable airplane mode on device
- [ ] Navigate to dashboard view — loads without error
- [ ] Navigate to fuel view — loads without error
- [ ] Navigate to trip stop view — loads without error
- [ ] Create a new trip entry in airplane mode — no error message
- [ ] Record a fuel log in airplane mode — no error message
- [ ] SyncStatusBanner shows orange "Sem ligação — a gravar localmente" during airplane mode
- [ ] Counter "X registos pendentes" increments as records are added

### PWA-01: Background Sync on Reconnect

- [ ] Disable airplane mode
- [ ] SyncStatusBanner transitions to green "A sincronizar..."
- [ ] Within 60 seconds: offline records appear in manager dashboard
- [ ] SyncStatusBanner returns to idle (hidden) after sync completes
- [ ] Check manager dashboard: all offline records present and correct

### PWA-01: SW Cache-Control Header

- [ ] Open Chrome DevTools (desktop) → Application → Service Workers
- [ ] Confirm sw.js is registered and active
- [ ] Open Network tab → filter for sw.js → confirm Cache-Control: no-store header

### AUTH-01: Manager Token Refresh

- [ ] Login to manager dashboard
- [ ] Wait 15 minutes (or temporarily reduce token TTL in dev env)
- [ ] Navigate to any manager page — page loads normally (no redirect to /login)
- [ ] Verify no 401 errors in browser Network tab after 15 minutes

### AUTH-02: Driver Token Refresh After Extended Offline

- [ ] With driver app open, go offline for > 15 minutes (or test with short TTL token)
- [ ] Come back online — sync proceeds without 401 error in logs
- [ ] SyncStatusBanner does NOT show "Sessão expirada" on reconnect

### Lighthouse PWA Audit

- [ ] Run Lighthouse PWA audit: `npx lighthouse <device-accessible-url> --only-categories=pwa --output=json`
- [ ] Installability: PASS (no blocking issues)
- [ ] PWA score: record actual score here: [____]

## Results

**Overall result:** [ ] PASS  [ ] FAIL — issues listed below

**Issues found:**
[list any issues]

**Sign-off:** [name] on [date]
```
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_sync_update.py tests/test_driver_revocation.py tests/test_token_refresh.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest -x -q` exits 0
    - `cd apps/driver && npx vite build` exits 0
    - `ls apps/driver/dist/sw.js` confirms sw.js exists
    - `.planning/phases/02-pwa-offline-first-completion/02-FIELD-TEST-CHECKLIST.md` exists
    - `grep "Automated Pre-Flight" .planning/phases/02-pwa-offline-first-completion/02-FIELD-TEST-CHECKLIST.md` returns a match
  </acceptance_criteria>
  <done>All automated tests pass. Both apps build. Field test checklist created in .planning/phases/02-pwa-offline-first-completion/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 2 PWA implementation:
    - PWA-01: Service Worker with vite-plugin-pwa (injectManifest), workbox-background-sync for sync/batch, network-first for APIs, cache-first for assets
    - PWA-02: Web App Manifest with ROTAS Motorista branding, 192x192 and 512x512 icons
    - PWA-03: Cache strategy implemented in sw.ts
    - AUTH-01: Manager silent token refresh via refreshAccessToken() server action + apiFetch 401 retry
    - AUTH-02: Driver pairDevice() now stores refresh_token; refreshAccessToken() with in-memory lock; request() 401 retry
    - AUTH-04: sync/batch update operations for trip, fuel_log, trip_stop, delivery_proof — all return processed
    - D-08: backend returns driver_access_revoked error code when DriverDevice.is_active=False
    - SyncStatusBanner: 7-state banner with offline/syncing/error/session_expired/access_revoked/update_available states
  </what-built>
  <how-to-verify>
    Complete the field test checklist at:
    `.planning/phases/02-pwa-offline-first-completion/02-FIELD-TEST-CHECKLIST.md`

    Priority tests (minimum required to close Phase 2):

    1. **PWA Installability** — On Android Chrome:
       - Navigate to the driver app URL
       - Confirm "Add to Home Screen" appears
       - Install and launch — confirm standalone mode (no address bar)

    2. **Offline Operation** — Enable airplane mode, then:
       - All driver views load
       - Can create trip, fuel, stop records
       - SyncStatusBanner shows orange "Sem ligação — a gravar localmente"

    3. **Sync on Reconnect**:
       - Disable airplane mode
       - SyncStatusBanner transitions to green "A sincronizar..."
       - Records appear in manager dashboard within 60 seconds

    4. **SW Cache-Control** — In Chrome DevTools:
       - Network tab → filter sw.js → Cache-Control response header = `no-store`

    5. **Manager Token Refresh**:
       - Wait >15 min after login OR test with reduced token TTL
       - Manager pages continue loading without redirect to /login

    Fill in the checklist file with actual results. For any FAIL items, describe the issue.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all Priority Tests pass.
    Type "issues: [description]" to report failures for gap closure planning.
  </resume-signal>
</task>

</tasks>

<verification>
Automated pre-flight:
```bash
cd backend && python -m pytest -x -q
cd apps/driver && npx vite build
```

Phase 2 closes only after the checkpoint is approved with all priority tests passing on a real Android device.
</verification>

<success_criteria>
- All automated tests pass: backend suite GREEN, driver build GREEN, manager TypeScript clean
- Field test checklist completed with results
- Checkpoint approved: PWA installable + offline works + sync on reconnect + sw.js no-store header confirmed
- Phase 2 STATE.md updated to complete
</success_criteria>

<output>
After completion, create `.planning/phases/02-pwa-offline-first-completion/02-08-SUMMARY.md`
Update `.planning/ROADMAP.md` Phase 2 status to complete.
Update `.planning/STATE.md` phases table to mark Phase 2 complete.
</output>
