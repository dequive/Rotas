# PR-05 — Revalidação do Runbook até `rec13`

Data: 2026-07-26

Origem: snapshot sintético vazio em `95929ae669b9`

Destino: restore isolado e upgrade até `rec13`

## Veredicto

O runbook atualizado passou localmente até ao head canónico `rec13`. Esta
execução prova o percurso técnico de dump, restore, upgrade, monitorização de
locks, verificação de revisão e detecção de drift. Não substitui o ensaio com
snapshot real anonimizado, volume representativo ou tráfego concorrente.

O relatório estruturado está em
`docs/evidence/PR05_RUNBOOK_REC13_SMOKE_20260726.json`.

## Resultados

| Medida | Resultado |
| --- | ---: |
| Revisão de origem | `95929ae669b9` |
| Revisão final | `rec13 (head)` |
| Dump | 470.480 bytes |
| SHA-256 | `5cc71adfba54bb897479b51ea762c822f525ba3474dd6ed786dfc1e79a8f7173` |
| Dump | 2,606 s |
| Restore | 39,214 s |
| Upgrade | 29,554 s |
| Amostras de lock | 43 |
| Máximo de locks em espera | 0 |
| Erros do monitor | 0 |
| `alembic check` | sem novas operações |

Foram observados `AccessExclusiveLock`, `AccessShareLock`, `ExclusiveLock`,
`RowExclusiveLock`, `RowShareLock`, `ShareLock` e `ShareRowExclusiveLock`.
Logo, continua obrigatória uma janela de manutenção até existir ensaio sob
carga representativa em staging.

Depois do gate, a partição de regressão de payables, índices compostos,
separação de roles, isolamento cross-tenant, RLS e Oficina passou:

```text
26 passed, 1 warning in 10.54s
```

O warning corresponde ao rate limiter em memória porque `REDIS_URL` foi
deliberadamente removido para o teste local.

## Limitações

- A origem desta revalidação estava vazia; a preservação de dados sintéticos
  contabilísticos foi demonstrada separadamente no ensaio anterior até
  `rec11`.
- Os testes de regressão foram executados na base canónica local depois do
  gate, não no destino descartável já removido.
- Não houve tráfego concorrente representativo.
- Não houve snapshot anonimizado de staging/produção.
- O restore de rollback ainda não foi executado por operador independente.

PR-05 permanece `in_progress`.
