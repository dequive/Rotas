---
phase: "15.1"
plan: "02"
subsystem: "billing"
status: complete
tags: [ddl, migration, billing, fiscal]
dependency_graph:
  requires: ["01"]
  provides: [billing_document_types_ddl]
  affects: [15.1-04, 15.1-05]
tech_stack:
  added: []
  patterns: [alembic-migration, sqlalchemy-mapped-columns]
key_files:
  created:
    - backend/alembic/versions/NEW_add_billing_document_types.py
  modified:
    - backend/app/modules/billing/models.py
    - backend/app/modules/billing/service.py
decisions:
  - "document_type defaults to 'invoice' server-side via SQLAlchemy default"
  - "parent_document_id is nullable FK referencing billing_documents.id (self-referential)"
  - "client_nuit stored as String(20) matching Mozambican NUIT format"
metrics:
  duration: "15 minutes"
  completed_date: "2026-06-18"
  tasks_completed: 3
  files_changed: 3
---

# Phase 15.1 Plan 02: Wave 1A — DDL billing_documents extension (FDOC-01) Summary

## One-liner

Added `document_type`, `parent_document_id`, `due_date`, and `client_nuit` columns to `billing_documents` via Alembic migration + ORM model update.

## What Was Built

- Alembic migration adding 4 columns to `billing_documents` table with backfill (`document_type='invoice'` for existing rows)
- `BillingDocument` ORM model updated with 4 new `Mapped` fields
- `serialize_billing_document()` updated to return all 4 new fields

## Verification

All FDOC-01 tests pass:
- `test_billing_document_has_document_type_column` ✓
- `test_billing_document_type_defaults_to_invoice` ✓

## Deviations from Plan

None.

## Commits

| Hash | Message |
|------|---------|
| 406533c | feat(15.1-02): extend billing_documents with document_type, parent_document_id, client_nuit (FDOC-01) |

## Self-Check: PASSED

- [x] `billing_documents` has `document_type`, `parent_document_id`, `due_date`, `client_nuit`
- [x] BillingDocument ORM model exposes all four fields
- [x] Existing rows default to `document_type='invoice'`
- [x] `serialize_billing_document` returns all four new fields
