---
phase: 03-manager-dashboard-reporting-layer
plan: "12"
type: execute
wave: 4
depends_on: ["03-02", "03-03", "03-04", "03-05", "03-06", "03-08", "03-09", "03-10", "03-11"]
files_modified: []
autonomous: false

requirements:
  - CT-01
  - CT-02
  - CT-03
  - BILL-01
  - BILL-02
  - BILL-03
  - RPT-01
  - RPT-02

must_haves:
  truths:
    - "All backend tests pass: pytest tests/ -x -q exits 0"
    - "Control Tower loads in under 3 seconds for a tenant with at least 5 vehicles"
    - "A manager can export an invoice as PDF and the PDF opens correctly with no corrupt characters"
    - "A manager can export an invoice as XLSX and the file opens in a spreadsheet application"
    - "The waiver flow works end-to-end: request → approval → trip enters billing"
    - "The /analytics page loads and shows KPI cards and document expiry panel"
    - "All migrated components render without visible regressions vs before Tailwind migration"
---

<objective>
Human verification checkpoint for Phase 3 — confirm all requirements are met end-to-end before marking the phase complete.

Purpose: Automated tests verify correctness of isolated units. This checkpoint verifies the full user journey across CT performance, billing exports, waiver workflow, and analytics KPIs.

Output: Phase 3 verified and ready to close.
</objective>

<execution_context>
@C:\Users\Quive\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Quive\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-manager-dashboard-reporting-layer/03-CONTEXT.md
@.planning/phases/03-manager-dashboard-reporting-layer/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full backend test suite</name>
  <files></files>
  <read_first>
    - backend/tests/ (list test files to confirm all Wave 0 test files exist)
  </read_first>
  <action>
Run the complete backend test suite and report results:

```bash
cd /c/Users/Quive/OneDrive/Documents/Rotas/backend && python -m pytest tests/ -v --tb=short 2>&1
```

Expected: all tests pass. If any test fails, fix the root cause before continuing to the human verification checkpoint.

Also run the targeted per-requirement checks:
```bash
python -m pytest tests/test_cross_tenant_isolation.py tests/test_control_tower_api.py tests/test_control_tower_optimized.py -v -q
python -m pytest tests/test_billing_export.py tests/test_waiver_flow.py tests/test_billing_domain.py -v -q
python -m pytest tests/test_analytics_api.py -v -q
```
  </action>
  <verify>
    <automated>cd /c/Users/Quive/OneDrive/Documents/Rotas/backend && python -m pytest tests/ -x -q --tb=short 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ -x -q` exits 0 (no test failures)
    - `python -m pytest tests/test_cross_tenant_isolation.py -x -q` exits 0
    - `python -m pytest tests/test_control_tower_optimized.py -v -q 2>&1 | grep "PASSED" | wc -l` returns ≥ 4 (stubs turned green)
    - `python -m pytest tests/test_billing_export.py -v -q 2>&1 | grep "PASSED" | wc -l` returns ≥ 2 (UTF-8 PDF + XLSX format tests green)
    - `python -m pytest tests/test_waiver_flow.py -v -q 2>&1 | grep "PASSED" | wc -l` returns ≥ 3 (waiver lifecycle tests green)
    - `python -m pytest tests/test_analytics_api.py -v -q 2>&1 | grep "PASSED" | wc -l` returns ≥ 2 (KPI + document expiry tests green)
  </acceptance_criteria>
  <done>Full backend test suite passes; all Phase 3 requirement tests (CT, billing, analytics) are green</done>
</task>

<task type="auto">
  <name>Task 2: Verify frontend builds clean</name>
  <files></files>
  <read_first>
    - apps/manager/package.json (confirm Tailwind and shadcn deps present)
  </read_first>
  <action>
Run the Next.js production build to confirm no TypeScript or build errors:

```bash
cd /c/Users/Quive/OneDrive/Documents/Rotas/apps/manager && npm run build 2>&1
```

Also run TypeScript check:
```bash
cd /c/Users/Quive/OneDrive/Documents/Rotas/apps/manager && npx tsc --noEmit 2>&1
```

If build fails due to missing shadcn peer dependencies, install them:
```bash
npm install class-variance-authority clsx tailwind-merge lucide-react
```
  </action>
  <verify>
    <automated>cd /c/Users/Quive/OneDrive/Documents/Rotas/apps/manager && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (no build errors)
    - `npx tsc --noEmit` exits 0 (no TypeScript errors)
    - Build output includes `/analytics` route (grep for "analytics" in Next.js build output)
    - Build output includes all migrated component routes (home, viaturas, motoristas, viagens, contratos)
  </acceptance_criteria>
  <done>apps/manager builds clean with no TypeScript errors; all routes including /analytics are included</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 3 complete — all 8 requirements implemented:
    - CT-01: Control Tower query rewrite (N+1 eliminated, ≤6 queries)
    - CT-02: Redis cache-aside for CT KPIs (TTL 60s, stampede lock)
    - CT-03: CT queue pagination (page/page_size params)
    - BILL-01: PDF export with fpdf2 + DejaVuSans (UTF-8 safe)
    - BILL-02: XLSX export with openpyxl (bold headers, currency format)
    - BILL-03: Negative margin waiver workflow (request → approve → billing)
    - RPT-01: /analytics page with KPI cards and driver summary
    - RPT-02: Document expiry alerts panel (30/15/7 day severity levels)

    Frontend: Full Tailwind + shadcn migration completed for all existing manager components.
  </what-built>
  <how-to-verify>
    Start both services:
    ```bash
    # Terminal 1 — backend
    cd backend && python -m uvicorn app.main:app --port 8000 --reload

    # Terminal 2 — frontend
    cd apps/manager && npm run dev
    ```

    Then verify each requirement:

    **CT-01 / CT-02 / CT-03:**
    1. Open http://localhost:3030 (or deployed URL). Open browser Network tab.
    2. Navigate to the main Control Tower dashboard.
    3. In Network tab, confirm the /api/v1/control-tower/ response time is under 3 seconds.
    4. On second load, confirm response comes faster (Redis cache hit reduces DB round-trips).

    **BILL-01 — PDF export:**
    5. Go to billing queue. Find a trip with a validated delivery proof.
    6. Click "Exportar PDF".
    7. Button should show "A gerar…" then "Descarregar".
    8. Click "Descarregar". Open the PDF. Verify Mozambican names with diacritics (ã, ç, â, ê) display correctly — no '?' or corrupted characters.

    **BILL-02 — XLSX export:**
    9. On the same billing document, click "Exportar XLSX".
    10. Open the downloaded file in LibreOffice Calc or Excel.
    11. Verify: header row is bold; currency columns (unit price, total) display with 2 decimal places and right-alignment.

    **BILL-03 — Waiver workflow:**
    12. In billing queue, find or create a trip with a negative margin.
    13. Confirm "Margem negativa" badge (orange) and "Solicitar waiver" button are visible.
    14. Click "Solicitar waiver". Dialog should open with textarea.
    15. Try submitting with < 10 characters — button should remain disabled.
    16. Fill textarea with ≥ 10 characters. Submit. Row badge should update to "Waiver pendente".
    17. Log in as owner/admin. The "Rever waiver" button (cyan outline) should appear.
    18. Click "Rever waiver". Approval dialog opens (680px). Click "Aprovar waiver".
    19. Row badge should update to "Aprovado". Trip should now be billable (no 409 error when billing).

    **RPT-01 — Analytics KPIs:**
    20. Navigate to /analytics (sidebar "Análise" item).
    21. Page should show 4 KPI cards: Custo médio/km, Utilização de frota, Consumo L/100km, Viagens concluídas.
    22. Change period filter to "Últimos 3 meses". KPI cards should refresh (loading skeletons briefly visible).
    23. Driver summary table should show rows if trips exist in the selected period.

    **RPT-02 — Document expiry:**
    24. On the /analytics page, scroll to "Vencimento de Documentos" section.
    25. If any vehicles or drivers have documents expiring within 30 days, they should appear with colored severity badges.
    26. Badge colors: orange (≤30d), red-light (#fee4e2, ≤15d), red-solid (≤7d).
    27. Each badge shows "Vence em N dias" text (not just color).

    **Frontend migration:**
    28. Navigate through all pages: home, viaturas, motoristas, viagens, contratos, /analytics.
    29. Visual appearance should match the existing design (no broken layout, no missing styles).
    30. Sidebar background should be dark navy (#102033 = --nav). Active nav item should have white/semi-transparent highlight.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 30 verification steps pass, or describe which step failed and what the symptom is.

    Common issues and fixes:
    - PDF shows corrupt chars: check DejaVuSans.ttf is in backend/app/modules/billing/fonts/ and fpdf2 is installed
    - Waiver 403 error: check RBAC role assignment in test user; owner/admin role required for approve endpoint
    - /analytics page 404: confirm apps/manager/app/analytics/page.tsx exists and npm run build succeeded
    - KPI cards show "—" always: check GET /api/v1/analytics/kpis endpoint is registered in main.py; check tenant_id in JWT
  </resume-signal>
</task>

</tasks>

<verification>
All automated tests pass. Human verification checkpoint completed successfully.
```bash
cd /c/Users/Quive/OneDrive/Documents/Rotas/backend && python -m pytest tests/ -q --tb=short
cd /c/Users/Quive/OneDrive/Documents/Rotas/apps/manager && npm run build
```
</verification>

<success_criteria>
- Full backend test suite: 0 failures
- Frontend build: exits 0, no TypeScript errors
- Human verification: all 8 requirements (CT-01 through RPT-02) confirmed working end-to-end
- Phase 3 status can be updated to "complete" in STATE.md and ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/03-manager-dashboard-reporting-layer/03-12-SUMMARY.md`

Also update:
- `.planning/STATE.md`: mark Phase 3 as complete, update progress bar to Phase 3 100%
- `.planning/ROADMAP.md`: mark all Phase 3 plans as complete ([x])
</output>
